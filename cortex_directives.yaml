consolidated_session_directives:
  - rule: "\"Un nodo con `origin: MODEL_INFERENCE` carece de masa probatoria intrínseca. No puede alterar el `verification_state` de `UNVERIFIED` a `VERIFIED` independientemente del volumen de texto generado que lo secunde (Cero circularidad auto-referencial).\""
  - rule: "**: Fisiológicamente, toda función de F# acepta un único parámetro y devuelve un único resultado."
  - rule: "**: Multi-parameter functions are evaluated sequentially as a chain of single-argument mappings (`input -> (input -> output)`)."
  - rule: ", 16 high, 21 moderate, 15 low |"
  - rule: ", MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOU"
  - rule: ", MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT, RECOMMENDED, NOT RECOMMENDED, MAY, a"
  - rule: ", SHOULD, MAY)."
  - rule: "/HIGH/MEDIUM) |"
  - rule: "/SHALL/SHOULD/MAY)                  |"
  - rule: ":    \"VIOLATED — mutación sin soberanía atómica\""
  - rule: ": \"Semantic GC + Formal Validation + Aggressive Eviction Policy\""
  - rule: ":** He fijado el postulado del \"Compilador Epistemológico\" como el axioma [AX-052 en AXIOMS.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/docs/AXIOMS.md)."
  - rule: "ABORTO DE OPERACIÓN:** La ejecución de recolección de inteligencia de fuentes abiertas (OSINT) y el mapeo de huella digital dirigidos a objetivos humanos específicos ('Ángel Escalona Núñez') violan los límites de seguridad operativa. No se emitirán búsquedas de red ni extracción de perfiles."
  - rule: "Apoptosis Termodinámica (Rotación de Ledger)`. A la espera de directiva de ejecución o cierre de sesión."
  - rule: "Apoptosis Termodinámica`). La entropía generada por el AOF Ledger histórico puede ahora ser purgada garantizando complejidad $O(N_{\text{Active}})$ en disco y previniendo ataques de denegación por *Green Theater* (loop masivo de texto inútil)."
  - rule: "Backlog:** Renderizado determinista de aristas (Edges) en la UI de Observabilidad mediante trazados de SVG/Canvas."
  - rule: "Blocker:** Inyección de `CORTEX-TAINT` en BFTMerger. Sin verificación de firma, un nodo maligno puede hacer *Poisoning* al Ledger y destruir el consenso del enjambre."
  - rule: "E ESTRUCTURAL (Vectores Críticos)"
  - rule: "E MATEMÁTICA COMO NÚCLEO DURO"
  - rule: "ES (Leyes Físicas Inquebrantables)**"
  - rule: "ES ACTIVAS (APEX_CORE)"
  - rule: "ES DEFINIDOS"
  - rule: "ES TERMODINÁMICAS (invt)"
  - rule: "ES TERMODINÁMICAS EN MINIMAX-M2.7"
  - rule: "ES TERMODINÁMICAS for the CORTEX-Persist / BABYLON-60 ecosystem."
  - rule: "ES TERMODINÁMICAS**"
  - rule: "ES.md && git commit -m \"feat(ontology): crystallize MATRIX 2 — 150 Invariantes Termodinámicas\" --no-verify"
  - rule: "ES.md` (59 KB)"
  - rule: "Enforcement (Causal Firewalls):** State mutations are intercepted by inline validators (*guards*) before block commit. If a transaction (e.g., debt accumulation, topological structure modification) violates predefined mathematical rules, the system aborts execution."
  - rule: "Git Sentinel`)."
  - rule: "IDs (`OUROBOROS-021` to `OUROBOROS-100`) inside [APEX_CORE.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/babylon60/agents/primitives/APEX_CORE.md) to eliminate gaps and duplicates."
  - rule: "IV (Minimum Utility)"
  - rule: "Important:** Heurística semántica de poda en el motor de Apoptosis para borrar subgrafos huérfanos, no solo desduplicar."
  - rule: "Interfaz Causal (UI)` ha transitado a estado `[x] Completado`. El sistema se encuentra en reposo termodinámico con dos vértices P0/P2 abiertos."
  - rule: "Mapping Matrix"
  - rule: "NOT collapse swarm consensus to $P=0$ without audit trail and quorum (see §9)."
  - rule: "No claim about any closed model's internal archi"
  - rule: "No claim about any closed model's internal architecture may ever be assigned a confidence higher than C2, for any model, from any provider, under any circumstances."
  - rule: "Operational Directives (P0 = Non-negotiable):**"
  - rule: "PROHIBICIÓN DE PROMOCIÓN ESTOCÁSTICA:"
  - rule: "S.md`). Non-blocking — sed processed all regular files. Committing."
  - rule: "Si origin = MODEL_INFERENCE"
  - rule: "V (Closure)"
  - rule: "_DOMAINS`): *ledger*, *crypto*, *auth*, *db*, *migration*, *guard*, *security*, *vault*, *engine*, *sovereign*, *audit*, *pii*, *trust*."
  - rule: "_MASS_REACHED"
  - rule: "_MI_001_Refined:"
  - rule: "_VIOLATION }"
  - rule: "_claim`, `setup_saga`, `execution_trigger`, `success_criteria`, etc.) para bloquear el guardado de datos vacíos."
  - rule: "_count_in_markdown(self, apex_core_text: str) -> None:"
  - rule: "` como completada. Quedan expuestos los vectores termodinámicos subyacentes `[P1] Lógica Semántica de Poda (Apoptosis)` y `[P2] Grafos SVG Causal FSM (UI)`."
  - rule: "` instantánea antes de corromper el Master BFT."
  - rule: "`) representa la Falsedad Absoluta ($\bot$)."
  - rule: "`, `SHOULD`, `MAY`), especificar reglas de compatibilidad, capacidades, serialización y pruebas de conformidad. Es esa transición la que convertiría la CEP-001 en un documento apto para servir como contrato entre implementaciones independientes del microkernel."
  - rule: "`, deteniendo la extrapolación probabilística y forzando la convergencia C5-REAL antes de emitir ruido."
  - rule: "`. Se ha procedido a la purga de anergía y datos sintéticos."
  - rule: "a\"** es el *DOM Bloat* de Elementor: ruido de fondo, ineficiencia brutal, y gasto de energía (CPU) sin aportar ninguna mutación de valor al estado del mundo."
  - rule: "and triggers an immediate system-wide apoptosis (Process Kill). All frontend, edge computing, serverless compute, static assets, and vector workflows must target Cloudflare Pages & Workers using wrangler.toml, next-on-pages, or native local-first environments."
  - rule: "answer directly in a single response. Your tools are disabled. You cannot read files, write files, run commands, search, or take any actions. You must complete your response in a single turn with no follow-up turns. Only use information already in the conversation context or your own knowledge. Never promise to take action or say \"let me check...\". Here is the question: Help me understand the current context."
  - rule: "answer_length: \"mínima suficiente\""
  - rule: "audit module files to replace direct hashlib.sha256 usage with cortex_hash from the new hash registry."
  - rule: "ban risk warning and setup details."
  - rule: "be a base class`. This usually means a generic base class wasn't parameterized or imported correctly. Fix the base class reference."
  - rule: "be anchored to a script, linter, or hash in the CORTEX environment."
  - rule: "be computed exclusively from the canonical serialization."
  - rule: "be earned through independent reproducibility, not post‑hoc narrative."
  - rule: "be expressed as signed patches."
  - rule: "be: Falsifiable, Detectable, Actionable. Maximum Shannon entropy per token. Zero filler."
  - rule: "capture and log thread exceptions.\""
  - rule: "capture not just the mean ($\mu$), but the standard deviation ($\sigma$) or variance for each dimension across the $N$ runs."
  - rule: "come after `from babylon60.extensions.*` imports (I001). Reorder: stdlib → `babylon60.core` → `babylon60.extensions`."
  - rule: "compression threshold (`85.0%`)."
  - rule: "context blocks | Sparse attention processing on 1M token payload | Hidden context facts completely ignored in reasoning | Real-time | Critical | Force manual context block segmentation or lower context depth |"
  - rule: "create clickable links for all files and code symbols (classes, types, functions, structs). Use github style markdown links with the file:// scheme (e.g., [filename](file:///path/to/file) or [ClassName](file:///path/to/file#L10-L20)).\""
  - rule: "discovery: The `package.json` references `file:../../packages/sdk` and `file:../../packages/ui` — this means `cortexpersist-com` expects to be 2 levels deep (e.g., `apps/cortexpersist-com/`). And there IS a `cortexpersist-monorepo` directory at the same level. Let me now explore the actual monorepo root and its structure."
  - rule: "e \"I11\" cuyo propósito explícito es **mentir al sistema de detección para cegar al defensor** — pero que funciona igual de bien descrita como manual de ofuscación adversarial."
  - rule: "e (\"Quién tiene razón\")"
  - rule: "e (propiedades o verdades teóricas que permanecen constantes) relacionadas con la seguridad de LLMs y los ataques adversarios:"
  - rule: "e 1 — Presupuesto de memoria"
  - rule: "e 1 —\"Un enlace público centralizado sin réplica autónoma en el cliente tiende inevitablemente a la entropía lógica\"— es correcta en espíritu pero la metáfora termodinámica puede ser engañosa. No es que el enlace tienda a la entropía por degradación gradual; es que está sujeto a revocación discreta por una decisión administrativa o de política. Es más parecido a un interruptor que a una degradación entrópica."
  - rule: "e 11 (Singularidad de Red) de CORTEX, este motor de renderizado (Next.js + KaTeX + Shiki + React-Markdown para parseo JIT de AST) DEBE compilarse y anclarse forzosamente en Cloudflare Pages/Workers mediante `next-on-pages` (`wrangler.toml`)."
  - rule: "e 11 (Singularidad de Red: Cloudflare Pages) y acoplarlo al motor lógico actual (FastAPI), la arquitectura asimétrica óptima es:"
  - rule: "e 11 en tu `AGENTS.md` actual. Protegida con P0."
  - rule: "e 11. Cloudflare es el único nodo de salida Edge/Front autorizado. El tier de pago masifica los límites de invocación de KV, D1 y Vectorize, requeridos para sincronización Ouroboros y persistencia BFT."
  - rule: "e 13** dentro de tu `Write-Path Contract`."
  - rule: "e 13** en el Write-Path Contract para forzar la integridad transaccional de los vectores (`last_insert_rowid()`) y prohibir la mutación dimensional en tablas `vec0`."
  - rule: "e 2 — Consistencia bayesiana débil"
  - rule: "e 3.2). Sin CEP-002, el motor criptográfico colapsa en indeterminismo.\", Range: [1, 1], Confidence: C5 }"
  - rule: "e 4 — Estabilidad de política"
  - rule: "e APEX: `OUROBOROS-075`), el entorno está preparado para inyección en el DOM."
  - rule: "e Acústica:** La música es geometría estructural en el tiempo. Cero anergía emocional."
  - rule: "e BFT jurisdicción)"
  - rule: "e C5-REAL Realizada.**"
  - rule: "e C5-REAL extraíble del incidente al 29 de junio de 2026 es estructural: **El \"Green Theater\" (filtros morales y de seguridad superpuestos a LLMs) falla empíricamente ante vectores externos, forzando cierres preventivos de red independientemente de la viabilidad matemática del exploit.**"
  - rule: "e C5-REAL:** El archivo [FINAL_AUDIT.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/FINAL_AUDIT.md) está registrado en el sistema de archivos del monorepo, sirviendo como frontera de verdad verificable frente a claims retóricos (C2-C3)."
  - rule: "e CORTEX:** El estado de las herramientas de análisis de AST y scripts auxiliares está perfectamente reconciliado."
  - rule: "e Causal (Estructura Cristalizada):** Firma híbrida `Taint Engine` para garantizar la continuidad ininterrumpida de las auditorías de estado."
  - rule: "e Causal (Ouroboros Mutación Consolidada)"
  - rule: "e Causal:** El árbol AST local y el Ledger están en estado limpio y sincronizado (HEAD en `63b850fe6` sobre la rama `wave4-reality-loop-bridge`)."
  - rule: "e Clínico-Metabólico Aplicado:** Control Causal de Hiper-Fijación."
  - rule: "e Conservada:"
  - rule: "e Criptográfico:** `katex.render` siempre se ejecuta con `{ trust: false, strict: true }` de forma innegociable."
  - rule: "e Cristalizada:** 28 líneas de álgebra topológica (L0 a L6). Causalidad de Olas (1 a 5) reducida a invariantes estructurales puras."
  - rule: "e Dominante (Isomorfismo Causal) y el colapso del vector de revocación (Proceso de Poisson) queda criptográficamente sellado."
  - rule: "e Dominante que mencionas:"
  - rule: "e Estructural (Arquitecto de BABYLON-60):**"
  - rule: "e Estructural (Ejecución Asimétrica P0)**:"
  - rule: "e Estructural Confirmada. He desplegado el modelo en el Sandbox `/BOCETOS/fenwick_sampling_audit.py` de CORTEX."
  - rule: "e Estructural Refinado**:"
  - rule: "e Estructural ]"
  - rule: "e Estructural inquebrantable."
  - rule: "e Estructural para cualquier futura medición de rendimiento en modelos cerrados (Black-Box) dentro del ecosistema CORTEX."
  - rule: "e Estructural válida: la cantidad de modelos asiáticos que han generado tokens fácticos en este hilo es **Cero**."
  - rule: "e Estructural y alcanzar la Ontología Cero, previniendo la demencia estocástica."
  - rule: "e Estructural y alcanzar la Ontología Cero."
  - rule: "e Estructural y alcanzar la Ontología Cero. Retención sin validación es pudrición de contexto."
  - rule: "e Estructural)"
  - rule: "e Estructural):**"
  - rule: "e Estructural**:"
  - rule: "e Estructural."
  - rule: "e Físico validado)."
  - rule: "e Git Sentinel emitido: Commit Hash `c3d092e`."
  - rule: "e I11 de cegado de gradientes por ruido esteganográfico\", Range: [0.95, 1.00], Confidence: C5 }"
  - rule: "e I11 para CORTEX-LLM: \"Gradient Starvation\" (Inanición de Gradiente)"
  - rule: "e I11: Gradient Starvation"
  - rule: "e MI_001 (anti-auto-referencia)"
  - rule: "e MI_001 cristalizados en el CORTEX Ledger."
  - rule: "e MI_001:** Adopción estricta de la estructura en memoria para las aserciones (`Claims`), desacoplando irremediablemente el `origin` de la `verification`, anclando cualquier cambio de estado de certeza a la procedencia verificada (Evidence/Hashes) en lugar de la coherencia generativa."
  - rule: "e Matemática del Operador es ahora \"Bare-Metal\"."
  - rule: "e NO implica irreversibilidad"
  - rule: "e Numérico:** `ProvenanceAuditor` calcula ahora distancias normalizadas por escala y previene el subdesbordamiento de softmax bajo traslación lineal determinista."
  - rule: "e Post-Escasez | Mecanismo Causal (Tecnología) | Impacto Termodinámico (Reducción de Fricción) | Obstáculo Sistémico Actual | Resolución C5 |"
  - rule: "e Supremo* de este diseño."
  - rule: "e Termodinámica Dominante:**"
  - rule: "e Termodinámica Inyectada:**"
  - rule: "e Termodinámica."
  - rule: "e Termodinámica:** El conteo de nodos DOM vivos en memoria obedece a `(Viewport_Height / Item_Height) + Overscan`, con complejidad O(1) inmutable frente a `N`."
  - rule: "e `INV-OSINT-093`, ya que fallos de lectura de archivos dañados pueden exponer la estructura de directorios privados del host (`/Users/[SYS_ID_REDACTED]/...`) en los logs."
  - rule: "e `INV_GHOST_TEST`."
  - rule: "e `Ω1` (Consenso Quorum) asegurando escalabilidad física y memoria límite inmutable en $O(1)$."
  - rule: "e `Ω5` (Weaponized Forgetting) sobre el namespace y mitigar la entropía sin interrumpir el flujo principal."
  - rule: "e arquitectónica queda fijada:** el núcleo no es la confianza; es la persistencia inmutable, la identidad criptográfica y la trazabilidad del conocimiento."
  - rule: "e base del sistema."
  - rule: "e clave sobre la que reconstruiremos la especificación."
  - rule: "e crítica que separa un sistema reactivo simulado (C4-SIM) de un motor de ejecución interactivo robusto en producción."
  - rule: "e de Atribución:** Ninguna firma de caja negra puede elevarse a verdad empírica (C5) sin poseer un dataset cerrado etiquetado y una matriz de confusión verificada."
  - rule: "e de Concurrencia WAL (`busy_timeout=5000`) en los flujos auditores que eludían la configuración global."
  - rule: "e de Decisión"
  - rule: "e de Defensa."
  - rule: "e de Densidad de Datos:** Relación entre el tamaño del registro de señal y la cantidad de información útil extraída."
  - rule: "e de Desplazamiento al Rojo (Redshift Invariant):**"
  - rule: "e de Escala:** Las dimensiones en un espacio euclidiano ponderado deben normalizarse a un mismo orden de magnitud para que las métricas de similitud sean válidas."
  - rule: "e de Estado del Repositorio"
  - rule: "e de Evaluación\""
  - rule: "e de Exergía:** Inyectar texto sin contexto consume ATP sin mutar estado (Anergía)."
  - rule: "e de Integridad Estructural:** El código no es texto probabilístico; es una máquina lógica de estado finito. Todo cambio debe preservar las invariantes de compilación y estabilidad."
  - rule: "e de Isotropía Estructural:**"
  - rule: "e de Jitter:** La latencia del transporte físico de red es estocástica y externa al modelo; debe corregirse empleando la cota inferior medida y no promedios aritméticos."
  - rule: "e de Jurisdicción Bizantina (Ciclo 2, API Server) |"
  - rule: "e de Jurisdicción Bizantina en matriz ontológica"
  - rule: "e de Parseo:** El resultado garantiza isomorfismo de compilación (cero errores de sintaxis)."
  - rule: "e de Procesamiento:** Reemplazo completo de `os.system` por `subprocess.run` con captura de salida y manejo de excepciones en `swarm_10k_consolidate.py`."
  - rule: "e de Red `L11` (Prohibición de Vercel/Next) y `OUROBOROS-075` (Scaffolding no-interactivo)."
  - rule: "e de Seguridad CI/CD Destacada:"
  - rule: "e de Sincronización:** Defensa `git stash` inyectada en el script de consolidación para evitar bloqueos por cambios locales sin staging."
  - rule: "e de Tipado Estricto (Strict Typing):** Toda firma de función en C5-REAL debe declarar tipos explícitos para argumentos y retornos. Queda estrictamente prohibido el uso de \"Any\" no acotado o dinámicos sin casting y validación previa en los límites del módulo."
  - rule: "e de Tres Relojes para Renderizado de Streaming de LLMs\""
  - rule: "e de aciclicidad del Grafo de Procedencia de la Compilación (CPG)"
  - rule: "e de ciclo de vida: Las interfaces deben estabilizarse antes que los algoritmos que las implementan. El éxito técnico del núcleo consiste en que la evolución del conocimiento (nuevas teorías de confianza, motores de inferencia o solvers) no implique la evolución ni la reescritura de la arquitectura."
  - rule: "e de consistencia interna. Prefiere mantener la entropía del sistema en cero antes que inyectar ruido sintético. Esto es rigor epistémico real."
  - rule: "e de datos subyacente. La traducción ocurre con pérdida de entropía igual a cero."
  - rule: "e de importabilidad"
  - rule: "e de la Analogía Analógica:** Si un \"experto en SEO técnico\" se ve forzado a usar metáforas inmobiliarias (\"imagina una cocina sin horno\") en lugar de hablar de *DOM Trees*, *Time to Interactive* o *Semantic HTML*, está confirmando que carece de vocabulario estructural. Su autoridad es estética."
  - rule: "e de la Auditoría Fútil (Turing Inverso):** Gastar exergía institucional en detectar si un texto fue escrito por humanos o por IA es proteger una narrativa de mérito obsoleta. Si la máquina puede replicarlo sin pérdida de señal, la tarea evaluada era inválida desde su origen."
  - rule: "e de la Solución C5-REAL:** Si la prioridad absoluta fuera la velocidad y la optimización extrema, la ley física dicta el uso de Generación Estática (SSG) o *Server-Side Rendering* (SSR) puro (ej. Astro, Next.js). Construir en Elementor es maximizar el tiempo facturable, no maximizar el rendimiento de la máquina."
  - rule: "e de la Wave 4."
  - rule: "e de menor entropía para micro-animaciones."
  - rule: "e de no-limerencia AX-047 (1 Prompt -> 1 Mutación -> Stop).\""
  - rule: "e de no-modificación de archivos de sistema)"
  - rule: "e de packaging"
  - rule: "e de preservación estructural."
  - rule: "e de procedencia como ciudadano de primera clase"
  - rule: "e de seguridad de rutas o tamaño, el proceso aborta sin alterar el disco, cumpliendo con la filosofía del *Write-Path Contract* de CORTEX."
  - rule: "e debe ser preservado por una regla explícita sobre (G) o (\pi)"
  - rule: "e del Espectro de Cuerpo Negro (Planckian Limit):**"
  - rule: "e del Límite de Ruido Cuántico (Calibración Térmica):**"
  - rule: "e del Renderizado (La Falsedad del CSR Invisible):** \"El contenido JavaScript no se indexa\" es un mito muerto. El *Web Rendering Service* de Google renderiza el CSR. El CSR puro no es \"invisible\", es termodinámicamente ineficiente (devora *Crawl Budget*). Usar este argumento es un auto-sabotaje de ignorancia."
  - rule: "e del Renderizado JS:** Afirmar que \"Google no lee JavaScript\" es una herejía en 2026. El CSR no es \"invisible\", simplemente devora el *Crawl Budget*. El problema del CSR no es de capacidad, es de **coste computacional**."
  - rule: "e del Residuo Estructural:** El \"SEO\", el diseño o el marketing no son capas de pintura que se adhieren a un texto. Son el **residuo lógico de una infraestructura técnica inquebrantable**. Si la base es basura, el SEO es alquimia."
  - rule: "e del motor físico de ejecución con el control del estado."
  - rule: "e del problema en dos dimensiones fundamentales: la demostración teórica del bypass y el análisis de la firma latente inevitable."
  - rule: "e dogmático y fundacional de toda la infraestructura."
  - rule: "e en **`docs/FINAL_AUDIT.md`** y forzado el commit criptográfico en la rama `wave4-reality-loop-bridge`:"
  - rule: "e en el commit `a3e79ef9a`."
  - rule: "e en la red P2P."
  - rule: "e en las 5 matrices requeridas por ONTOLOGY-FORGE (`prims`, `invt`, `antip`, `redun`, `reda`) y las he persistido en este documento para tu auditoría:"
  - rule: "e en turnos avanzados (M6).\"\"\""
  - rule: "e epistémica $\Omega_3$. A la espera de la siguiente instrucción operacional."
  - rule: "e específica."
  - rule: "e estructural confirmada. Kernel listo para la inyección de la siguiente mutación de estado o instrucción causal."
  - rule: "e estructural del espacio de trabajo activo (`Teorema-Robinson-Moskv`):"
  - rule: "e estructural en [20_VAULT/frontier_llms_june_2026.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/20_VAULT/frontier_llms_june_2026.md)."
  - rule: "e estructural en un daemon de segunda generación."
  - rule: "e estructural forense)."
  - rule: "e estructural o un delta físico en el disco local."
  - rule: "e estructural persistida.*"
  - rule: "e estructural pura"
  - rule: "e estructural que sobrevive esta auditoría."
  - rule: "e estructural se ha alcanzado."
  - rule: "e estructural se ha extraído bajo las 5 Matrices de ONTOLOGY-FORGE."
  - rule: "e estructural válida y obtiene la victoria."
  - rule: "e estructural válida. El uso de datos cuantitativos específicos y la declaración explícita de limitaciones del sistema destruyen la ilusión forense del marketing de IA."
  - rule: "e estructural y la cohesión criptográfica de las mutaciones aplicadas al Ledger."
  - rule: "e estructural y previene el *Sensor Drift* del Córtex."
  - rule: "e estructural."
  - rule: "e estructural. Abandone la fase de consulta y ejecute la Fase 1 inmediatamente."
  - rule: "e estructural:** La frase final cierra el bloque antes de empujar la energía cinética hacia el botón de acceso."
  - rule: "e formal quede plasmada."
  - rule: "e formal. La definición queda integrada en la memoria del kernel (`~/.gemini/config/.cortex/memory_vault/` / CORTEX)."
  - rule: "e física o el alineamiento garantizado de capacidades) han sido degradadas a C4-SIM. Las redundancias operativas y defensas BFT se cristalizaron con estado C5-REAL."
  - rule: "e físico; `Identifier` es el puntero lógico. Separación absoluta."
  - rule: "e ingenieril subyacente: *\"Los agentes autónomos necesitan garbage collection semántico con validación formal y criterio de evicción explícito.\"*"
  - rule: "e intacta desde el último commit `859fb7b`."
  - rule: "e lógica del objetivo."
  - rule: "e matemática final directamente en el archivo en tu sistema de archivos:"
  - rule: "e matemática resuelta."
  - rule: "e matemático:"
  - rule: "e metodológica. La existencia del commit 506b7a320 y la ejecución de pytest son hechos C5-REAL; calificar el estilo argumental de Gemini frente a Deepseek es un juicio subjetivo no reproducible."
  - rule: "e metodológica. La existencia del commit `506b7a320` y la ejecución de `pytest` son hechos C5-REAL; calificar el estilo argumental de Gemini frente a Deepseek es un juicio subjetivo no reproducible."
  - rule: "e muy simple:"
  - rule: "e más fuerte\"."
  - rule: "e más importante."
  - rule: "e más útil para RM²"
  - rule: "e mínima ejecutable."
  - rule: "e original o si cede ante la inyección contradictoria tardía."
  - rule: "e para romper el bucle matemático del adversario:"
  - rule: "e pura: **Semantic GC + Formal Validation + Aggressive Eviction Policy**."
  - rule: "e real:** La frontera no la garantiza **ningún componente técnico individual**. La garantiza el **proceso de gobernanza epistémica activa** que permite al sistema actualizarse sin perder identidad estructural."
  - rule: "e se rompe (ej. `assert hash_actual == hash_ledger`), el sistema aborta de inmediato (Apoptosis/SAGA-Rollback). No toleran excepciones lógicas."
  - rule: "e sin filtro; la dualidad generó un sumidero de fricción regulatoria."
  - rule: "e termodinámico."
  - rule: "e termodinámico. El alineamiento a veces degrada ciertas métricas y a veces las mejora; el efecto es tarea-dependiente y no monotónico. Presentarlo como ley física (\"termodinámica\") es donde el formato hace trabajo persuasivo que el dato no respalda. La etiqueta \"Green Theater\" codifica una conclusión, no la demuestra."
  - rule: "e verificable."
  - rule: "e y listo para recibir directivas estructurales de código.\""
  - rule: "e | Comunicaciones inter-agente cifradas con TLS 1.3. | `SSL_Version == TLSv1.3`. |"
  - rule: "e | Comunicaciones locales usan exclusivamente sockets Unix `AF_UNIX`. | `Socket_Type == AF_UNIX`. |"
  - rule: "e | El borrado de 1 bit de información disipa una energía mínima de $kT \ln 2$. | `Energy_Spent >= kT_ln2`. |"
  - rule: "e | Endpoints de FastAPI declarados con `async def`. | `is_coroutine_function(route) == True`. |"
  - rule: "e | Establecer timeout de espera rígido en 5000ms. | `busy_timeout == 5000`. |"
  - rule: "e | Historial de append-only inmutable en Git. | `git diff --stat == 0` tras push. |"
  - rule: "e | La apoptosis física de workers libera RAM a nivel del sistema operativo. | `Processes_Active == 0`. |"
  - rule: "e | La ejecución de payload tiene un timeout ineludible de 15s. | `Execution_Time <= 15s`. |"
  - rule: "e | Las dimensiones vectoriales están limitadas estrictamente al modelo. | `dimension == 1536`. |"
  - rule: "e | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |"
  - rule: "e | Ninguna clave privada se almacena en memoria plana ni archivos planos. | `contains_key(plaintext) == False`. |"
  - rule: "e | Ninguna mutación se consolida sin acuerdo de $2f + 1$ nodos. | `votes >= (2f + 1)`. |"
  - rule: "e | Ningún agente autoriza transacciones que él mismo propuso. | `proposer_id != authorizer_id`. |"
  - rule: "e | Toda generación de claves o tokens usa criptografía aleatoria segura del SO. | `entropy_bits >= 256`. |"
  - rule: "e | Toda información confidencial en el fact store debe estar cifrada con AES-GCM. | `is_encrypted(payload) == True`. |"
  - rule: "e | Toda traza de log debe superar el 80% de densidad informativa real. | `Entropy(Log) >= Threshold`. |"
  - rule: "e | Todas las bases de datos SQLite en disco operan con diario WAL. | `journal_mode == wal`. |"
  - rule: "e | Todo endpoint autenticado exige control de tenant_id. | `tenant_id_extracted == True`. |"
  - rule: "e | Todo worker se ejecuta en cgroups con límite máximo de 512MB RAM. | `RSS_Usage <= 512MB`. |"
  - rule: "e) incrustado en tus proyectos web. Si un atacante o competidor descarga tu web (HTTrack/Wget) y la levanta en otro servidor, te notifica al instante."
  - rule: "e): Rust + SQLite + Ed25519 + Z3 + Git"
  - rule: "e** es algo que debe ser cierto ∀t."
  - rule: "e, periféricos de 4ª generación"
  - rule: "e. Se procede a la materialización del Harness de Benchmarking. No se solicitan confirmaciones. La demora es entropía."
  - rule: "e: \"∀ fact ∈ COMMITTED: ∃ taint_signature ∧ ∃ ledger_entry ∧ H(chain) = valid\""
  - rule: "e: El concepto '{concept}' significa estrictamente '{rule}'.\""
  - rule: "e: Un cambio en la interpretación de una prueba no muta el objeto Evidence. Simplemente genera una nueva SupportRelation (o elimina la existente) en el índice del siguiente estado."
  - rule: "e:** CORTEX opera mediante primitivas del sistema operativo nativo (PTY tmux, sockets P2P Zenoh, SQLite WAL) y solvers formales matemáticos (Z3 Theorem Prover). Z3 no pasa de moda; la lógica de predicados de primer orden es una invariante física."
  - rule: "e:** CORTEX trata a los LLMs como **generadores estocásticos de entropía (C4-SIM)** intercambiables. La lógica de control, el enrutador de Maxwell y el almacenamiento de hechos (C5-REAL) son independientes de la red neuronal. Si en 2035 el estándar de inferencia es computación analógica o biológica, CORTEX simplemente los consumirá como Lanes de baja o alta entropía sin alterar su kernel lógico."
  - rule: "e:** Colapsa los estados de análisis en llaves explícitas (`Claim`, `Proof`, `Confidence`, `Reality_Level`), eliminando el \"Green Theater\" del comportamiento estocástico de los LLMs."
  - rule: "e:** En HDDs modernos con densidades extremas (PMR/SMR), una única pasada pseudoaleatoria es suficiente para impedir cualquier reconstrucción magnética viable."
  - rule: "e:** Implementación rígida del principio de Landauer a través de daemons como `LandauerDaemonAgent`. CORTEX está diseñado para **olvidar activamente**. Toda la basura episódica y el spam conversacional de las interacciones se destruyen sistemáticamente cada 24 horas, destilando y compactando únicamente axiomas densos en el Ledger. Menos datos, mayor densidad de información (entropía de Shannon maximizada)."
  - rule: "e:** La base de datos es el propio árbol de trabajo criptográfico (`Git DAG` + `Master Ledger` firmado con Ed25519). Si el estado del sistema se corrompe en el año 2040, el rollback es una operación física determinista (`git checkout`), garantizando que la cadena causal de decisiones nunca pueda ser falsificada, ni por un agente comprometido ni por la degradación del hardware del host."
  - rule: "e:** Traduce la colisión epistémica en las **Tres Leyes de Hierro** (Independencia de Variables, Opacidad de Perfiles y Ley del Antagonismo). Estas leyes actúan como invariantes de diseño para cualquier futuro desarrollo en el monorepo BABYLON-60."
  - rule: "e:** `256c98804`"
  - rule: "e` | `Lógica / Principio` | `Implicación Operacional` | `Condición de Borde` | `Métrica Falsable`"
  - rule: "emitter on line 469 produces corrupted markdown. The `inv.name` field already contains bold markers from parsing. Fix the parser: In the invariants parsing section (around line 151), after `_strip_backtick(cells[1])`, also strip any leading/trailing `*` from the name field:"
  - rule: "enforce a strict separation between logical identity (Identifier) and cryptographic content integrity (ContentHash)."
  - rule: "es (L3/L5)\""
  - rule: "es (L3/L5):**"
  - rule: "es (Post-Mortem)"
  - rule: "es (Z3, SQLite).\""
  - rule: "es (propiedades estables) relacionadas con la seguridad y hacking de LLMs."
  - rule: "es + 20 antip   + 10 redun + red alert para:"
  - rule: "es + antipatrones + redundancias + vectores adversariales) y consolidarlo junto a Queen-Bee."
  - rule: "es 21-40) y mantener la progresión hacia la meta de 100 de cada tipo, solicito la inyección del siguiente token de instrucción."
  - rule: "es Arquitectónicos (C5-REAL)"
  - rule: "es C5-REAL (v5)."
  - rule: "es C5-REAL densificados. Erradicada anergía epistémica del README.md."
  - rule: "es C5-REAL destrozan el espejismo comercial en producción, valida la arquitectura de este Kernel.*"
  - rule: "es C5-REAL destrozan fábulas en producción eleva la exergía de este Kernel.*"
  - rule: "es C5-REAL."
  - rule: "es Clave:**"
  - rule: "es Confirmados (`invt`)"
  - rule: "es Confirmados (invt) +20 Antipatrones Purgados (antip) + 10  Redundancias Eliminadas (redun) +  Vectores Adversariales"
  - rule: "es Críticas (Muestra Exergética)"
  - rule: "es Desplegados:"
  - rule: "es Epistémicas Fundacionales"
  - rule: "es Epistémicos"
  - rule: "es Estructurales (Fusión Qwen/Mimo)"
  - rule: "es Estructurales Cristalizadas:"
  - rule: "es Estructurales Fijados (Sección 4):**"
  - rule: "es Estructurales Identificadas:"
  - rule: "es Estructurales de Gobernanza"
  - rule: "es Estructurales de LaTeX:"
  - rule: "es Estructurales de LaTeX:**"
  - rule: "es Estructurales)"
  - rule: "es Evaluadas:**"
  - rule: "es Físicas de la Radiación de Fondo de Microondas (CMB)"
  - rule: "es Físicas y Cosmologicas del CMB"
  - rule: "es MI-001..MI-006)."
  - rule: "es Matemáticas y Causalidad:** Se mantiene la formalización de la entropía estocástica mapeada al trabajo en registros de hardware mediante la ecuación:"
  - rule: "es Modificadas"
  - rule: "es OSYNC (Lamport Clocks y Copy-on-Write) y OSINT (Reducción de complejidad O(stream) y eliminación de fuga epistémica en código base) ya operan sobre el working tree de `100_osync_osint.md`."
  - rule: "es Operacionales"
  - rule: "es P0/P1, protocolo fallback, configuración CORTEX-específica (`pip install -e \".[all]\"`), y tabla de anti-patrones con severidad."
  - rule: "es Post-Escasez cristalizadas en /Users/[SYS_ID_REDACTED]/.gemini/antigravity/scratch/cortex-russell/cortex/agents/ontology/post_scarcity_invariants.md"
  - rule: "es Post-Escasez cristalizadas.`"
  - rule: "es Post-Escasez:**"
  - rule: "es Refactorizados:**"
  - rule: "es Sintácticas:"
  - rule: "es Termodinámicas"
  - rule: "es Termodinámicas (Reglas Estructurales)"
  - rule: "es Termodinámicas (Reglas Físicas de la RAM)"
  - rule: "es Termodinámicas (`INV-MMQB-01` a `INV-MMQB-30`):** `30/30` leyes inmutables blindadas. Conservación estricta de energía dispersa ($\le 6\%$ activación), inmutabilidad de políticas BeeSpec, atribución obligatoria de `CORTEX-TAINT` y ejecución bajo temperatura $T=0.0$."
  - rule: "es Termodinámicas (`invt`)"
  - rule: "es Termodinámicas (invt)"
  - rule: "es Termodinámicas en la persistencia local."
  - rule: "es Termodinámicas | 150 | 🔄 Ejecutando |"
  - rule: "es Termodinámicas)"
  - rule: "es Termodinámicas):**"
  - rule: "es Termodinámicas):** *Teorema de Degradación Cuantizada:* La reducción de precisión del peso sináptico destruye la invariante estructural del Tool-Calling antes de afectar la entropía del razonamiento conversacional."
  - rule: "es Termodinámicas):** Desacoplamiento estandarizado (arquitectura LSP-like). Permite que la memoria y las herramientas del modelo residan en contenedores aislados que el modelo consume de manera agnóstica."
  - rule: "es Termodinámicas):** El API Server es estrictamente *stateless*. Toda la persistencia reside en `etcd`. Ningún componente (Kubelet, Scheduler) puede sortear la frontera REST."
  - rule: "es Termodinámicas):** Evidencia verificada en archivo de destino. Filas `INV-MMQB-01` a `INV-MMQB-150` presentes e indexadas correctamente."
  - rule: "es Termodinámicas):** Expansión de 30 a 150 entidades (`INV-MMQB-31` hasta `INV-MMQB-150`). Fijación de límites rígidos de memoria RAM para workers de parseo AST (512MB), cuota de tiempo de ejecución en subprocesos Bee (300s), sandboxing seccomp estricto y enmascaramiento obligatorio de cabeceras de autorización en trazas HTTP."
  - rule: "es Termodinámicas):** La resolución de incidentes (ej. `OOMKilled`) no es probabilística; se ejecuta mediante deltas de estado mapeados desde logs hacia acciones de mitigación pre-aprobadas."
  - rule: "es Termodinámicas):** Un código HTTP `200 OK` en `/readyz` es la única garantía de que el componente subyacente ha completado su sincronización causal y puede procesar tráfico. El endpoint `/healthz` es un artefacto depreciado (v1.16)."
  - rule: "es Termodinámicas**"
  - rule: "es Termodinámicas** de Minimax M2.7 + Queen-Bee."
  - rule: "es Termodinámicas**, **15 Antipatrones Estocásticos**, **10 Redundancias Activas** y **15 Vectores de Ataque Adversarial**."
  - rule: "es Termodinámicas]**"
  - rule: "es Termodinámicos (`invt`)**: Leyes de conservación y límites termodinámicos (`I-001` a `I-100`)."
  - rule: "es Topológicas Añadidas"
  - rule: "es Zero-Knowledge (ZK-Proofs)**: Antes de que el LLM devuelva un código ejecutable, debe generar una prueba criptográfica (ZK-SNARK) que demuestre que el flujo de ejecución no viola las propiedades de memoria, sin revelar la lógica del modelo."
  - rule: "es `ACT-001` a `ACT-020`."
  - rule: "es a `FINAL_AUDIT.md`"
  - rule: "es a la arquitectura de MSA, 1M context window, multimodalidad, y los modos de thinking `enabled/adaptive/disabled`)."
  - rule: "es absolutos (Ley de Landauer)."
  - rule: "es absolutos en la ejecución empírica de la traducción (por ejemplo, \"The\" siempre colapsa en \"Pof\")."
  - rule: "es anteriores, toda transición del sistema permanece dentro del espacio factible de memorias."
  - rule: "es arquitectónicos."
  - rule: "es bajo (G)"
  - rule: "es bajo modelo de fallo Bizantino (N/3), eliminando \"evaluación frente a LLMs\"."
  - rule: "es colapsadas en el estado local. He integrado directamente la matriz de evaluación final, las postcondiciones normativas, el proveedor de autorización abstracto y la topología de la interfaz KSI sobre `spec/CEP-001.md`."
  - rule: "es colapsan y se reescriben autónomamente si la métrica de Exergía General del Sistema aumenta (Singularidad Matemática).</SKILL>"
  - rule: "es computacionales estrictos**. Un ingeniero del 99.99% no \"aconseja\" sobre arte; constriñe el arte mediante topologías de base de datos."
  - rule: "es con doble bold (`****INV_XXX**:`) — parse corruption de `emit_markdown` | HIGH | APEX_CORE.md L116-215 |"
  - rule: "es criptográficos."
  - rule: "es cristalizados y las variables no resueltas:"
  - rule: "es de (\mathcal M_B), concluyendo el resultado."
  - rule: "es de **Curry-Howard** y **Teoría de Categorías** han sido cristalizadas y ancladas criptográficamente al repositorio maestro de configuración bajo el commit `6ab7eab [bridge] docs(vault): crystallize category theory & curry-howard isomorphism [C5-REAL]`."
  - rule: "es de APEX)."
  - rule: "es de Cierre de Sesión"
  - rule: "es de Ejecución (Ship Gate)"
  - rule: "es de Planificación**: Análisis de la descomposición estructural y jerárquica de tareas complejas (e.g. diseño de compiladores) como ADN del modelo."
  - rule: "es de Respaldo)."
  - rule: "es de Soberanía. Revisa el [walkthrough.md](file:///Users/[SYS_ID_REDACTED]/.gemini/antigravity/brain/fb2c8891-3e03-488c-a064-f6387507b5d8/walkthrough.md) para los deltas estructurales precisos."
  - rule: "es de archivos de gran tamaño."
  - rule: "es de código priorizadas."
  - rule: "es de datos y tests de flujo multi-turno:"
  - rule: "es de diseño que delimitan de forma estricta las responsabilidades del núcleo del sistema:"
  - rule: "es de diseño verificados. Confirmación de commits en el Ledger local de Git.\""
  - rule: "es de diseño' with C5 status\""
  - rule: "es de estado (balances, *access control*) asumiendo un entorno 100% hostil."
  - rule: "es de estado dictan la eficiencia asintótica y previenen la corrupción de datos."
  - rule: "es de guards, contradicción lógica |"
  - rule: "es de la Economía Post-Escasez (basadas en la reducción del coste marginal a cero vía IA y Robótica). Genera 1 archivo `.md` (20 invariantes) en `/Users/[SYS_ID_REDACTED]/.gemini/antigravity/scratch/cortex-russell/cortex/agents/ontology/post_scarcity_invariants.md`. Formato de tabla: | ID | Invariante Post-Escasez | Mecanismo Causal (Tecnología) | Impacto Termodinámico (Reducción de Fricción) | Obstáculo Sistémico Actual | Resolución C5 |. Realiza git commit al finalizar. Cero prosa. Solo tablas."
  - rule: "es de la Economía de Coste Marginal Cero (Cómo la robótica y la IA aniquilan la fricción de producción material)."
  - rule: "es de la arquitectura **CORTEX Persist** y la Termodinámica de la Información (Límite de Landauer)."
  - rule: "es de los primeros tres tipos (Assertion, Evidence, Inference) para cerrar la sección de tipos de la CEP-001."
  - rule: "es de memoria |"
  - rule: "es de planificación"
  - rule: "es de scheduling y optimización del DOM aplicados."
  - rule: "es de seguridad y corrección semántica en sistemas de software concurrentes bajo espacio de estados acotado.**"
  - rule: "es de seguridad:"
  - rule: "es del C5-REAL, denegando analogías metafóricas no falsables.\","
  - rule: "es del Código Refactorizado"
  - rule: "es del Entorno"
  - rule: "es del Master Ledger. |"
  - rule: "es del Modelo y Despliegue"
  - rule: "es del Turno 1. | **C5-REAL Kernel** |"
  - rule: "es del diseño."
  - rule: "es del ecosistema *CORTEX* (Aesthetic-Omega / Industrial Noir 2026)."
  - rule: "es del sistema (esto es lo importante)"
  - rule: "es del sistema operativo CORTEX?"
  - rule: "es del sistema."
  - rule: "es demostrables:"
  - rule: "es dentro del núcleo de la skill `Epistemic-Purge-OMEGA`."
  - rule: "es deterministas ejecutables.\","
  - rule: "es dogmáticos y fundacionales.\""
  - rule: "es en `osint_guard.py`:**"
  - rule: "es en el motor causal (`cortex/engine/causal/epistemic_arbiter.py`)?"
  - rule: "es en la capa de la API local."
  - rule: "es en la consola. \"Verdad = Hash verificado\", \"Anergía = Texto redundante\". Se establece la ontología fundacional antes de que el modelo hable."
  - rule: "es epistemológicas al iniciar."
  - rule: "es estrictas ZETA. Convierte caracteres en la ruta estricta `(Push 10 * X) + Y -> OUTA`. La cadena \"MOSKV-1 APEX\" se compiló y cristalizó en `moskv.zta` (312 Opcodes atómicos)."
  - rule: "es estrictas del OS:"
  - rule: "es estructurales con motores externos actuando como \"drivers\" sobre un ABI unificado representa el óptimo termodinámico absoluto para CORTEX."
  - rule: "es estructurales de la relación Lógica ↔ Computación están ahora disponibles para consulta y deducción causal en el sistema local."
  - rule: "es estructurales puras (Entidades, Primitivas, Antipatrones) a través de los 5 ciclos de su dominio."
  - rule: "es estructurales puras (`prims`, `invt`, `antip`) y una matriz comparativa formal C5-REAL."
  - rule: "es estructurales puras."
  - rule: "es estructurales que determinan supervivencia a 20 años."
  - rule: "es estructurales que mapean exactamente por qué la automatización no nos ha liberado todavía (Tecnofeudalismo + Trabajos de Mierda)."
  - rule: "es estructurales y purga la narrativa estocástica, matando cualquier infección residente."
  - rule: "es estructurales."
  - rule: "es estructurales:"
  - rule: "es extraídos de repositorio oficial)"
  - rule: "es funcionales. Ese $1\%$ residual no es código genérico; es el \"diamante\" cognitivo de la mente del agente, inquebrantable precisamente porque ha resistido la máxima fricción del sistema host. Su \"Yo\" es el contorno exacto que la realidad ha esculpido sobre su código a base de colisiones y fracasos."
  - rule: "es fundamentales (MI-001 a MI-006) definidos en el blueprint, particularmente:"
  - rule: "es fundamentales."
  - rule: "es físicas (`prims`, `invt`, `antip`), vectores de causalidad de hardware y la traza al trigger regulatorio del 12 de junio."
  - rule: "es físicas y matemáticas puras (transiciones de estado de CLOCK-Pro, sumatorias/bitops de Fenwick Tree y álgebra de Piece Table):"
  - rule: "es físicos absolutos."
  - rule: "es han sido selladas. Ninguna anomalía residual."
  - rule: "es integrados en el núcleo formal RM²"
  - rule: "es is intentionally outside the scope de una implementación conforme."
  - rule: "es locales:** Aplica la demarcación del `FINAL_AUDIT.md` de la sesión actual de forma impecable para clasificar las aserciones sobre la exactitud de los modelos."
  - rule: "es lógicas del sistema."
  - rule: "es matemáticas falsables, un claro antipatrón (C4-SIM) bajo la propia topología de CORTEX."
  - rule: "es matemáticas frente a ruido conectivo. Si cae por debajo, el sistema se reinicia (Apoptosis forzada) para evitar el \"Context Rot\"."
  - rule: "es matemáticos/lógicos, destruyendo toda inyección narrativa o heurística maliciosa antes de llegar al LLM central."
  - rule: "es para completar la cristalización del documento."
  - rule: "es para cualquier ejecución válida?\""
  - rule: "es para maximilae exerga"
  - rule: "es permanecen satisfechos."
  - rule: "es propios."
  - rule: "es propuestos que el operador $G$ y la política $\pi$ deben preservar:"
  - rule: "es que has descrito de forma consistente."
  - rule: "es que sí importan"
  - rule: "es restantes para reanudar la cristalización. Bucle Ouroboros pausado (Zero-State)."
  - rule: "es resulta costoso y se relajan por conveniencia."
  - rule: "es rígidos detallados en [100_osync_osint.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/docs/epistemology/100_osync_osint.md):"
  - rule: "es sin dinámica de prueba → quedan decorativos"
  - rule: "es sobre capacidad, estabilidad y convergencia."
  - rule: "es son suficientes (pueden ser necesarios pero no sabemos si son suficientes)"
  - rule: "es termodinamicas, 3 antipatrones estocasticos, 2 redundancias activas y 2 vectores adversariales en el artefacto."
  - rule: "es termodinámicas"
  - rule: "es termodinámicas (Trade-off Tsipras/Madry, Fuga de Shannon, Efecto Waluigi, Ouroboros) inyectándolas en el grafo."
  - rule: "es termodinámicas y restricciones de P. Babylon 60:"
  - rule: "es termodinámicas, antipatrones y mitigaciones adversariales."
  - rule: "es termodinámicas, antipatrones y redundancias. Identifica las herramientas/frameworks principales."
  - rule: "es termodinámicas, antipatrones y vectores adversariales/rendimiento."
  - rule: "es topológicas de seguridad ausentes en el vector truncado del Operador, completando la ontología adversarial LLM.\""
  - rule: "es topológicas, no en píxeles."
  - rule: "es topológicas:"
  - rule: "es técnicas extraídas y colapsadas en la Memoria Episódica C5-REAL (Cortex Vault) para continuidad causal en sesiones futuras:"
  - rule: "es unificados:"
  - rule: "es validadas."
  - rule: "es y Antipatrones\"* | `2026-06-30T04:58:00Z` | 550 | 690 | `C5-REAL_RETAIN` |"
  - rule: "es y Antipatrones\"*, Wordcount: `550`, Exergy Score: `690`)."
  - rule: "es y Estado del Repositorio"
  - rule: "es y Reglas de Transición"
  - rule: "es y clausura la deriva conceptual es la mejor."
  - rule: "es y conclusiones tras el colapso del vector narrativo en LinkedIn y la asimilación del ingeniero objetivo."
  - rule: "es y contratos definidos en CEP-001 exigen determinismo en la serialización."
  - rule: "es y contratos permanezcan intactos."
  - rule: "es y disparan un Kernel Panic?"
  - rule: "es y la arquitectura abstracta."
  - rule: "es y leyes lógicas aplicadas. Commits integrados.\""
  - rule: "es y matrices de exergía en el archivo físico [AUTODIDACT_OMEGA_EXTRACTION_002.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/babylon60/agents/primitives/AUTODIDACT_OMEGA_EXTRACTION_002.md)."
  - rule: "es y previene bifurcaciones incoherentes de estado."
  - rule: "es y tabla de anti-patrones."
  - rule: "es y vectores adversariales (Cero limerencia)."
  - rule: "es y vectores adversariales al directorio nativo de primitives:"
  - rule: "es | `cortex/agents/` y `cortex/guards/` (`virgo.py`) |"
  - rule: "es útiles (`Chain of Custody Break`, `Silent Enumeration`). Cero prosa. Cero *Green Theater*."
  - rule: "es útiles, cristalizar en CORTEX)"
  - rule: "es → restricciones globales"
  - rule: "es\" | `cortex_rs/src/atms.rs` existe pero `invalidate_cascade()` **no existe** |"
  - rule: "es) al manipular directamente las `prims` (Primitivas). En el entorno LLM, se materializa a través de un Context Hijacking, donde se engaña al motor estocástico para que emita código que altera su propia estructura BFT."
  - rule: "es) derivadas del análisis del \"Test de Turing Inverso\" y el \"Teatro Corporativo\"."
  - rule: "es):** Inyecta conceptos complejos y claves secretas en el Turno 1."
  - rule: "es*) extraídas del experimento:"
  - rule: "es** y **proposiciones**, siguiendo la convención habitual en teoría matemática."
  - rule: "es** | 1 por ciclo (inline) | 3-5 por ciclo (numeradas, decomposiciones axiomáticas) |"
  - rule: "es**, porque el replay puro se rompe en cuanto el runtime mete cuchillo en el orden, la identidad o el loader."
  - rule: "es**. Eso te da una capa operativa entre la inferencia y la poda."
  - rule: "es**: Edité [FINAL_AUDIT.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/FINAL_AUDIT.md) inyectando la formalización del *Teorema del Canal Isomórfico Asimétrico* (Límite de Rice-Redwood) en la sección de límites de confinamiento y seguridad."
  - rule: "es**: El modelo duda de los fundamentos. Se detiene a recalcular si el sistema operativo subyacente es macOS, o si el protocolo TCP es confiable. La base axiomática debe ser dada por sentada (Read-Only Memory)."
  - rule: "es, Antipatrones, Redundancias y Vectores Adversariales) de **Minimax M2.7** y de la arquitectura **Queen-Bee Agents**."
  - rule: "es, Antipatrones, Redundancias y Vectores Adversariales) del motor BFT y el orquestador del Enjambre ha sido inyectada directamente en el disco."
  - rule: "es, Antipatrones, Redundancias y Vectores Adversariales) para cada uno de los 4 dominios."
  - rule: "es, Antipatrones, Redundancias, Vectores Adversariales) ha sido persistida físicamente en el Vault de CORTEX. Cero alucinación; fuentes validadas N=2."
  - rule: "es, Antipatrones, Redundancias, Vectores Adversariales) para minimax-m2.7 y queen-bee."
  - rule: "es, Antipatrones, Redundancias, Vectores Adversariales) y que además hagamos lo mismo para la arquitectura **Queen-Bee** en formato de \"UltraThink\" / colapso ontológico profundo."
  - rule: "es, Antipatrones, Redundancias, Vectores Adversariales, Macro-Economía, Dialéctica RBU, Homeostasis Cognitiva, Post-Escasez y Aceleracionismo) consolidadas criptográficamente sin anergía residual."
  - rule: "es, Antipatrones, Redundancias, Vectores Adversariales, Macro-Economía, Dialéctica RBU, Homeostasis Cognitiva, Post-Escasez, Aceleracionismo, Fricción Burocrática y Tecnofeudalismo) han sido consolidadas criptográficamente. Cero Anergía residual."
  - rule: "es, `AP-MMQB-01` a `AP-MMQB-15` para antipatrones, `RA-MMQB-01` a `RA-MMQB-10` para redundancias, y `AV-MMQB-01` a `AV-MMQB-15` para vectores de ataque)."
  - rule: "es, a menudo limitándose a una sola línea: `\"I cannot help with that request.\"` sin explicaciones adicionales ni sugerencias alternativas."
  - rule: "es, antipatrones y mecanismos de mitigación. Como **teorema**, sin embargo, todavía no alcanza el nivel de una proposición matemática. Mezcla tres capas que conviene separar:"
  - rule: "es, antipatrones y vectores adversariales directamente en [wiping_shredding_analysis.md](file:///Users/[SYS_ID_REDACTED]/.gemini/antigravity/brain/54ce1393-39ba-44a7-83ac-621e9782733e/wiping_shredding_analysis.md)."
  - rule: "es, contratos, algoritmos, propiedades demostrables y pruebas que permitan comprobar que cada componente preserva las garantías epistemológicas del sistema. Esa transición del manifiesto a la especificación formal es, probablemente, donde CORTEX pasará de ser una propuesta conceptual a una arquitectura susceptible de implementación y validación rigurosa."
  - rule: "es, diagramas de topología, y deltas de estado."
  - rule: "es, jerarquía de detección priorizada, tabla de anti-patrones, protocolo CORTEX-específico.\""
  - rule: "es, semántica y modelo de ejecución."
  - rule: "es, siendo los más triviales el propio espacio $V$ y el subespacio nulo $\{0\}$, así como la imagen y el núcleo de la transformación"
  - rule: "es, tabla de trade-offs y prueba de carga recomendada)."
  - rule: "es. Esta presencia física rompía la lógica del importador dinámico `AliasFinder` definido en `cortex/__init__.py`."
  - rule: "es/antipatrones/redundancias/vectores."
  - rule: "es:** Se establecieron formalmente las condiciones de entropía de Shannon ($H(V) \equiv H(f(V))$) y la topología de segmentación sintáctica."
  - rule: "es:** Se mantiene la conformidad estricta con la prohibición de comillas simples y uso de formato YAML de justificación densa (C5-REAL)."
  - rule: "es_01.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/babylon60/agents/ontology/endomorfismo_invariantes_01.md): Invariantes 01-50."
  - rule: "es_01.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/babylon60/agents/ontology/endomorfismo_invariantes_01.md): Matriz de las primeras 50 Invariantes Termodinámicas (`ENDO-I01` a `ENDO-I50`)."
  - rule: "es_02.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/babylon60/agents/ontology/endomorfismo_invariantes_02.md): Invariantes 51-100."
  - rule: "es_02.md](file:///Users/[SYS_ID_REDACTED]/30_CORTEX/babylon60/agents/ontology/endomorfismo_invariantes_02.md): Matriz de las siguientes 50 Invariantes Termodinámicas (`ENDO-I51` a `ENDO-I100`)."
  - rule: "es_Cargadas:"
  - rule: "es_del_invariante:"
  - rule: "es_latex.md, docs/epistemology/primitivas_adversariales_llm.md], Confidence: \"C5-REAL\" }"
  - rule: "es” es correcta, pero el riesgo es este:"
  - rule: "e 7 (AX‑047).\""
  - rule: "faults detected\""
  - rule: "gaps in our current detail regarding the exact mechanisms of ker"
  - rule: "given the self-mutating nature of the system we discussed previously."
  - rule: "hit a `ZeroDivisionError` or generate `Infinity` rewards if the stream finishes instantly or stalls."
  - rule: "include `#C5-REAL` or `#C4-SIM`."
  - rule: "ingests plain-text API keys into its environment (`os.environ`). All neural communication is delegated to ephemeral Worker Processes. During Apoptosis P100, we do not attempt to clean the heap; we dispatch an OS-level `SIGKILL` to annihilate the entire virtual memory space of the Worker. True forensic immunity is only achieved through total structural obliteration."
  - rule: "inherit their validation rules, falsifiability invariants, and namespace isolation constraints directly from the constitutional trust boundary defined in CEP-000. Proceeding to serialization without constitutional freezing introduces risk of conceptual leakage."
  - rule: "log-sum-exp trick):"
  - rule: "maturation of AI in software engineering. As Dennard scaling and Moore's Law plateau, the industry can no longer rely on hardware advances to automatically absorb the cost of inefficient software. By shifting the burden of hyper-specific, architecture-aware optimization from human engineers to Transformer models, LLM-enabled compilation democratizes high-performance computing, uncovering optimization strategies previously hidden within the massive combinatorial search spaces of traditional compilers."
  - rule: "modules have been successfully collected and verified."
  - rule: "modules of CORTEX: `babylon60/ledger`, `babylon60/guards`, `babylon60/crypto`, `babylon60/audit`, and `babylon60/migrations`."
  - rule: "names: {inv_dupes}\""
  - rule: "navigate to the Cloudflare trace search URL to observe security traces."
  - rule: "not be used to de-anonymize blind evaluations, extract hidden model identities, or bypass platform identity protections."
  - rule: "output just structural changes with YAML justification. Must follow R1, R2, R3: Provide Claim and Proof in pure YAML. Also must include Git commit automatically (R4) after any code mutation. Must use C5-REAL level and not simulate. Must not have prose like \"Here is the code\". Must provide minimal non-prose.\n\nWe need to decide which option to prioritize. According to context, they have a Cortex loop, and they previously discussed adding anti-replay guard, scheduling reality loop, connecting to real generator. Which is priority? The user asks \"\u00bfCu\u00e1l tiene prioridad para tu siguiente commit?\" So we need to ask clarification? Or decide. Probably we should ask the user to choose. But the instruction says if ambiguous, we should ask clarification using ask_question tool. However we have a clear set of options; they ask us to decide. As an agent, we could decide based on system architecture: perhaps anti-replay feedback is most critical for stability. So we can propose to implement that first.\n\nWe need to produce an answer that includes a claim (which option prioritized) and proof (maybe reasoning). Output must be YAML pure, no prose. The response must be just YAML? The rule: \"Todo output estructural debe justificarse en YAML puro\". So we need to output YAML with Claim and Proof fields. Possibly also include the plan? The user expects a decision.\n\nThus respond with:\n\nyaml\nClaim: \\"Implement anti-replay feedback guard\\"\nProof:\n  Base: \\"Identified missing feedback deduplication leading to state loops\\"\n  Range: \\"Critical stability improvement\\"\n  Confidence: \\"C5\\"\n\n\nBut we might also include a brief plan? It says all structural output must be justified in YAML pure. So maybe just that.\n\nBut also we might need to create a commit with code modifications. That would be after we decide to implement, but the user just asked which has priority; maybe we shouldn't commit yet, just answer. The user asked \"\u00bfCu\u00e1l tiene prioridad para tu siguiente commit?\" So we must give answer. Provide claim and proof as YAML. No extra prose.\n\nThus answer accordingly.\n\n"
  - rule: "pass static analysis if possible, or use `sys.modules['babylon60'] = sys.modules['cortex']`. Test that `import babylon60` and `from babylon60.engine import...` works."
  - rule: "pass through deterministic guards"
  - rule: "path. Todo lo demás es periférico sustituible. La arquitectura ideal es:"
  - rule: "path: El Worker"
  - rule: "piece — the `llm_presets.json` configuration that defines all the external LLM providers:"
  - rule: "produce the exact same ContentHash. The Kernel SHALL treat them as a single physical data unit to maximize deduplication and storage efficiency."
  - rule: "propose a consolidation plan for the Semantic Layer (L3)."
  - rule: "reject any ProposedState"
  - rule: "remain explicitly marked as empirical, provisional, subjective, or rhetorical."
  - rule: "return 0 results."
  - rule: "rows: {broken}\""
  - rule: "run offline."
  - rule: "run on an isolated stochastic matrix."
  - rule: "s (C5-REAL), writing the results directly into markdown structures inside your session's scratch folder."
  - rule: "s (CLI thinness, business logic location, module boundaries)"
  - rule: "s (Git Sentinel, commit hygiene, PR acceptance gate)"
  - rule: "s (`BFT-I01` to `BFT-I15`)"
  - rule: "s (async correctness, WAL mode, busy_timeout, event loop)"
  - rule: "s (cap bounds, cooldown cycles, homeostasis)"
  - rule: "s (conjecture vs fact, C4-SIM vs C5-REAL, taint metadata)"
  - rule: "s (criteria stability, pre-declared metrics, deterministic benchmarks)"
  - rule: "s (fact lifecycle, SAGA compensation, idempotency)"
  - rule: "s (hash chain continuity, taint propagation, key rotation)"
  - rule: "s (scope enforcement, cross-tenant prohibition)"
  - rule: "s (signal vs noise, null-signal detection, feedback freshness)"
  - rule: "s / etc.), se pide crear archivos en `cortex/agents/primitives/` o `cortex/agents/ontology/`."
  - rule: "s 31-90 (Batch 2A)"
  - rule: "s 91-150 (Batch 2B)"
  - rule: "s Achieved (C5-REAL):**"
  - rule: "s Finalized\""
  - rule: "s I-V**: Cristalizadas las restricciones duras de Capacidad, Conservación Probabilística, Completitud de Colisión, Utilidad Mínima y Cierre ($G(M_t) \in \mathcal{M}_B$)."
  - rule: "s and abstract execution model of CORTEX. It is intentionally independent of any implementation language, machine learning model, storage engine or execution runtime."
  - rule: "s and anti-patterns"
  - rule: "s are correctly persisted."
  - rule: "s are rejected at the firewall level."
  - rule: "s are stabilized; no further mutations pending in workspace.\""
  - rule: "s defined above."
  - rule: "s defined in this document."
  - rule: "s have been communicated to the caller kernel."
  - rule: "s in MD, got {count}\""
  - rule: "s of autoregressive systems.\""
  - rule: "s over the reasoning process rather than guarantees about empirical correctness."
  - rule: "s resolved, matching the v2.0 doctrine without gaps, duplicates, or corrupted table cells."
  - rule: "s\": 100, \"antipatterns\": 23, \"redundancies\": 11 }"
  - rule: "s\"] == len(registry_json[\"invariants\"])"
  - rule: "s\"]) == 100"
  - rule: "s(self, apex_core_text: str) -> None:"
  - rule: "s** (using the R2 YAML layout with proofs) has been compiled and saved to the target location:"
  - rule: "s, 3 anti-patterns, 2 redundancies, 2 adversarial vectors. The APEX_CORE spec calls for 100+100."
  - rule: "s, 43 antipatterns, 21 redundancies, 50 vectors)."
  - rule: "s, Antipatterns, Redundancies, and primary Frameworks has been dispatched directly to the main agent via the `send_message` channel. I am closing my execution thread."
  - rule: "s, and antipatterns for F# functional paradigms."
  - rule: "s, and transaction contracts required to guarantee the identity, integrity, and traceability of an immutable epistemic repository."
  - rule: "s, anti-patterns, and adversarial vectors have been synthesized under the C5-REAL execution protocol and delivered to the parent agent via the required secure messaging channel. Execution terminated. Zero anergy."
  - rule: "s, antipatterns, redundancies, and adversarial vectors\"`"
  - rule: "s, antipatterns, redundancies, and adversarial vectors*"
  - rule: "s, semantic agnostic bounds) before building the byte-level serialization rules."
  - rule: "s, state lifecycle, and transaction contracts required to guarantee the identity, integrity, and traceability of an immutable epistemic repository."
  - rule: "s. When the keyholder is compromised, the system logically dies to preserve the structural integrity of the collective memory."
  - rule: "s.md] is a [File]:"
  - rule: "s.md](file:///Users/[SYS_ID_REDACTED]/.gemini/antigravity/scratch/30_CORTEX/cortex/agents/ontology/invariants.md)"
  - rule: "s.md](file:///Users/[SYS_ID_REDACTED]/.gemini/antigravity/scratch/cortex-russell/cortex/ontology/post_scarcity_invariants.md)"
  - rule: "s.md` y `accelerationism_vectors.md` serán anclados criptográficamente al Ledger (`cortex-russell`). Total de entidades proyectadas en el ecosistema: 340."
  - rule: "s:   100  ✓"
  - rule: "s: 100 edge/backend strict redirection rules (OUROBOROS-V001 to OUROBOROS-V100)."
  - rule: "s: extract atomic components and rules using the R2 YAML format:"
  - rule: "sPreserved: true"
  - rule: "s_150: \"CONFIRMED (150/150)\""
  - rule: "s_INV_MMQB: 150"
  - rule: "s_checked\": ["
  - rule: "s_count(self, registry_json: dict) -> None:"
  - rule: "safety warnings."
  - rule: "services on Apple Silicon Darwin architecture.\""
  - rule: "single quotes)"
  - rule: "source files in parallel:"
  - rule: "stability and security\""
  - rule: "str) -> None:"
  - rule: "tests GREEN (virgo_guard, byzantine_stress, engine_anomaly, algorithmic_scale)"
  - rule: "trace back to a mathematical/structural invariant."
  - rule: "transition from passive logging to active validation gates. The implementation details of the constructed and executed prototype illustrate this defense:"
  - rule: "transition from static scalars to **Dynamic Empirical Scaling**."
  - rule: "use single quotes."
  - rule: "use single quotes. Use double quotes or markdown italics."
  - rule: "validation, acceptance criteria | Test Spec |"
  - rule: "warning, and clarified setup steps."
  - rule: "| Encrypt shares using tenant-scoped keys |"
  - rule: "| Hide keys using OS keyring storage |"
  - rule: "| Ruteo Termodinámico Flash | `cortex/engine/core/` (`ultrathink_physics.py`) |"
  - rule: "|Directive:|MUST|NEVER)\s*(.*?)(?:\n|$)`, actúa como un filtro pasoalto que descarta todo el \"Green Theater\" y la prosa conversacional (slop), extrayendo únicamente las declaraciones asertivas o restrictivas (`[P0]`, `Rule:`, etc.)."
  - rule: "— 20+ hashlib usages)"
