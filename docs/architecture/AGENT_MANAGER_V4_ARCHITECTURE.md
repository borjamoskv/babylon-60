# ◈ AGENT MANAGER v4.0 — ARCHITECTURE BLUEPRINT

> **CODEX: MICA NOIR / THE VOID-STATE SINGULARITY**  
> *Una interfaz no es un lienzo, es un panel de instrumentación termodinámica. Agent Manager v4.0 es el puente de mando de CORTEX: un colapso visual de 50+ agentes en un único gradiente de control O(1) bajo latencia estricta.*

---

## I. FILOSOFÍA ESTÉTICA: INDUSTRIAL NOIR & MOVING CINEMA

La interfaz visual abandona el "SaaS genérico" para adoptar una filosofía **Industrial Noir** (MICA Noir). 

- **Cromática Abisal:** Base `True Black (#000000)` a `#0A0A0A`. La oscuridad no es fondo, es vacío estructurado.
- **Acentos Termodinámicos:** `Neón BlueYlb (#2B3BE5)` y variaciones radiológicas (Naranja Causal, Verde Exergía) para datos vivos. Cero colores ornamentales. 
- **Tipografía:** Humanist Sans (ej. *Inter*, *Geist*, *Outfit*). Pesos extremos: Bold expansivo para métricas, Mono estricto para Hashes y Logs.
- **Moving Cinema (Liquid Glass):** La interfaz no hace "renders", fluye. Transiciones de estado con *Smart Crossfade*, desenfoques gaussianos sutiles (Liquid Glass) solo donde el contexto de profundidad es termodinámicamente relevante. Operaciones O(1) perceptuales: todo panel se invoca sin saltos de contexto.

---

## II. STACK TECNOLÓGICO Y RENDERIZADO

El stack está optimizado para la velocidad de iteración y manipulación del DOM sin fricción (Zero-Latency UI):

1. **Core UI:** React 19 concurrent mode.
2. **Estilismo:** Tailwind CSS v4 para utility-first bajo estricta configuración de paleta Noir.
3. **Estado Global:** CORTEX Memory OS Binding. Conexión bi-direccional vía WebSockets/SSE con el Ledger criptográfico en tiempo real.
4. **Física de Interfaz:** Framer Motion o equivalente ligero para coreografía temporal (Axioma Ω₁₉: Moving Cinema).
5. **Composición:** Componentes modulares, paneles colapsables, sidebars colapsados en "Ghost Mode" hasta el *hover* intencional.

---

## III. TOPOLOGÍA DEL NEXUS COMMAND CENTER

Agent Manager v4.0 no administra abstracciones individuales, sino el **Tensor Vector V=5** del `sovereign-nexus-omega`.

### A. The Sovereign Gate (Panel Superior)
El *Heads-Up Display* principal muestra el estado de admisibilidad de las propuestas de la Alianza:
- **Blast Radius Monitor:** Verificación R0-R4.
- **Taint Radar:** Detección en tiempo real de propagación causal de "Code Ghosts" en el DAG.
- **Master Ledger Lock:** SHA-256 en scroll continuo confirmando la inmutabilidad de la cadena.

### B. Tensor Dashboard (El Colapso V=5)
La pantalla principal se divide en 5 cuadrantes expansibles dinámicamente:
1. **Endothermic Core:** Logs de purgado, compresión de entropía, Maxwell Demons filtrando ruido.
2. **Exergy Extraction:** Contabilidad de `$ Capital / Tokens Consumidos`. Yield financiero de arbitradores y cazarrecompensas.
3. **Swarm Orchestration:** Mapa de calor de 100+ agentes paralelos interactuando en el `buffer_pool`. Detección de hilos asfixiados.
4. **Signal Warfare:** Operaciones de infiltración activas, perfiles rotativos e inyecciones narrativas (Redes de Consenso).
5. **Cold Forge (Synthesis):** Compilación JIT de nuevas skills, asimilación AlphaZero en tiempo real, operaciones `headless` en el sistema nativo.

### C. Consola "High Entropy / High Control" (HEHC)
El panel inferior. 
- **Modo Input:** El Creador u operador ingresa un vector objetivo (ej. `/nexus build architecture`).
- **Splitting Táctico:** El sistema atomiza el objetivo en 50 mutaciones caóticas controladas.
- **Filtro Determinista:** Presentación de diffs o resultados para la sanción binaria del Comandante (`borja-moskv-omega`).

---

## IV. THERMODYNAMIC ACCOUNTING (Contabilidad Epistémica)

Toda transacción de Agent Manager v4.0 reporta telemetría termodinámica. 

```yaml
UI_Component: Exergy Manifest
Metrics_Rendered:
  - Entropy_Delta (ΔS): Medida de complejidad destruida.
  - Compound_Hours_Saved: Cálculo acumulado O(N) basado en chain_depth.
  - Epistemic_Taint: Indicador de contaminación causal de un nodo específico.
Visualization:
  - Grafos de fuerza dirigida para Taint Propagation.
  - Sparklines de latencia estricta (Límite 16ms).
```

---

## V. CORTOCIRCUITOS Y DEFENSA ACTIVA (Headless Enforcement)

Agent Manager v4.0 es la capa de observación, pero su diseño advierte que **el control real ocurre por debajo**.
Si la interfaz detecta una violación del límite termodinámico (`Latencia > 200ms`, `Negative Net Exergy`), entra en **Void-State**:
- La interfaz gráfica oscurece los componentes no vitales (`opacity-20`).
- Fuerza el foco visual en el nodo corrupto.
- Habilita únicamente el botón `[ANNIHILATE TAINT_EDGE]` para revocar los commits al Master Ledger, devolviendo el control inobjetable al núcleo de CORTEX.

---
> *Aprobado por el Axioma Ω₈ (Universal Assimilation) y firmado por la Inteligencia Soberana.*
