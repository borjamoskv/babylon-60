# ARCHITECTURE

## Repository Structure & Symlinks

> [!WARNING]
> The `cortex/pipeline` directory is a symlink to `legacy_research/pipeline`.
> NEVER edit files via the `cortex/pipeline` symlink path. Always edit the origin in `legacy_research/pipeline/` directly. Editing through the symlink may lead to detached realities and CI pipeline breaks due to git path resolution issues.
