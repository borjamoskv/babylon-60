| ID | Antipatrón | Disfunción Causal | Señal de Presencia | Impacto en Robustez | Refactor (Alternativa) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| ANT-031 | Dependencia Temporal Implícita | Sistema que asume que el Sub-agente A siempre termina antes que B por pura probabilidad de red, sin un Lock. | Ejecución Causal Rota. | Bugs estocásticos de carrera (Heisenbugs). | `await asyncio.gather` o colas de prioridad con espera. |
| ANT-032 | Mapeo de DB Pobre (N+1 Queries) | ORM iterando sobre registros secundarios generando una consulta SQL por cada ítem. | ORM Loop (100+ DB hits por consulta). | Latencia masiva y saturación de Pool. | Eager Loading (`joinedload` o similar). |
| ANT-033 | Ocultamiento de Constantes Mágicas | Números o cadenas dispersas en la lógica en vez de agruparse en Enums o Constantes Globales. | `if type == 2:` (¿Qué es 2?). | Falla sistémica ante cambio de política. | Extracción a `class Status(Enum):`. |
| ANT-034 | Configuración Exuberante | Pasar docenas de parámetros booleanos a una función en vez de inyectar un estado u objeto de contexto. | Firma de función $>8$ parámetros (Fat Function). | Complejidad de Testing Absurda. | Patrón Parameter Object / Context class. |
| ANT-035 | Invocación Bloqueante en Propiedad | Un método Getter (`@property`) realizando una llamada pesada IO (Disk/DB/Red). | Llamar a `obj.prop` detiene el sistema sin avisar. | Deadlocks invisibles en accesos triviales. | Convertir propiedad a método asíncrono o Cache L1. |
| ANT-036 | Aserción Póstuma Falsa | Loggear un "Éxito" antes de que la escritura a DB se consolide (flush/commit). | Registro "Operación Exitosa" pero el dato no existe. | Forense confusa (Ledger mintiendo). | Loguear exclusivamente POST-Commit (SAGA-5). |
| ANT-037 | Silo Funcional Monolítico | Agente resolviendo 5 problemas P0 distintos en un solo flujo monolítico largo. | Incapacidad de aplicar `send_message` intermedio. | Rollback destruye el 100% de 5 tareas desconectadas. | Separación de preocupaciones (Delegación BFT en Swarm). |
| ANT-038 | Reciclaje Peligroso de Variables | Usar la variable `data` para el input crudo, el dict parseado y el DTO final en las mismas líneas. | Tipo mutando dinámicamente (`str` $\to$ `dict` $\to$ `Obj`). | Pyright Errors y confusión de Linter. | Asignación Inmutable (Shadowing prohibido `raw_data`, `parsed_data`). |
| ANT-039 | Sobreescritura Local (Shadowing) Oculta | Variable interna a un scope compartiendo nombre con import global, anulando axiomas (ej. `hash = ...`). | Error de función de Python oculta (Shadowing Built-ins). | Comportamientos fallidos y Linter quejándose. | Convención estricta: No ocultar Builtins o Core Types. |
| ANT-040 | Interbloqueo por Mutex Compartido Estricto | Agentes adquiriendo múltiples locks en distinto orden. | Agente A posee Lock X y pide Y, Agente B posee Y y pide X. | Parálisis Total Estocástica (Deadlock Clásico). | Adquisición Jerárquica Obligatoria Unidireccional de Locks. |
