| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| PRM-031 | Silenciamiento de Excepción (Swallowed Exception) | Bloque `except Exception` sin re-lanzamiento ni log estructurado en el Ledger. | Error interceptado y ocultado en el pipeline lógico. | Fallo de estado sin rastro en el Ledger. | Sub-segundo | Alta | Linter `ruff TID251` forzado; Modificación manual prohibida. |
| PRM-032 | Colapso de Router Interno | Saturación o desvío de enrutamiento asimétrico por fallo de evaluación. | Reglas de routing `REASONING_MODE_MAP` con colisión o caída de agente puente. | Cuello de botella en la asignación de tareas del orquestador. | Segundos | Crítica | Reseteo del Router; Fallback a ejecución síncrona de emergencia. |
| PRM-033 | Invalidez Tipológica | Paso de tipos primitivos (ej. `float`) donde se exige precisión absoluta (`Decimal`). | Cálculo estocástico de métricas financieras o de score. | Pérdida de precisión matemática observable. | Inmediato | Alta | Aserción fuerte de tipos (Pydantic validator); Excepción `TypeError`. |
| PRM-034 | Mutación de Contexto Oculto | Agente alterando variables globales o estado de Singleton de forma no determinista. | Acceso Write a espacio global compartido fuera de la DB C5. | Comportamiento intermitente no reproducible (Heisenbug). | Horas | Alta | Aislamiento funcional estricto; Prohibición de Singletons mutables. |
| PRM-035 | Inyección Estocástica de Prompt | Ejecución incontrolada de strings provenientes del usuario directamente en el orquestador. | Falta de sanitización o encapsulamiento en el Input. | Comportamiento anómalo guiado por el payload inyectado. | Inmediato | Crítica | Blindaje de parámetros (Sanitization); Uso exclusivo de plantillas rígidamente estructuradas. |
| PRM-036 | Corrupción de Vector ONNX | Degradación de memoria o fallo matemático en embeddings generados localmente. | Error en el runtime ONNX o hardware (GPU/NPU). | Distancias coseno anómalas (Ej: todas cercanas a 1 o 0). | Segundos | Media | Reinicio del motor ONNX; Recálculo de hash vector. |
| PRM-037 | Retención Fantasma de Archivo | Descriptores de archivos (FD) no liberados que bloquean el disco. | Archivo abierto sin bloque `with` (context manager). | Error `TooManyOpenFiles` o bloqueo de E/S. | Horas | Alta | Linter rule; Cierre forzado de FDs huérfanos por el SO. |
| PRM-038 | Dependencia Externa Bloqueante | Código asumiendo latencia cero o respuesta garantizada de API de terceros. | Servidor externo caído o en timeout sin circuito de ruptura. | Hilo del event loop congelado indefinidamente. | Minutos | Crítica | Circuit Breaker Pattern estricto; Timeouts máximos de red (2000ms). |
| PRM-039 | Pérdida de Linaje de Snapshot | Eliminación accidental o corrupción del punto de restauración C5. | Fallo en la persistencia o sobrescritura de backup. | Rollback fallido al ejecutar SAGA-6. | Minutos a Horas | Alta | Replicación obligatoria de Snapshots a múltiples discos seguros. |
| PRM-040 | Interbloqueo Recursivo (Recursive Mutex Deadlock) | Un hilo intentando adquirir el mismo lock no reentrante múltiples veces. | Pipeline lógico cruzado mal diseñado. | Congelamiento total de una instancia del Agent Bus. | Sub-segundo | Crítica | Uso estricto de Locks controlados; Refactor de lógica de flujo cruzado. |
