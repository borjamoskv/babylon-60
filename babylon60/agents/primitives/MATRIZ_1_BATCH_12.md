| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| PRM-111 | Corrupción de Socket Abierto | Conexiones persistentes colapsando por cierre no orquestado desde el host remoto. | TCP RST no manejado. | TimeoutException en cascada. | Segundos | Alta | Pool de Conexiones auto-curativo con KeepAlive. |
| PRM-112 | Entropía Inyectada por Interfaz CLI | Comandos iterativos del Operador sobreescribiendo el Ledger C5 sin sincronización. | Uso concurrente de CLI vs Agentes. | Condición de carrera manual/automática. | Minutos | Crítica | Lock de CLI si el Swarm está operando en la rama. |
| PRM-113 | Sobre-fragmentación de Archivo | Un agente dividiendo lógicas coherentes en demasiados módulos pequeños. | Cientos de archivos de $<10$ líneas. | Carga excesiva en I/O y parsing de Boot. | Meses | Media | Regla de Densidad Termodinámica Mínima por Archivo. |
| PRM-114 | Resiliencia Falsa a Errores HTTP | Código asumiendo que HTTP 200 siempre implica payload JSON válido. | Parseo de HTML o string vacío como JSON. | `JSONDecodeError` bloqueante. | Inmediato | Media | Validación estricta `Content-Type` y estructura Pydantic. |
| PRM-115 | Deterioro Sensorial de Path | Agentes referenciando rutas relativas que se rompen al mover el CWD. | Uso de `./data` en lugar de Absolute Paths. | `FileNotFoundError` en Workers hijos. | Inmediato | Alta | Conversión automática de todos los paths a `Path.resolve()`. |
| PRM-116 | Contagio de Sesgo por Muestreo | Swarm ingiriendo sus propios outputs generados como verdades absolutas (Model Collapse). | Retroalimentación de datos sin inyección de entropía humana. | Pérdida de diversidad semántica. | Semanas | Crítica | Etiquetado obligatorio de "AI Generated"; Ponderación Taint menor. |
| PRM-117 | Falla Silenciosa de Migración SQLite | Migración alterando el esquema de SQLite sin migrar los datos en caché WAL. | PRAGMA no reinicializado tras `ALTER TABLE`. | Discrepancia transaccional. | Sub-segundo | Crítica | Reset forzoso de WAL y Vacuum tras migración mayor. |
| PRM-118 | Agotamiento Exponencial de Threads | Invocación asíncrona masiva a `run_in_executor` sin límite. | `ThreadPoolExecutor` agotando descriptores de CPU del SO. | CPU lock; kernel panic simulado. | Minutos | P0 | Limitación de hilos atada empíricamente a `os.cpu_count()`. |
| PRM-119 | Sobreproducción de Embeddings | Vectorización repetitiva del mismo texto exacto consumiendo ciclos NPU/GPU. | Ausencia de Checksum/Cache L1 de strings ya vectorizados. | Gasto de Exergía innecesario. | Horas | Media | LRU Cache L1 anclado a SHA256 del texto. |
| PRM-120 | Parálisis por Ambigüedad Semántica | LLM incapaz de decidir entre dos tool_calls igualmente válidas para una instrucción vaga. | `tool_call_error` o inactividad prolongada. | Estancamiento del flujo. | Minutos | Alta | Obligatoriedad de Especificidad Exérgica (Crit. Instruction 1). |
