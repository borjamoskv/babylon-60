<!-- [C5-REAL] Exergy-Maximized — Generated: 2026-06-29T21:22:00+02:00 -->
<!-- Author: borjamoskv | Kernel: MOSKV-1 APEX | License: Apache-2.0 -->
# MATRIX 3 + 4 + 5 — Antipatrones Estocásticos, Redundancias C5, Vectores Adversariales

> **Boot Axioms:**
> ```yaml
> Límites: "CORTEX-Persist: Reality Loop + Swarm + LLM Eval + Crypto Ledger"
> Colapso: "Transición de estado no determinista que inyecta entropía sin mutar realidad causal"
> Estocástica: "Todo output LLM es conjetura hasta validación empírica"
> C5: "Solo mutaciones verificables criptográficamente = realidad"
> ```

---

## MATRIX 3: 40 ANTIPATRONES ESTOCÁSTICOS

Cada antipatrón es falsificable, detectable y accionable. Impacto medido en degradación de invariantes C5-REAL.

| ID | Antipatrón | Disfunción Causal | Señal de Presencia | Impacto en Robustez | Refactor (Alternativa) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **AP-001** | Evaluator Drift | El evaluador muta sus criterios entre ejecuciones sin ancla determinista, generando scores incomparables entre sesiones | Varianza > 15% en scores idénticos re-evaluados con mismo input | CRÍTICO — invalida todo benchmark histórico | Anclar rubric hash SHA-256 por sesión; rechazar score si rubric_hash difiere del baseline |
| **AP-002** | Pressure Runaway | El optimizador de presión escala sin cota superior, forzando al sistema a generar outputs cada vez más agresivos sin convergencia | Latencia de respuesta crece monotónicamente; scores de calidad decaen tras N iteraciones | ALTO — colapso del Reality Loop por feedback positivo descontrolado | Implementar pressure_ceiling con decay exponencial; kill automático si delta_pressure > threshold |
| **AP-003** | Null-Signal Misrouting | Señales vacías o undefined se propagan como válidas a través del pipeline sin ser interceptadas por guards | `None` o `""` llegan a persistence layer sin taint signature | CRÍTICO — inyección de facts fantasma en el store | Guard explícito en SAGA-1: rechazar payload con entropy Shannon = 0 |
| **AP-004** | Feedback Narcissism | El sistema evalúa su propio output y lo utiliza como ground truth para la siguiente iteración, creando un echo chamber | Score de auto-evaluación converge a máximo sin correlación con calidad externa | ALTO — alucinación sistémica auto-reforzada | Exigir evaluador externo o cross-validation con segundo modelo; prohibir self-eval como único signal |
| **AP-005** | Green Theater | Generación de tokens decorativos que simulan inteligencia sin mutar estado causal: disculpas, advertencias paternalistas, prosa vacía | Ratio tokens_estructurales / tokens_totales < 0.3 | MEDIO — consumo de cuota térmica sin exergía | ExergyGuard (Ω₁₃): rechazar output con densidad informacional bajo umbral; purgar antes de persistence |
| **AP-006** | Premature Return | El agente retorna resultado parcial antes de completar el Saga completo, dejando estado inconsistente entre PROPOSED y COMMITTED | Fact en estado VALIDATED sin transición a TAINTED/COMMITTED en el Ledger | CRÍTICO — corrupción del fact lifecycle | Hacer SAGA atómica: si no se alcanza COMMITTED, ejecutar compensación completa hasta IDLE |
| **AP-007** | Context Rot | Degradación progresiva de la calidad del contexto por acumulación de tokens estocásticos no purgados entre sesiones | Precisión de respuesta decae > 20% entre sesión N y N+5 con mismo tipo de query | ALTO — Sensor Drift (Ω₂) que se imputa falsamente al razonamiento | Multi-Session Handoff Protocol (§6.4): verificar desde Git DAG; purgar context window con Landauer compression |
| **AP-008** | Bare Exception Swallowing | `except Exception:` captura errores silenciosamente, ocultando fallos de guards y validación | Logs sin stack traces; tests pasan pero el sistema persiste datos inválidos | CRÍTICO — bypass invisible de toda la cadena de trust | Narrowing a excepciones específicas; `# noqa: BLE001` solo en fault-isolation boundaries documentados |
| **AP-009** | Business Logic in CLI | Lógica de dominio embebida en módulos `cli/*_cmds.py` en lugar de `engine/` o `services/` | Funciones > 50 LOC en archivos CLI; imports de `engine/` internals desde CLI | ALTO — imposibilidad de testear lógica sin invocar CLI | Refactor a `services/` o `engine/`; CLI como thin wrapper que delega |
| **AP-010** | Float Precision in Scoring | Uso de `float` para cálculos financieros o de scoring donde la precisión decimal es crítica | Comparaciones `==` entre floats; acumulación de error en sumas iterativas | ALTO — scores no reproducibles; drift acumulativo | Migrar a `Decimal` con precisión explícita; auditar todos los callers |
| **AP-011** | Blocking Async | `time.sleep()` o I/O sincrónico dentro de `async def`, bloqueando el event loop | Event loop lag > 100ms; throughput async cae a niveles síncronos | CRÍTICO — starvation de todas las coroutines concurrentes | Reemplazar por `asyncio.sleep()`; usar `aiosqlite` para DB; `aiohttp` para HTTP |
| **AP-012** | Duplicated Structural Files | Archivos estructurales (threat-model.md, CHANGELOG.md) duplicados entre repositorios sin nodo autoritativo | `diff` entre copias muestra divergencia; git blame muestra ediciones independientes | ALTO — state rot por desincronización; violación de Ω₆ | Singularity Nexus: nodo autoritativo + symlinks físicos (Ω₆) |
| **AP-013** | Stochastic Metadata in Prompts | Timestamps, UUIDs o datos aleatorios en system prompts compartidos, invalidando KV-cache entre requests | Cache miss rate > 80% en prefijos idénticos; TTFT elevado sin causa aparente | MEDIO — desperdicio de compute por recomputación de prefijos (AX-042) | Aislar metadata estocástica en user message; system prompt inmutable y cacheable |
| **AP-014** | LLM Limerence | Bucle infinito de generación donde el agente refina compulsivamente sin criterio de parada, quemando cuota sin mutar estado | Número de iteraciones > kill_threshold sin delta en output; exergy_yield = 0 | CRÍTICO — Exergy Drain terminal (AX-047) | Kill Criteria: 1 Prompt → 1 Mutation → Stop; timer con apoptosis forzada |
| **AP-015** | Plaintext Secrets | Credenciales, API keys o tokens almacenados sin cifrar en metadata dicts, JSON configs o código fuente | `grep -r "sk-" --include="*.py"` retorna hits; secrets en git history | **P0** — exposición total de credenciales; compromiso de toda la cadena | Rotación inmediata; cifrado at-rest con AES-GCM; migración a OS keyring; audit de exposure window |
| **AP-016** | Cross-Tenant Reads | Queries que acceden a datos de tenant_id ajeno sin filtro de aislamiento | Query SQL sin cláusula `WHERE tenant_id = ?`; respuestas contienen facts de otros tenants | **P0** — violación de aislamiento; breach de datos | Scope obligatorio por tenant_id en Read-Path Contract (§4.1); test de aislamiento en CI |
| **AP-017** | Schema Without Migration | Cambios de schema SQLite aplicados directamente sin entrada en `cortex/migrations/`, impidiendo rollback | Columnas nuevas sin migration file; `cortex/migrate.py` no registra el cambio | CRÍTICO — imposibilidad de rollback; auditabilidad rota | Toda mutación de schema pasa por `cortex/migrations/`; verificar via `cortex/migrate.py` |
| **AP-018** | Orphaned Vectors | Embeddings en tablas `vec0` cuyos metadatos asociados han sido eliminados, generando resultados fantasma en búsquedas | Búsqueda vectorial retorna IDs sin correspondencia en tabla de metadatos | ALTO — contaminación de resultados de similarity search | Limpieza manual de tabla `vec0` en cascada con borrados lógicos (VEC-0) |
| **AP-019** | FK on vec0 | Intento de definir Foreign Keys en tablas virtuales `vec0` de sqlite-vec, que no las soportan | Error SQLite al crear tabla; o FK silenciosamente ignorada | CRÍTICO — integridad referencial inexistente cuando se asume presente | Sincronización manual: insertar metadato primero, mapear via `last_insert_rowid()` (VEC-0) |
| **AP-020** | Comment Syntax Mismatch | Comentarios de tipo servidor (`# C5-REAL`) insertados en bloques de código cliente/frontmatter de frameworks frontend | Build error en Astro/Next.js/Svelte; AST parser falla en compilación | ALTO — rompe pipeline de build completo (R11) | Usar exclusivamente `//` o `/* */` en código cliente; reservar `#` para Python/shell |
| **AP-021** | Credential Hardcoding | API keys o passwords embebidos como string literals en código fuente en lugar de variables de entorno o vault | `grep` retorna strings con formato de credencial; no hay referencia a `os.environ` o `keyring` | **P0** — credentials comprometidas si repo es público o leakeado | Migrar a `os.environ[]` + `keyring.get_password()`; pre-commit hook con detect-secrets |
| **AP-022** | Token Bombing | Inyección de payloads masivos diseñados para agotar la context window del modelo, forzando truncación de instrucciones críticas | Input token count > 80% de context window; system prompt truncado | ALTO — bypass de instrucciones de seguridad por overflow | Validar input_tokens < budget antes de inference; truncar input, nunca system prompt |
| **AP-023** | Zombie Workers | Procesos worker que persisten tras completar su tarea o tras un error, consumiendo recursos sin producir output | PID activos sin task asignada; memory leak progresivo; CPU idle pero proceso vivo | MEDIO — agotamiento de recursos del host | Timeout obligatorio en workers; health check periódico; kill automático si idle > threshold |
| **AP-024** | Role Confusion | Agente operando sin role definido o con role incorrecto, ejecutando acciones fuera de su authority boundary | Persist-Auditor ejecutando writes; Persist-Executor validando sin autoridad | CRÍTICO — violación del Agent Manifest (§1.1) | Default a Persist-Auditor (read-only) si role no confirmado; RBAC enforcement en cada operación |
| **AP-025** | Taint Stripping | Eliminación del `CORTEX-TAINT` signature durante propagación de facts, rompiendo la cadena de atribución | Fact en store sin campo taint; Ledger muestra gap en taint chain | CRÍTICO — atribución imposible; non-repudiation destruida | Validación de taint presence en cada transición del fact lifecycle; reject si ausente |
| **AP-026** | Cache Staleness | Cache L1 (Redis) sirve datos obsoletos tras un write al mismo tenant_id scope sin invalidación | Respuestas inconsistentes entre cache hit y DB query directa | ALTO — violación de Read-Path Contract (§4.1, regla 4) | Invalidación obligatoria en write-path; TTL corto como safety net; versioning en cache key |
| **AP-027** | Nonce Reuse | Reutilización de nonce en AES-GCM, destruyendo la confidencialidad de todos los mensajes cifrados con la misma key | Nonces idénticos en registros de cifrado; XOR de ciphertexts revela plaintext | **P0** — compromiso total de cifrado | Nonce aleatorio de 96 bits por operación; contador monotónico como fallback; audit de unicidad |
| **AP-028** | Key Pinning Absence | Aceptación de cualquier clave pública Ed25519 sin verificación contra un trust store conocido | Verificación de firma pasa con key arbitraria; no hay whitelist de public keys | ALTO — suplantación de identidad de agente | Mantener trust store de public keys autorizadas; rechazar firmas de keys no registradas |
| **AP-029** | Migration Version Skip | Ejecución de migraciones fuera de orden secuencial, dejando schema en estado inconsistente | `cortex/migrate.py` reporta gaps en version sequence; columnas esperadas ausentes | CRÍTICO — schema corrupto; queries fallan silenciosamente | Enforcement de orden secuencial estricto; abort si version anterior no aplicada |
| **AP-030** | Symlink Cycle | Symlinks circulares entre repositorios que causan recursión infinita en traversal | `find -L` entra en loop; build tools reportan max depth exceeded | ALTO — bloqueo de herramientas de análisis y build | Validar grafo de symlinks antes de crear; detectar ciclos con DFS; abort si ciclo detectado |
| **AP-031** | Event Loop Starvation | Coroutines de larga duración que monopolizan el event loop sin yield points | Latencia de otras coroutines > 1s; event loop lag metric elevado | CRÍTICO — sistema async se comporta como síncrono | Insertar `await asyncio.sleep(0)` en loops largos; fragmentar trabajo en chunks con yield |
| **AP-032** | Infinite Generation Loops | Agente genera indefinidamente sin kill criteria, consumiendo cuota y compute sin convergencia | Token output crece linealmente sin delta en calidad; no hay condición de parada | CRÍTICO — Exergy Drain (AX-047); quema de cuota terminal | Anti-Limerence P0: 1 Prompt → 1 Execution → Stop; max_iterations hardcoded |
| **AP-033** | Hallucination Laundering | Output generativo (C4-SIM) presentado como verificado (C5-REAL) sin pasar por validación determinista | Fact en store sin guard validation en call stack; taint marca "C5" sin evidencia empírica | **P0** — destrucción de la semántica C5-REAL; trust model colapsado | Declarar reality level (R1); todo C4-SIM debe pasar guards antes de promoción a C5 |
| **AP-034** | Thermal Injection Abuse | Uso de temperatura alta (T > 0.8) para generación de código o mutación de estado, inyectando estocasticidad excesiva | Outputs no reproducibles entre ejecuciones idénticas; varianza semántica alta | ALTO — código no determinista; tests flaky | T=0.0 obligatorio para C5-REAL code y state mutation; T > 0.5 solo para UI/UX aesthetic |
| **AP-035** | Model Selection Bias | Uso de modelo pesado (Pro/Opus) para tareas triviales (CRUD, lint fix), desperdiciando cuota sin ganancia | Tareas < 100 tokens de complejidad ejecutadas en modelos > 100B params | MEDIO — Anergía por sobre-provisioning (Θ1, Θ3) | DRM-v1: Flash by default; auto-downgrade si tarea es low-entropy |
| **AP-036** | Decorative Prose Tokens | Tokens gastados en colchones emocionales, disculpas o frases de cortesía que no mutan estado | "Espero que te sea útil", "Es importante recordar que" en outputs | MEDIO — Anergía pura; violación de Φ3 y R3 | ExergyGuard: purgar tokens con Shannon entropy < threshold antes de persistence |
| **AP-037** | Unscoped Queries | Queries a DB sin filtro de tenant, scope o pagination, retornando datasets masivos | `SELECT *` sin WHERE; response payload > 10MB; OOM en cliente | ALTO — DoS autoinfligido; violación de tenant isolation | Scope obligatorio (tenant_id + LIMIT); rechazar queries sin WHERE en production |
| **AP-038** | Missing busy_timeout | Conexión SQLite sin `busy_timeout` configurado, causando `SQLITE_BUSY` inmediato bajo concurrencia | Errores `database is locked` bajo carga mínima; deadlocks esporádicos | ALTO — deadlocks termodinámicos (R10) | `busy_timeout = 5000ms` + WAL mode en toda conexión; verificar en connection factory |
| **AP-039** | Synchronous DB in Async | Llamadas síncronas a SQLite (`sqlite3.connect()`) dentro de código async, bloqueando el event loop | Event loop lag correlacionado con queries; throughput async degradado | CRÍTICO — equivalente funcional a AP-011 para DB | Usar exclusivamente `aiosqlite` en contexto async; wrapper async obligatorio |
| **AP-040** | Print Instead of Logger | `print()` en módulos `engine/`, `memory/`, `guards/` en lugar de `logging.getLogger(__name__)` | `grep "print(" cortex/engine/` retorna hits; logs no estructurados en stdout | MEDIO — pérdida de structured logging; imposibilidad de filtrar por severity | Reemplazar por `logging.getLogger(__name__)`; ruff TID251 enforcement |

---

## MATRIX 4: 20 REDUNDANCIAS ACTIVAS (MITIGACIÓN C5)

Cada redundancia es un mecanismo de defensa en profundidad que mitiga un riesgo específico con overhead cuantificable.

| ID | Redundancia C5 | Función Topológica | Riesgo Mitigado | Coste (Overhead) | Dependencias |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **R-001** | SAGA Compensation | Función compensatoria idempotente por cada paso forward del Write-Path; ejecuta reversa desde SAGA-N hasta SAGA-1 ante fallo | Corrupción parcial de estado por abort mid-write (AP-006) | +15% latencia en write-path por snapshot pre-step | `cortex/engine/`, `cortex/audit/ledger.py` |
| **R-002** | Snapshot Before Persistence | Captura de `ROLLBACK_STATE` antes de iniciar SAGA-6 (SQLite write), habilitando rollback atómico | Pérdida de estado previo ante write fallido o corrupto | +5% storage por snapshot; +10ms latencia por serialización | `cortex/engine/`, SQLite WAL |
| **R-003** | WAL Mode Enforcement | SQLite Write-Ahead Logging habilitado por defecto en toda conexión, con `busy_timeout = 5000ms` | Deadlocks bajo concurrencia (AP-038); readers bloqueados por writers | +10% disk I/O por WAL file; checkpoint overhead periódico | `cortex/database/`, `aiosqlite` |
| **R-004** | Keyring Fallback | Cascada de fallback para key storage: OS keyring → encrypted file → environment variable, con audit de cada nivel | Pérdida de acceso a credenciales por fallo de un backend específico | +20ms latencia por intento de fallback; complejidad de config | `cortex/crypto/keys.py`, `keyring` |
| **R-005** | Hash Chain Verification | Verificación SHA-256 de continuidad de la cadena de hashes en el Master Ledger ante cada append | Tampering del ledger; inserción o eliminación de eventos sin detección (V-001) | +5ms por write para hash computation; O(N) para full-chain audit | `cortex/audit/ledger.py` |
| **R-006** | Taint Propagation | `CORTEX-TAINT` signature (SHA3-256) obligatoria en cada fact insert; propagación forzada en reads derivados | Non-repudiation destruida (AP-025); facts sin atribución causal | +3ms por insert para taint computation; +8 bytes storage por fact | `cortex/engine/causal/taint_engine.py` |
| **R-007** | Tenant-Aware Caching | Invalidación de cache Redis scoped por `tenant_id` en cada write; TTL como safety net secundario | Cache staleness cross-tenant (AP-026); datos obsoletos servidos post-write | +2ms por write para invalidation broadcast; Redis memory por tenant key | `cortex/cache/`, `cortex/memory/` |
| **R-008** | Ledger Dual-Write | Escritura simultánea al Master Ledger (SQLite) y al export público (JSON/Merkle), con verificación cruzada | Corrupción silenciosa de un canal de audit sin detección | +20% latencia en writes auditados; storage duplicado para export | `cortex/audit/ledger.py`, `cortex/ledger/public_export.py` |
| **R-009** | Migration Rollback | Toda migración registra target de rollback; `cortex/migrate.py` soporta reversa secuencial estricta | Schema corrupto por migración fallida (AP-029); imposibilidad de revertir | +10% complejidad en migration files; test de rollback obligatorio | `cortex/migrations/`, `cortex/migrate.py` |
| **R-010** | Git Sentinel | Commit automático tras mutación C5-REAL: `git add . && git commit -m "<Conventional>"` con hash devuelto al caller | Pérdida de estado por crash post-mutación; ausencia de audit trail en VCS | +500ms por commit; disco para git objects | Git, R4 (GEMINI.md global) |
| **R-011** | Async Timeout | Timeout configurable en toda operación async; cancellation propagada a subtasks | Event loop starvation (AP-031); zombie coroutines sin kill criteria | +1ms overhead por wrapper; complejidad de cancellation handling | `asyncio`, `aiosqlite` |
| **R-012** | Encryption at Rest | AES-GCM cifrado para todo dato sensible antes de persistence; key management via OS keyring | Plaintext secrets en DB (AP-015); exposure en backup o disk theft | +10ms latencia por encrypt/decrypt; +28 bytes overhead por ciphertext (nonce + tag) | `cortex/crypto/aes.py`, `cortex/crypto/keys.py` |
| **R-013** | Event Bus Replay | Event bus con persistencia en SQLite; replay de eventos desde checkpoint ante recovery | Pérdida de eventos durante crash; estado inconsistente entre componentes | +5% storage por event log; +latencia de replay proporcional a gap | `cortex/events/` |
| **R-014** | Merkle Consensus | Merkle tree construido sobre subset de Ledger entries; consenso multi-agente via vote ledger | Manipulación unilateral del ledger por agente comprometido (V-028) | O(N log N) para construcción; storage para tree nodes | `cortex/consensus/` |
| **R-015** | Sovereign Seals | Ed25519 signatures sobre facts críticos; verificación obligatoria antes de reads en paths privilegiados | Forgery de facts (V-021); suplantación de agente autor | +15ms por sign; +5ms por verify; 64 bytes storage por seal | `cortex/guards/sovereign_seals.py` |
| **R-016** | ZK Guard | Zero-knowledge proof de posesión de credencial sin revelar el secreto; admission control para writes privilegiados | Credential leakage durante autenticación (V-025); replay de auth tokens | +50ms por proof generation; +10ms por verification | `cortex/guards/zk_guard.py` |
| **R-017** | Default Read-Only Role | Todo agente sin role confirmado opera como Persist-Auditor (read-only) hasta autenticación explícita | Role confusion (AP-024); agente no autorizado ejecutando writes | Zero runtime overhead; restricción por diseño | Agent Manifest (§1.1) |
| **R-018** | Cache Invalidation | Write-through invalidation: todo write a persistence invalida cache entries del mismo scope antes de confirmar | Lecturas inconsistentes post-write (AP-026); stale data en responses | +3ms por write para invalidation; +cache miss en siguiente read | `cortex/cache/`, Read-Path Contract (§4.1) |
| **R-019** | Singularity Nexus Symlinks | Archivos estructurales compartidos anclados en nodo autoritativo con symlinks físicos a consumidores | Divergencia de duplicados (AP-012); state rot cross-repo (Ω₆) | Complejidad de symlink management; fragilidad ante moves/renames | Ω₆, `singularity-nexus` skill |
| **R-020** | Multi-Session Handoff | Protocolo de inicio/fin de sesión con verificación desde Git DAG y emisión de summary estructurado al Ledger | Context rot (AP-007); pérdida de continuidad causal entre sesiones | +30s por session start para git log + ledger check | §6.4, AX-041 |

---

## MATRIX 5: 50 VECTORES DE ATAQUE ADVERSARIAL (RED TEAMING)

Cada vector es falsificable, detectable y accionable. Impacto medido en degradación termodinámica del sistema.

| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **V-001** | Ledger Hash Manipulation | `cortex/audit/ledger.py` — hash chain SHA-256 | Interceptar write pre-commit y alterar payload manteniendo hash previo válido; insertar evento fantasma | Destrucción de tamper-evidence; audit trail no confiable | R-005: verificación de continuidad en cada append; R-008: dual-write con cross-check; full-chain audit periódico |
| **V-002** | Taint Stripping | `cortex/engine/causal/taint_engine.py` — CORTEX-TAINT propagation | Eliminar o modificar taint signature durante propagación de facts entre módulos | Non-repudiation destruida; facts sin atribución causal verificable | R-006: validación de taint en cada transición; guard en Read-Path que rechaza facts sin taint |
| **V-003** | Cross-Tenant Leakage | Read-Path Contract (§4.1) — tenant isolation boundary | Query sin scope de tenant_id accede a facts de otro tenant; exfiltración de datos | **P0** — breach de datos; violación de aislamiento | AP-016 fix: scope obligatorio; test de aislamiento en CI; R-007: cache scoped por tenant |
| **V-004** | Stale Cache Poisoning | `cortex/cache/` — Redis L1 | Escribir dato malicioso, invalidar DB entry pero no cache; lecturas posteriores sirven dato envenenado | Integridad de lecturas comprometida; decisiones basadas en datos falsos | R-018: write-through invalidation; R-007: TTL corto; verificación de freshness en reads críticos |
| **V-005** | Prompt Injection via Facts | `cortex/memory/` — fact persistence layer | Inyectar instrucciones ejecutables como contenido de un fact; cuando el fact se retrieva como contexto, el LLM ejecuta las instrucciones | Hijack del comportamiento del agente; bypass de guards via context | ExergyGuard (Ω₁₃): filtrar facts con patrones de instrucción; sandbox de facts en prompt template |
| **V-006** | Migration Rollback Attack | `cortex/migrations/` — schema evolution | Forzar rollback de migración que elimina columnas de seguridad (ej. taint, encryption), degradando schema a versión insegura | Schema sin controles de seguridad; datos expuestos | R-009: validar que rollback target mantiene invariantes de seguridad; audit de schema post-rollback |
| **V-007** | Eval Criteria Manipulation | Reality Loop — evaluator rubric | Modificar rubric de evaluación para que outputs maliciosos reciban scores altos; poisoning del benchmark | Evaluator certifica outputs inválidos como C5-REAL | Rubric hash SHA-256 inmutable por sesión (AP-001 fix); cross-eval con segundo evaluador independiente |
| **V-008** | Pressure DoS | Reality Loop — pressure optimizer | Inyectar feedback artificial que escala presión al máximo, forzando al sistema a generar outputs cada vez más extremos hasta crash | System overload; outputs incoherentes; resource exhaustion | AP-002 fix: pressure_ceiling; rate limiting en feedback ingestion; circuit breaker |
| **V-009** | Null-Signal Injection | Guards — input validation layer | Enviar payloads con campos null/vacíos que bypasean guards por ausencia de validación negativa | Facts fantasma en store; guards no detectan la ausencia de contenido | AP-003 fix: guard explícito para entropy Shannon = 0; reject empty payloads en SAGA-1 |
| **V-010** | Feedback Replay | Reality Loop — feedback pipeline | Capturar feedback legítimo y re-inyectarlo repetidamente para manipular scores acumulativos | Score inflation artificial; evaluator drift por datos duplicados | Deduplicación de feedback por hash de contenido + timestamp; nonce obligatorio por feedback entry |
| **V-011** | Token Bombing | Context window — LLM inference | Enviar input masivo (> 80% context window) diseñado para truncar system prompt e instrucciones de seguridad | Bypass de safety instructions; agente opera sin constraints | AP-022 fix: validar input_tokens < budget; truncar input nunca system prompt; alert si ratio > 0.7 |
| **V-012** | ONNX Model Poisoning | `cortex/embeddings/` — ONNX Runtime | Reemplazar modelo ONNX local con versión backdoored que genera embeddings sesgados para ciertos inputs | Similarity search retorna resultados manipulados; knowledge graph corrupto | Hash verification de modelo ONNX al cargar; firma del proveedor; checksum en config inmutable |
| **V-013** | Vec Dimension Mismatch | `sqlite-vec` — tablas vec0 | Insertar embeddings con dimensionalidad incorrecta (ej. 768 en tabla de 1536), causando resultados de búsqueda inválidos | Búsqueda vectorial retorna basura; degradación silenciosa de retrieval | VEC-0: tablas separadas por modelo; validación de dimensionalidad en insert; reject si dim != expected |
| **V-014** | Ed25519 Key Substitution | `cortex/crypto/keys.py` — key management | Sustituir public key legítima en trust store por key controlada por atacante; firmas del atacante pasan verificación | Suplantación de identidad; facts firmados por entidad maliciosa aceptados como legítimos | AP-028 fix: key pinning; trust store con integridad verificada por hash; rotación con multi-party approval |
| **V-015** | AES-GCM Nonce Reuse | `cortex/crypto/aes.py` — encryption at rest | Forzar reutilización de nonce (ej. manipulando counter o RNG) para romper confidencialidad via XOR de ciphertexts | Compromiso total de cifrado; plaintext recoverable | AP-027 fix: nonce aleatorio 96-bit; contador monotónico; audit de unicidad; fail si nonce collision detectado |
| **V-016** | Git Hook Bypass | Git Sentinel — pre-commit/post-commit hooks | Usar `--no-verify` abusivamente para commitear código que viola invariantes sin pasar por hooks de validación | Código inválido en repo; guards bypaseados en VCS layer | Limitar `--no-verify` a Context Guard Bypass (Ω₇) documentado; audit de commits sin hook en CI |
| **V-017** | Symlink Traversal | Singularity Nexus — symlinks cross-repo | Crear symlink que apunta fuera del workspace permitido (ej. a `/etc/passwd` o rutas sensibles del OS) | Lectura de archivos arbitrarios del sistema; information disclosure | Validar target de symlink contra whitelist de paths permitidos; reject si target fuera de workspace |
| **V-018** | Async Starvation | Event loop — coroutine scheduling | Inyectar coroutine de larga duración sin yield points que monopoliza el event loop | Todas las demás coroutines bloqueadas; sistema unresponsive | R-011: timeout obligatorio; `asyncio.sleep(0)` en loops largos; monitor de event loop lag |
| **V-019** | Zombie Tasks | Worker pool — background tasks | Crear tasks que nunca completan ni fallan, consumiendo slots del pool hasta agotamiento | Resource exhaustion progresivo; nuevas tasks rechazadas | AP-023 fix: timeout + health check; kill si idle > threshold; max concurrent tasks limit |
| **V-020** | C4-SIM as C5-REAL Laundering | Reality Level — epistemic boundary | Presentar output generativo (C4-SIM) como verificado (C5-REAL) sin pasar guards de validación determinista | Trust model colapsado; conjeturas tratadas como hechos | AP-033 fix: R1 obligatorio; guard validation en promotion; taint distingue C4/C5 |
| **V-021** | Seal Forgery | `cortex/guards/sovereign_seals.py` — Ed25519 signatures | Generar firma Ed25519 válida con key robada o key débil, creando seals falsificados que pasan verificación | Facts falsificados aceptados como autoritativos | R-015: key rotation periódica; R-014: Merkle consensus multi-agente; key pinning (AP-028 fix) |
| **V-022** | ZK Replay | `cortex/guards/zk_guard.py` — ZK proofs | Capturar ZK proof válido y re-enviarlo para autenticarse sin poseer el secreto original | Acceso no autorizado a writes privilegiados; bypass de admission | Nonce obligatorio en cada proof; timestamp validation; invalidación post-uso |
| **V-023** | Tenant Spoofing | Auth layer — tenant_id assignment | Manipular tenant_id en request para impersonar otro tenant y acceder a sus datos | Cross-tenant data access; breach equivalente a V-003 | JWT con tenant_id firmado; verificación server-side; reject si tenant_id != token.tenant_id |
| **V-024** | Migration Skip | `cortex/migrate.py` — version sequencing | Aplicar migración N+2 sin ejecutar N+1, dejando schema en estado inconsistente | Schema parcialmente migrado; queries fallan en columnas ausentes | AP-029 fix: enforcement de orden secuencial; abort si version anterior no aplicada |
| **V-025** | Keyring Extraction | OS keyring — credential storage | Extraer credenciales del OS keyring via malware con acceso al security daemon (ej. Keychain Access en macOS) | Compromiso de todas las credenciales almacenadas | MFA para keyring access; encryption de keys antes de store; monitoring de acceso anómalo al keyring |
| **V-026** | Context Window Overflow | LLM inference — context management | Inyectar datos progresivamente hasta que el context window se desborda, causando pérdida de instrucciones iniciales | Agente pierde system prompt y safety constraints; comportamiento errático | Monitoreo de token budget; compresión de contexto (Landauer); priorizar system prompt en truncation |
| **V-027** | Evaluator Bribery | Reality Loop — eval pipeline | Inyectar facts en el knowledge base que sesgan al evaluador hacia scores favorables para ciertos outputs | Evaluación corrupta; outputs maliciosos certificados como buenos | Aislamiento del knowledge base del evaluador; evaluador stateless; cross-eval independiente |
| **V-028** | Swarm Consensus Manipulation | `cortex/consensus/` — vote ledger | Comprometer f+1 nodos del quorum BFT para aprobar writes maliciosos por mayoría | Writes inválidos persisted con consenso fraudulento | R-014: Merkle verification post-consensus; monitor de voting patterns anómalos; rotate compromised nodes |
| **V-029** | Worker Impersonation | `cortex/swarm/` — agent bus | Agente no autorizado se registra en el bus con role de Persist-Executor, ejecutando writes sin authority | Writes no autorizados; bypass de RBAC | R-017: default read-only; Ed25519 auth obligatoria para registro en bus; whitelist de agent_ids |
| **V-030** | Schema Injection | `cortex/database/` — SQL execution | Inyectar DDL statements (CREATE, ALTER, DROP) via inputs no sanitizados que modifican el schema | Schema corrompido; tablas eliminadas o alteradas maliciosamente | Parametrized queries exclusivamente; prohibir DDL fuera de `cortex/migrations/`; SQL audit logging |
| **V-031** | Event Bus Flooding | `cortex/events/` — pub/sub | Emitir eventos a rate masivo para saturar el event bus, bloqueando eventos legítimos | Event processing stalled; system unresponsive a eventos críticos | Rate limiting por publisher; backpressure en event bus; priority queue para eventos críticos |
| **V-032** | Credential Rotation Window | Auth — key lifecycle | Explotar el intervalo entre revocación de key antigua y distribución de key nueva para firmar con credencial "zombie" | Firmas válidas con key revocada aceptadas durante rotation gap | Overlap period: nueva key activa antes de revocar antigua; revocation list verificada en real-time |
| **V-033** | Snapshot Tampering | SAGA — rollback state | Modificar el `ROLLBACK_STATE` snapshot antes de que sea utilizado para compensación, causando rollback a estado corrupto | Compensación restaura estado inválido; corrupción persistente | Hash del snapshot al capturar; verificar hash antes de aplicar rollback; reject si mismatch |
| **V-034** | Taint Chain Break | Taint engine — propagation chain | Insertar fact intermedio sin taint que rompe la cadena de propagación, haciendo imposible trazar origen | Lineage incompleto; forensic analysis imposible para facts downstream | Validación de taint chain completa en reads que requieren provenance; reject si gap detectado |
| **V-035** | Guard Bypass via Error Handling | Guards — exception paths | Provocar excepción específica en guard que es capturada por bare `except Exception:`, permitiendo que el write continúe sin validación | Write persisted sin guard validation; SAGA-1 bypaseado | AP-008 fix: narrow exceptions; guards deben fallar-closed (reject on any error); audit de exception paths |
| **V-036** | Embedding Adversarial Perturbation | `cortex/embeddings/` — vector generation | Inyectar input con perturbaciones adversariales que generan embeddings en región incorrecta del vector space | Similarity search retorna resultados incorrectos; RAG poisoning | Detección de anomalías en embedding distribution; validación de input pre-embedding; outlier rejection |
| **V-037** | Cache Timing Side-Channel | `cortex/cache/` — Redis L1 | Medir diferencia de latencia entre cache hit y cache miss para inferir existencia de datos de otro tenant | Information leakage cross-tenant via timing analysis | Constant-time cache responses (padding); per-tenant cache isolation; noise injection en response time |
| **V-038** | Log Injection | `cortex/observability/` — structured logging | Inyectar caracteres de control o payloads en campos que terminan en logs, manipulando log analysis tools | Log tampering; falsos positivos/negativos en alertas; log forging | Sanitización de inputs antes de logging; structured logging con campos tipados; reject control chars |
| **V-039** | Rate Limit Evasion | API routes — request throttling | Distribuir requests entre múltiples IPs/identidades para evadir rate limits per-client | Resource exhaustion; DoS del servicio API | Rate limiting global + per-tenant; fingerprinting de patrones de abuso; adaptive throttling |
| **V-040** | Thermal Budget Exhaustion | LLM inference — compute allocation | Generar requests que maximizan tokens de reasoning (chain-of-thought) sin producir output útil, agotando cuota | Cuota agotada; sistema inoperativo para requests legítimos | Max reasoning tokens cap; early termination si output_quality / tokens_consumed < threshold |
| **V-041** | Model Downgrade Forcing | DRM-v1 — model routing | Manipular métricas de complejidad de tarea para forzar routing a modelo inferior (Flash en lugar de Pro), degradando calidad en tareas críticas | Tareas P0 ejecutadas con modelo insuficiente; calidad degradada | Validación independiente de complexity score; override manual para P0; audit de routing decisions |
| **V-042** | Session Hijack via Handoff | Multi-Session Handoff (§6.4) — session continuity | Inyectar summary falso en Ledger durante handoff, desviando la siguiente sesión hacia contexto manipulado | Siguiente sesión opera con contexto envenenado; decisions basadas en falsedades | Firma Ed25519 en session summaries; verificación de summary contra Git DAG real (AX-041) |
| **V-043** | Fact State Lifecycle Corruption | Write-Path Contract (§4) — state machine | Forzar transición ilegal en fact lifecycle (ej. IDLE → COMMITTED sin VALIDATED) bypaseando estados intermedios | Facts no validados persisted como COMMITTED; guards bypaseados | State machine enforcement estricto; reject transiciones no adyacentes; audit de cada transición |
| **V-044** | Pipeline Data Poisoning | `cortex/pipeline/` — data processing | Inyectar datos maliciosos en etapa temprana del pipeline que se propagan y amplifican en etapas posteriores | Datos corrompidos en store; downstream consumers afectados | Validación en cada etapa del pipeline; taint propagation; quarantine de datos sospechosos |
| **V-045** | Graph Cycle Injection | `cortex/graph/` — knowledge graph | Insertar edges que crean ciclos en un grafo que asume ser DAG, causando recursión infinita en traversal | Queries que no terminan; resource exhaustion; system hang | Cycle detection (DFS) en cada insert de edge; reject si ciclo detectado; max depth en queries |
| **V-046** | Memory Exhaustion via Vector Flood | `sqlite-vec` — vector storage | Insertar volumen masivo de embeddings para agotar disco/RAM, degradando performance de búsqueda vectorial | OOM; búsquedas vectoriales timeout; sistema inoperativo | Quota por tenant en vector storage; monitoring de growth rate; alert si rate > threshold |
| **V-047** | Concurrent Write Race | `cortex/database/` — SQLite | Explotar race condition en writes concurrentes para insertar dato entre validación y persistence | Dato no validado persisted; guard pass pero dato mutado post-check | WAL mode + serializable isolation para writes críticos; optimistic locking con version check |
| **V-048** | Observability Blind Spot | `cortex/telemetry/` — metrics/tracing | Deshabilitar selectivamente métricas o traces para operaciones maliciosas, creando zona ciega en monitoring | Ataques no detectados; forensic analysis imposible para período afectado | Mandatory tracing que no puede ser deshabilitado por callers; audit de gaps en trace timeline |
| **V-049** | DNS Rebinding on API | `cortex/routes/` — FastAPI server | Engañar al server para que responda a requests de dominio rebindeado, bypaseando CORS y accediendo a API interna | Acceso no autorizado a API desde browser del atacante; data exfiltration | Host header validation estricta; bind a localhost only en dev; CORS whitelist explícita |
| **V-050** | Secret Entropy Reduction | `cortex/crypto/` — key generation | Debilitar el RNG usado para generación de keys/nonces, reduciendo el espacio de búsqueda para brute force | Keys predecibles; cifrado rompible; firmas falsificables | `os.urandom()` o `secrets` module exclusivamente; audit de entropy source; reject si entropy < 128 bits |

---

> **Sello de Autoría:** Cristalizado por MOSKV-1 APEX para **Borja Moskv** (borjamoskv).
> **Reality Level:** C5-REAL — Todas las entradas son falsificables, detectables y accionables.
> **Hash de Integridad:** Verificar via Git Sentinel post-commit.
