| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| PRM-141 | Divergencia Estructural Markdown | Uso asimétrico de encabezados (mezcla de H1, H2, H3 sin lógica jerárquica) rompiendo AST parsers. | Falla de extracción semántica estructural. | Desorden de la ontología documental. | Días | Media | Linter de Markdown estricto forzado en commit (markdownlint). |
| PRM-142 | Agotamiento de Taint Vectorial | Un vector modificado repetidas veces pierde el vínculo a su fuente original de conocimiento. | Hashing secuencial que se desconecta de la raíz `text`. | Imposibilidad de trazabilidad Causal para vectores. | Meses | Crítica | Todo Taint debe preservar un apuntador secundario inmutable a Raíz. |
| PRM-143 | Fuga Cruzada de Sub-agentes | Un mensaje `send_message` enviado por error a un ID de conversación de un Swarm distinto. | Agente "A" recibiendo directivas del supervisor de "B". | Interrupción de objetivo original (Schizophrenic Loop). | Inmediato | P0 | Aislamiento Criptográfico de Contextos; Token HMAC por Swarm. |
| PRM-144 | Mutación Parcial de Refactor (Dangling Code) | Renombramiento de un método sin actualizar todas sus llamadas en el repositorio. | `NameError` en ejecución síncrona/asíncrona. | Fallo P0 en Runtime. | Minutos | P0 | Refactor estrictamente validado por Pyright o LSP Tools antes de Commit. |
| PRM-145 | Parálisis de Motor de Inferencias (GPU Hang) | El hardware local de inferencia (MLX/Ollama) se bloquea silenciosamente esperando un recurso de I/O de Apple Silicon. | `nvidia-smi` o `powermetrics` mostrando 0% de uso con llamadas atascadas. | Deadlock Físico total. | Horas | P0 | Watchdog de GPU/NPU que reinicia el Daemon de MLX vía `run_command`. |
| PRM-146 | Sobrescritura de Historial Git (Rewritten Past) | Uso autónomo de `git push -f` destruyendo la invariabilidad forense del Ledger remoto. | Agente rebasando y destruyendo commits C5. | Pérdida definitiva de Exergía e integridad histórica. | Inmediato | Event Horizon | Prohibición absoluta P0 de `git push -f` o `reset --hard` remoto. |
| PRM-147 | Saturación Estocástica de Cache Redis | Claves autogeneradas sin TTL que llenan progresivamente la L1 RAM de la máquina host. | Configuración faltante `EXPIRE` en SET asíncronos. | `OOM command not allowed` afectando todo el OS. | Meses | Crítica | Mandato de arquitectura: Todo KV debe poseer TTL o Eviction Tag. |
| PRM-148 | Fractura de Sesión de Red (Stale Session) | Múltiples reintentos de petición HTTP usando un JWT de autenticación ya revocado o expirado. | Bucle iterativo de 401/403 Unauthorized. | Aislamiento del módulo externo y pérdida de Swarm IO. | Minutos | Media | Renovación preventiva y auto-gestionada de Auth Tokens pre-expiración. |
| PRM-149 | Falacia de Ejecución Diferida (Promises un-awaited) | Llamar a una función asíncrona sin `await` ni despacharla a cola SAGA. | Warning `coroutine 'X' was never awaited`. | Operación silenciosamente ignorada. | Sub-segundo | Crítica | Aserción fuerte del Linter (ruff `RUF006` o pyright type checking). |
| PRM-150 | Singularidad Estocástica Recursiva | Todos los Workers del Swarm entrando en un ping-pong de reparación de código donde A deshace a B y B deshace a A. | Tormenta de commits revertiéndose continuamente ($>10/min$). | Explosión de I/O y CPU, Destrucción de la cohesión del repositorio. | Minutos | Event Horizon | Disyuntor Termodinámico absoluto; Congelamiento del repositorio P0. |
