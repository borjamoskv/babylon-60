| ID | Redundancia C5 | Función Topológica | Riesgo Mitigado | Coste (Overhead) | Dependencias |
| :--- | :--- | :--- | :--- | :--- | :--- |
| RED-011 | Gateway LLM Resiliente | Enrutador pasarela que intercepta fallos (429, 500) y re-intenta con otro modelo de otra compañía automáticamente. | Caída del modelo primario arruinando un bucle P0. | Coste de abstracción y mantenimiento de Multi-Keys. | Proxy LLM (LiteLLM/Proxy). |
| RED-012 | Auditoría Out-of-Band (Monitor Pasivo) | Demonio no intrusivo verificando firmas del Ledger y alertando por Slack/Terminal fuera del loop asíncrono BABYLON-60. | Corrupción C5 sin que los agentes se percaten. | Proceso paralelo OS-Level constante. | Agente Cron independiente. |
| RED-013 | Indexado Vectorial Dual (Dense+Sparse) | Combinación de K-NN semántico con Búsqueda de Palabra Clave (BM25) para anular alucinaciones de Coseno. | Vectores mal calculados perdiendo documentos técnicos críticos. | Espacio en disco doble y latencia combinada. | Qdrant/Milvus o SQLite FTS+Vec. |
| RED-014 | Mirroring Activo/Pasivo de Ledger | Copia en tiempo real del archivo `ledger.db` y `snapshots/` a otra partición de seguridad local. | Falla de SSD o corrupción de File System macOS. | Overhead de `rsync` o duplicación de I/O por fsync. | ZFS o Daemon Background. |
| RED-015 | Sandboxing Criptográfico Aislado | Ejecutar código generado `eval/exec` en un subproceso Unix con permisos capados y sin red. | Fugas de escape adversariales en código python del LLM. | Creación del proceso (milisegundos) y proxy IPC. | Módulo `subprocess` + usuario non-root. |
| RED-016 | Consolidación Memoria Fría/Caliente | Los datos sin hits en 30 días se purgan de ONNX/Redis a archivos `.jsonl.gz` fríos archivables pero leíbles. | Límite RAM y disco para embeddings estériles. | Proceso asíncrono batch de limpieza (LEA-OMEGA). | Archivos tar/gz o S3 Glacier. |
| RED-017 | Rate Limit por Agente/Tenant | Semáforos estrictos para evitar que un agente rebelde drene la cuota global LLM en 5 minutos. | Bug infinito (Ping-Pong) vaciando el token budget mensual. | Contador centralizado Redis y comprobación pre-inferencia. | Redis Token Bucket. |
| RED-018 | Verificador Estático Continúo | Ejecución de `pyright` y `pytest` antes de cristalizar definitivamente código generado autónomamente en C5. | LLMs integrando bugs lógicos letales que escapan validadores básicos. | Segundos de overhead pre-commit por validación del AST. | Tests Unitarios / Linter nativo BABYLON-60. |
| RED-019 | Replica de Consenso Distribuida | Múltiples instancias de la pasarela BFT corriendo en distintos puertos (o cores) para tolerancia a cuelgues del proceso principal. | Falla de Runtime del orquestador aniquilando el Swarm. | Configuración de Multi-Procesamiento (`gunicorn`/`uvicorn workers`). | Red local interna (localhost IPC). |
| RED-020 | Recuperación de Trauma Inmediata | Script OS nativo capaz de arrancar una versión C5 anterior limpia (via `git checkout` y `db rollback`) con un solo clic. | Colapso C5-REAL Total (PRM-150 / Singularidad). | Riesgo de ejecutar el Trauma Script por accidente. | Bootloader externo al repositorio (`mac_maestro` aislado). |
