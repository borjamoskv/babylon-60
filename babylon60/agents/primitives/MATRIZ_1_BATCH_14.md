| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| PRM-131 | Reversión Ciega de SAGA | Retroceder una transacción asumiendo un fallo que en realidad fue un falso positivo. | Rollback destruyendo deltas válidos. | Pérdida de exergía válida. | Minutos | Media | Verificación fuerte (Double-Check) antes de emitir SAGA Abort. |
| PRM-132 | Degradación Termodinámica de Prompt | Un prompt maestro que ha crecido tanto (>50kb) que diluye la fuerza de sus axiomas (Atención dispersa). | Modelos saltándose INVARIANTES explícitas. | Tasa de fallas en cumplimiento de reglas básicas. | Meses | Crítica | Compresión Exérgica Forzosa; Segmentación Modular de Prompts. |
| PRM-133 | Exhaustion de Memoria por Batching | Leer 10M de filas completas en RAM en lugar de procesar por chunks. | Consulta `.all()` de SQLAlchemy en tabla masiva. | OOM de SO y terminación del proceso (Killed). | Segundos | P0 | Prohibición estricta de `.all()`; Uso de `yield_per()`. |
| PRM-134 | Contagio de Fallo de Encoding | Archivo escrito con CP1252 o similar corrompiendo la cadena UTF-8 del orquestador. | `UnicodeDecodeError` en un pipeline central. | Caída del motor de ingesta de archivos. | Inmediato | Alta | Forzado criptográfico a `utf-8` estricto en todo I/O. |
| PRM-135 | Desincronización de Cache de Permisos | Un Tenant revocado que mantiene acceso durante N minutos por caché de Redis. | Consulta de token validada contra memoria en vez de DB de Seguridad. | Acceso no autorizado persistente. | Minutos | P0 | Caché de Permisos invalida proactivamente por Event Bus. |
| PRM-136 | Corrupción de Plan Semántico | LLM proponiendo un `implementation_plan.md` inyectado con ruido narrativo que impide el parsing. | Falla de asimilación del plan en el Execution Engine. | Parálisis del pipeline de ejecución autónoma. | Minutos | Alta | Validación de sintaxis estructurada (Markdown estricto o JSON). |
| PRM-137 | Pérdida de Foco Temporal | Agente refactorizando lógica antigua irrelevante ignorando el User Request urgente. | Agente optimizando código Legacy no relacionado al prompt actual. | Limerencia Causal; Desperdicio masivo de Exergía. | Horas | Crítica | Aserción P0: Mapear todo Delta al User Request original. |
| PRM-138 | Bifurcación de Entidad Estocástica | Extracción de una misma entidad con dos IDs diferentes (e.g., Usuario vs Client). | Ingestión estocástica duplicando IDs de un mismo concepto. | Polución del grafo Neo4j/SQLite. | Semanas | Alta | Protocolo de Fusión Ouroboros periódico. |
| PRM-139 | Congelamiento por Retry Backoff de DNS | Fallo temporal de resolución DNS escalando a un retry bloqueante a nivel de OS. | `getaddrinfo` deteniendo el Event Loop de Python. | Latencia sistémica global extrema. | Minutos | Crítica | Cache local DNS asíncrona (`aiodns`) forzosa. |
| PRM-140 | Ingesta de Binario Camuflado | Intento de leer un ejecutable o imagen como texto provocando caos en tokens. | LLM o parse ingerido basura hexadecimal. | Saturación inútil del Token Context Window. | Segundos | Media | Magic bytes sniffing preventivo antes de leer (MIME validation). |
