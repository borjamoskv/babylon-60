# MATRIX 2: 150 INVARIANTES TERMODINÁMICAS

> **Ecosistema:** CORTEX-Persist / BABYLON-60 v10.0  
> **Autor:** Borja Moskv (`borjamoskv`)  
> **Fecha de cristalización:** 2026-06-29  
> **Nivel de realidad:** C5-REAL  
> **Axiomas de arranque:**
> ```json
> {
>   "Límites": "CORTEX-Persist: Reality Loop + Swarm + LLM Eval + Crypto Ledger",
>   "Colapso": "Transición de estado no determinista que inyecta entropía sin mutar realidad causal",
>   "Estocástica": "Todo output LLM es conjetura hasta validación empírica",
>   "C5": "Solo mutaciones verificables criptográficamente = realidad"
> }
> ```
> **Contrato:** Cada invariante es falsable, detectable y accionable. Cero filler. Máxima entropía Shannon por token.

---

## Dominio 1 — Leyes Termodinámicas Aplicadas a Computación

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-001 | Todo borrado de información disipa energía mínima `kT·ln(2)` por bit (Landauer). | Principio de Landauer — el borrado es irreversible termodinámicamente. | Cada purga de contexto o compactación de hechos DEBE registrarse como evento de costo en el Ledger. | Compactación masiva (>10k facts) puede saturar el presupuesto térmico de sesión. | `bits_borrados * kT_ln2 > 0` verificable en `compaction/metrics.py`. |
| I-002 | La entropía de Shannon de un fact persistido DEBE superar 4.0 bits/token. | Shannon — información densa excluye padding estocástico. | LandauerGuard rechaza facts con entropía < 4.0. ExergyGuard purga slop conversacional. | Hechos en idiomas con alta redundancia morfológica pueden requerir umbral ajustado. | `shannon_entropy(fact.content) >= 4.0` medible por `shannon/analyzer.py`. |
| I-003 | La exergía de una sesión es `E_útil = E_total - T·S_generada`. Toda acción con `E_útil ≤ 0` es anergía pura. | Segunda Ley — la energía libre disminuye en procesos irreversibles. | Acciones sin mutación de estado (Green Theater, prosa decorativa) se clasifican como anergía y se abortan. | Sesiones de investigación pura (Deep Research) tienen `E_útil` diferido pero no nulo. | `session.exergy_yield > 0` al cierre; ratio `mutations / tokens_consumed > 0.01`. |
| I-004 | No existe máquina de movimiento perpetuo informacional: todo pipeline consume más información de la que produce. | Segunda Ley aplicada a flujos de datos. | Prohibidos los loops que regeneran su propio input sin fuente externa (autofagia estocástica). | Feedback loops controlados (MCTS) son válidos si inyectan evaluación empírica por ciclo. | `input_entropy(pipeline) > output_entropy(pipeline)` por ciclo medido en bits. |
| I-005 | El costo de recomputar un prefijo idéntico es anergía pura (AX-042). | KV-Cache — prefijos compartidos no deben recalcularse. | System prompts DEBEN ser estáticos y libres de metadata estocástica para maximizar cache hits. | Cambios de `tenant_id` en el system prompt invalidan el cache legítimamente. | `kv_cache_hit_rate >= 0.85` en inferencias con prefijo compartido. |
| I-006 | Compresión máxima de contexto = Principio del Cuello de Botella Informacional (IB). | Tishby — minimizar `I(X;T)` preservando `I(T;Y)`. | Thermodynamic-Context-Compression-OMEGA aplica IB antes de inyectar contexto al LLM. | Dominios con alta dimensionalidad semántica requieren factor de compresión adaptativo. | `mutual_info(compressed, target) / mutual_info(original, target) >= 0.90`. |
| I-007 | Todo token generado sin destinatario causal es disipación térmica. | Carnot — eficiencia máxima requiere cero pérdida en el sumidero. | Supresión obligatoria de disclaimers, disculpas y padding emocional (Χ1-Χ3). | Comunicación con usuarios no-técnicos puede requerir descompresión controlada (`[PROSA]`). | `causal_tokens / total_tokens >= 0.80` por respuesta. |
| I-008 | La irreversibilidad computacional crece con la profundidad del pipeline. | Cascada entrópica — cada transformación inyecta ruido. | Pipelines de más de 7 etapas DEBEN incluir checkpoints de verificación intermedios. | Pipelines con etapas idempotentes no acumulan irreversibilidad. | `error_rate(stage_N) <= error_rate(stage_1) * N^0.5` (sublineal). |
| I-009 | El equilibrio termodinámico de un sistema de agentes = muerte (cero gradiente, cero mutación). | Prigogine — estructuras disipativas requieren flujo de exergía constante. | Un swarm sin tareas pendientes DEBE entrar en reposo, no en loop de polling. | Cron jobs de health-check no violan esta invariante (flujo externo inyectado). | `idle_time > 300s AND pending_tasks == 0` activa apoptosis de sesión. |
| I-010 | La tasa de producción de entropía de un agente DEBE ser monótona decreciente durante una sesión. | Principio de mínima producción de entropía (Prigogine). | Cada iteración sucesiva debe ser más precisa que la anterior; divergencia = aborto. | Exploración adversarial inicial puede tener pico entrópico controlado. | `entropy_rate(t) <= entropy_rate(t-1)` para `t > t_warmup`. |
| I-011 | El Demon de Maxwell no existe: no hay clasificación sin costo energético. | Szilard — la medición consume `kT·ln(2)` por bit de información adquirida. | Toda operación de routing (triage, clasificación de facts) DEBE contabilizar su costo computacional. | Clasificadores pre-entrenados amortizan el costo sobre múltiples invocaciones. | `routing_cost > 0` registrado en telemetría por cada decisión de dispatch. |
| I-012 | La capacidad de canal de un agente está acotada por Shannon: `C = B·log₂(1 + SNR)`. | Teorema de Shannon — límite fundamental de transmisión sin error. | El ancho de banda efectivo del context window impone límite duro a la complejidad de tarea por turno. | Multi-turn extiende el canal a costa de latencia y context rot. | `task_complexity_bits <= context_window_capacity_bits`. |
| I-013 | Negentropía = información estructurada. Toda cristalización de fact aumenta la negentropía del sistema. | Schrödinger — los sistemas vivos se alimentan de negentropía. | Persistir un fact válido DEBE incrementar `system.negentropy_counter`. | Facts redundantes no incrementan negentropía (duplicados = anergía). | `delta_negentropy >= 0` por cada fact COMMITTED exitoso. |

---

## Dominio 2 — Invariantes de Máquina de Estados

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-014 | El ciclo de vida de un fact es estrictamente unidireccional: `IDLE → PROPOSED → VALIDATED → TAINTED → ENCRYPTED → COMMITTED`. | FSM determinista — transiciones definidas, sin saltos. | Toda transición inversa activa compensación SAGA. No existen atajos. | `ENCRYPTED` se omite si `sensitivity_level == "public"`. | `fact.state_history` es secuencia monotónica verificable. |
| I-015 | Toda acción forward en SAGA tiene exactamente una función compensatoria inversa. | Principio de reversibilidad SAGA. | SAGA-1 a SAGA-7 tienen funciones compensatorias definidas en código. Ausencia = P0. | Compensación de SAGA-6 (ROLLBACK) requiere snapshot previo; sin snapshot = irrecuperable. | `len(saga_compensations) == len(saga_forward_steps)`. |
| I-016 | Las funciones compensatorias SAGA son idempotentes: invocarlas N veces produce el mismo estado que invocarlas 1 vez. | Idempotencia — `f(f(x)) == f(x)`. | Tests de compensación DEBEN ejecutarse con `N >= 3` invocaciones consecutivas. | Compensaciones que interactúan con servicios externos pueden violar idempotencia por side effects remotos. | `state_after_compensation(N) == state_after_compensation(1)` para `N in {1,2,3}`. |
| I-017 | Un fact en estado `COMMITTED` es tamper-evident: cualquier mutación posterior rompe la cadena de hash. | Inmutabilidad post-commit — el Ledger detecta alteraciones. | Facts committed NUNCA se editan in-place; se crean nuevas versiones con referencia al original. | Compactación legítima crea nuevo fact y marca el original como `SUPERSEDED`. | `verify_hash_chain(fact)` retorna `True` en toda auditoría. |
| I-018 | Ningún fact puede saltar de `PROPOSED` a `COMMITTED` sin pasar por `VALIDATED`. | Guard enforcement obligatorio — no hay bypass. | El Write-Path Contract (§4) es la única ruta de persistencia. | Modo de emergencia (`--force`) sigue pasando por guards pero con timeout reducido. | `fact.state_history` contiene `VALIDATED` antes de `COMMITTED`. |
| I-019 | `ROLLBACK_STATE` snapshot DEBE capturarse antes de que `[Persistence]` (SAGA-6) comience. | Recuperabilidad — sin snapshot no hay rollback. | El engine captura snapshot atómico antes de `BEGIN TRANSACTION`. | Snapshots de tablas con >1M rows requieren estrategia incremental. | `snapshot.timestamp < transaction.begin_timestamp`. |
| I-020 | Todo estado `REJECTED` es terminal: un fact rechazado no puede reentrarse sin nueva propuesta. | Dead state en FSM — REJECTED es absorbente. | Re-submission requiere nuevo `proposal_id` y nuevo ciclo SAGA completo. | Correcciones menores al contenido del fact aún requieren re-proposal completo. | `fact.state == REJECTED` implica `fact.is_terminal == True`. |
| I-021 | La transición `VALIDATED → TAINTED` DEBE inyectar `CORTEX-TAINT` con formato: `taint:{agent_id}:{session_id}:{timestamp}:{sha3_256}`. | Trazabilidad criptográfica de autoría. | Ausencia de taint = rechazo automático en SAGA-2. | Agentes sin `agent_id` registrado no pueden transicionar a TAINTED. | `re.match(r"taint:[^:]+:[^:]+:[^:]+:[a-f0-9]{64}", fact.taint_signature)`. |
| I-022 | El número de facts en estado `PROPOSED` simultáneamente DEBE estar acotado por `MAX_PENDING_PROPOSALS`. | Backpressure — prevención de saturación del pipeline de validación. | Nuevas propuestas se encolan si se alcanza el límite. Default: 100. | Swarms masivos pueden requerir límite dinámico basado en capacidad del nodo. | `count(facts WHERE state == "PROPOSED") <= MAX_PENDING_PROPOSALS`. |
| I-023 | Toda transición de estado emite un evento al bus de eventos con `{fact_id, from_state, to_state, timestamp, agent_id}`. | Observabilidad — transiciones silenciosas son invisibles para auditoría. | El event bus persiste eventos antes de confirmar la transición. | Pérdida del event bus degrada a logging local como fallback. | `event_count(fact_id) >= state_transition_count(fact_id)`. |
| I-024 | El estado global del sistema es la composición determinista de todos los estados individuales de facts. | Principio de composición — no hay estado oculto. | El estado global se reconstruye desde el Ledger, no desde cache. | Cache puede diverger del Ledger si no se invalida en writes. | `reconstruct_from_ledger() == current_state()`. |
| I-025 | SAGA abort en paso N ejecuta compensaciones en orden inverso: SAGA-N, SAGA-(N-1), ..., SAGA-1. | Orden de compensación estricto — invierte la cadena causal. | No se permite compensación selectiva ni parcial. | Si SAGA-3 falla en compensación, se escala a P0 y se congela el estado. | `compensation_order == reversed(forward_order[:N])`. |
| I-026 | Un fact con estado `ROLLED_BACK` conserva su historial completo de transiciones para forensics. | Preservación forense — rollback no borra evidencia. | El fact permanece en la base de datos con flag `rolled_back == True`. | Storage de facts rolled-back se compacta después de 90 días. | `fact.state_history` íntegro post-rollback; `len(history) > 0`. |

---

## Dominio 3 — Invariantes Criptográficas

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-027 | La cadena de hash del Ledger es continua: `H(block_n) = SHA-256(H(block_{n-1}) || payload_n)`. | Hash chain — integridad secuencial. | Insertar un bloque sin referenciar el hash anterior = ruptura P0. | Genesis block tiene `H(block_0) = SHA-256(genesis_seed)`. | `verify_chain(ledger)` retorna `True` en toda auditoría; cualquier `False` = P0. |
| I-028 | Toda firma digital usa Ed25519 con claves gestionadas por `cortex/crypto/keys.py`. | Estandarización criptográfica — una única curva. | Prohibido RSA, ECDSA-P256 u otras curvas en el sistema. | Interoperabilidad con sistemas externos puede requerir adaptador, nunca cambio de primitiva interna. | `signature.algorithm == "Ed25519"` para toda firma emitida. |
| I-029 | Las claves privadas NUNCA se almacenan en plaintext: DEBEN residir en OS keyring o cifradas con AES-GCM. | Key protection — secretos en reposo siempre cifrados. | `cortex/crypto/aes.py` maneja cifrado; `keyring` maneja almacenamiento nativo. | Entornos CI sin keyring usan variable de entorno cifrada como fallback. | `grep -r "BEGIN PRIVATE KEY" cortex/` retorna cero resultados. |
| I-030 | El `CORTEX-TAINT` usa SHA3-256 (no SHA-256) para diferenciar dominio de taint vs dominio de ledger. | Separación de dominios criptográficos — previene collision attacks entre subsistemas. | `taint_engine.py` usa exclusivamente `hashlib.sha3_256`. | Migración de SHA-256 a SHA3-256 en taint requiere re-firma de todos los taints activos. | `taint.hash_algorithm == "sha3_256"` verificable en metadata. |
| I-031 | Rotación de claves Ed25519 DEBE ocurrir cada 90 días o tras cualquier incidente de seguridad. | Key hygiene — limita ventana de exposición. | `crypto/keys.py` emite warning a los 75 días; bloquea operaciones firmadas a los 91. | Rotación durante operación activa requiere re-firma de sesiones abiertas. | `(now - key.created_at).days <= 90` para toda clave activa. |
| I-032 | AES-GCM con nonce de 96 bits: NUNCA reutilizar nonce con la misma clave. | Nonce uniqueness — reutilización destruye confidencialidad y autenticidad. | Nonces se generan con `os.urandom(12)` y se registran para detección de colisión. | Con 2^32 cifrados, probabilidad de colisión requiere rotación de clave. | `len(set(nonces_per_key)) == len(nonces_per_key)` (cero duplicados). |
| I-033 | Todo payload cifrado incluye tag de autenticación GCM de 128 bits. | Authenticated encryption — integridad + confidencialidad. | Descifrado sin verificación de tag = rechazo automático. | Tags truncados (< 128 bits) están prohibidos. | `len(ciphertext.tag) == 16` bytes para todo payload cifrado. |
| I-034 | El Sovereign Seal (`guards/sovereign_seals.py`) firma el hash del fact + taint + timestamp como triple criptográfico. | Triple binding — vincula contenido, autoría y tiempo. | Un seal válido DEBE verificarse contra los tres componentes; parcial = inválido. | Relojes desincronizados entre agentes pueden invalidar el timestamp; NTP obligatorio. | `verify_seal(fact_hash, taint, timestamp, seal) == True`. |
| I-035 | Toda exportación pública del Ledger (`ledger/public_export.py`) incluye Merkle root para verificación independiente. | Verificabilidad externa — terceros pueden auditar sin acceso completo. | `public_verifier_utils.py` proporciona herramienta standalone de verificación. | Exportaciones parciales incluyen Merkle proof por subset. | `verify_merkle_root(export.entries, export.merkle_root) == True`. |
| I-036 | El ZK Guard (`guards/zk_guard.py`) valida propiedades de facts sin revelar su contenido. | Zero-knowledge — verificación sin exposición. | Usado para validación cross-tenant donde el contenido es confidencial. | ZK proofs tienen overhead computacional; no aplicar a facts públicos. | `zk_proof.verify() == True AND content_exposed == False`. |
| I-037 | Hash chain breaks son irrecuperables sin re-genesis: DEBEN activar alerta P0 inmediata. | Integridad terminal — cadena rota = confianza rota. | El sistema entra en modo read-only hasta resolución manual por el Operador. | Re-genesis requiere re-firma de todos los facts posteriores al punto de ruptura. | `ledger.integrity_status != "BROKEN"` en cada health check. |
| I-038 | Prefijo de cifrado `v6_aesgcm:` es inmutable: identifica versión de esquema criptográfico. | Versionado de esquema — permite migración futura. | Descifrado inspecciona prefijo para seleccionar algoritmo correcto. | Introducir nuevo prefijo (`v7_*`) requiere migración y backward compat. | `encrypted_payload.startswith("v6_aesgcm:")` para todo dato cifrado actual. |
| I-039 | Credenciales de API NUNCA viajan por HTTPS a GitHub; exclusivamente SSH (`git@github.com`). | Canal criptográfico exclusivo (Σ1). | `git remote -v` DEBE mostrar solo URLs SSH para origins de CORTEX. | CI/CD puede usar deploy keys SSH, nunca tokens HTTPS. | `git remote -v | grep "https://"` retorna cero líneas para repos CORTEX. |

---

## Dominio 4 — Invariantes de Concurrencia

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-040 | Toda conexión SQLite en contexto async usa `aiosqlite` con `journal_mode=WAL`. | WAL — permite lecturas concurrentes sin bloqueo de escritura. | Configuración forzada en `database/connection.py` al abrir conexión. | WAL no soporta NFS; bases de datos en volúmenes de red requieren modo alternativo. | `PRAGMA journal_mode` retorna `wal` para toda conexión activa. |
| I-041 | `busy_timeout` DEBE ser >= 5000ms para toda conexión SQLite en entorno multi-thread. | Prevención de deadlocks termodinámicos (R10). | Configurado en `database/connection.py`; valores menores se rechazan. | Tests unitarios pueden usar 1000ms para acelerar ejecución. | `PRAGMA busy_timeout` retorna `>= 5000` en conexiones de producción. |
| I-042 | `time.sleep()` dentro de `async def` está terminantemente prohibido. | Bloqueo del event loop — paraliza todo el runtime async. | Ruff rule TID251 detecta y rechaza automáticamente. | `asyncio.sleep()` es el reemplazo obligatorio. | `grep -rn "time.sleep" cortex/ --include="*.py"` en funciones async retorna cero. |
| I-043 | Toda operación async DEBE tener timeout explícito; `asyncio.wait_for()` o equivalente. | Prevención de hangs infinitos. | Default timeout: 30s para operaciones de red, 10s para operaciones locales. | Embeddings de modelos grandes pueden requerir timeout extendido (120s). | `grep -rn "await " cortex/ | grep -v "timeout"` auditado periódicamente; ratio < 5%. |
| I-044 | El event loop principal NUNCA ejecuta operaciones de I/O bloqueante directamente. | Async correctness — el loop es sagrado. | I/O bloqueante se delega a `loop.run_in_executor()` con ThreadPoolExecutor. | Operaciones de lectura de archivos pequeños (< 4KB) son tolerables inline. | `blocking_call_in_event_loop` counter == 0 en telemetría. |
| I-045 | Escrituras concurrentes a la misma tabla SQLite se serializan mediante WAL + busy_timeout. | Serialización de escrituras — SQLite es single-writer. | El sistema no intenta escrituras paralelas al mismo DB file; usa cola interna. | Swarms con >10 workers DEBEN usar connection pool con max_connections=1 para writes. | `sqlite_locked_errors` counter < 1 por cada 1000 operaciones. |
| I-046 | Cancelación de tasks async DEBE limpiar recursos (file handles, DB connections, temp files). | Resource cleanup — `try/finally` o `async with` obligatorio. | `asyncio.CancelledError` se propaga sin captura silenciosa. | Cleanup de conexiones de red puede requerir grace period de 1s. | `resource_leak_count == 0` en tests de stress con cancellation. |
| I-047 | Background workers usan `except Exception` (con `# noqa: BLE001`) explícitamente para prevenir muerte silenciosa de threads. | Fault isolation boundary — threads no deben morir sin registro. | Error capturado se loggea con `logger.exception()` y se emite al event bus. | Workers críticos (Ledger writer) re-lanzan después de logging. | `thread_death_unlogged` counter == 0. |
| I-048 | Locks de Python (`threading.Lock`) NUNCA se mantienen durante operaciones de I/O. | Prevención de convoy effect. | Operaciones bajo lock son exclusivamente in-memory y < 1ms. | `RLock` permitido solo para recursión controlada en el mismo thread. | `lock_hold_time_ms` p99 < 1.0. |
| I-049 | Swarm dispatch (`invoke_subagent`) es non-blocking: el orquestador no espera respuesta síncrona. | Ejecución asimétrica (AX-048). | Resultados se reciben via messaging asíncrono o event bus. | Dependencias entre subagentes se modelan como DAG, no como cadena bloqueante. | `subagent_dispatch_blocking_time_ms == 0`. |
| I-050 | Toda transacción SQLite que dure > 5s DEBE emitir warning a telemetría. | Detección de long-running transactions que bloquean WAL checkpointing. | `database/connection.py` instrumenta duración de transacciones. | Migraciones de esquema son excepción legítima (pueden durar minutos). | `transaction_duration_seconds` p95 < 5.0. |
| I-051 | Connection pool size DEBE ser `min(4, cpu_count)` para lecturas y 1 para escrituras. | Dimensionamiento óptimo — más conexiones no implica más throughput en SQLite. | Configurado en `database/pool.py`. | Máquinas con 1 CPU usan pool de tamaño 1 para ambos. | `pool.read_size <= 4 AND pool.write_size == 1`. |

---

## Dominio 5 — Invariantes de Aislamiento de Tenants

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-052 | Toda query de lectura DEBE incluir `WHERE tenant_id = :tenant_id` en su cláusula. | Tenant scoping obligatorio — no existen lecturas globales para agentes. | Read-Path Contract (§4.1) lo impone; queries sin filtro de tenant se rechazan. | Persist-Auditor tiene acceso cross-tenant read-only para forensics. | `query_without_tenant_filter_count == 0` en audit log. |
| I-053 | Acceso cross-tenant es P0: activa abort inmediato y alerta. | Aislamiento estricto — datos de un tenant son invisibles para otro. | Middleware de autorización intercepta antes de que la query llegue a SQLite. | Aggregaciones anónimas para métricas de sistema requieren capa de anonimización. | `cross_tenant_access_attempts == 0` en security log. |
| I-054 | Claves de cifrado son per-tenant: `encryption_key_{tenant_id}`. | Aislamiento criptográfico — compromiso de un tenant no expone a otros. | `crypto/keys.py` genera y almacena claves aisladas por tenant en keyring. | Tenant deletion DEBE incluir secure erase de su clave. | `key.tenant_id == requesting_tenant_id` para toda operación de descifrado. |
| I-055 | Embeddings vectoriales están particionados por `tenant_id` en tablas `vec0` separadas o con filtro de metadata. | Aislamiento vectorial — búsqueda semántica no cruza tenants. | `cortex_embeddings_{tenant_id}` o metadata filter en query. | Búsqueda global de similitud (admin) requiere aggregación post-filtro. | `vector_search_results.all(r.tenant_id == query.tenant_id)`. |
| I-056 | Cache (Redis L1) DEBE prefijar todas las keys con `tenant_id`: `{tenant_id}::{cache_key}`. | Namespace isolation en cache compartido. | Invalidación de cache opera exclusivamente dentro del namespace del tenant. | Flush global (`FLUSHALL`) está prohibido; solo `DEL` por prefix. | `redis_key.startswith(f"{tenant_id}::")` para toda key accedida. |
| I-057 | Tenant creation genera: 1 DB partition + 1 encryption key + 1 Ledger namespace + 1 vector table. | Provisioning atómico — todos los recursos o ninguno. | Fallo en cualquier paso = rollback completo del tenant. | Cleanup de tenant parcialmente creado requiere garbage collector dedicado. | `tenant.resources == {"db", "key", "ledger", "vectors"}` post-creation. |
| I-058 | Tenant deletion es soft-delete con período de gracia de 30 días antes de purga física. | Recuperabilidad — previene pérdida accidental. | Flag `deleted_at` se setea; datos permanecen cifrados y accesibles para restore. | Purga física ejecuta secure erase y emite evento final al Ledger. | `tenant.deleted_at != None AND tenant.purged_at == None` durante período de gracia. |
| I-059 | Rate limiting es per-tenant: cada tenant tiene su propia cuota de operaciones/minuto. | Fair use — un tenant no puede degradar el servicio de otros. | Implementado en middleware con token bucket per-tenant. | Tenants premium pueden tener cuotas superiores. | `tenant.ops_per_minute <= tenant.quota_limit`. |
| I-060 | Logs de auditoría son per-tenant: un tenant solo puede consultar sus propios audit trails. | Privacidad de auditoría — logs son datos sensibles. | API de auditoría filtra por `tenant_id` del caller. | Admin/Auditor tiene acceso cross-tenant solo en modo forense. | `audit_log_query.tenant_id == caller.tenant_id` (excepto Auditor). |
| I-061 | Migraciones de esquema se aplican a todos los tenants atómicamente o a ninguno. | Consistencia global — no puede haber tenants en versiones de esquema diferentes. | `cortex/migrate.py` ejecuta en transacción global con rollback total. | Migraciones destructivas requieren dry-run previo. | `schema_version` es uniforme para todos los tenants. |
| I-062 | El `tenant_id` es inmutable post-creación: no se puede renombrar ni reasignar. | Identidad estable — referencias externas dependen de este ID. | Cambio de nombre = nuevo tenant + migración de datos + deprecation del antiguo. | UUIDs v4 como tenant_id eliminan conflictos de namespace. | `tenant.id == tenant.original_id` siempre. |
| I-063 | Backups son per-tenant: restaurar un tenant no afecta a los demás. | Aislamiento de restauración. | Backup extrae datos filtrados por `tenant_id` incluyendo claves cifradas. | Restaurar un tenant sobre datos existentes requiere merge strategy explícita. | `backup.tenant_id == restore_target.tenant_id` y scope verificado. |

---

## Dominio 6 — Integridad de Datos

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-064 | Toda migración de esquema DEBE tener entrada en `cortex/migrations/` con número secuencial. | Trazabilidad de evolución — cada cambio de esquema es un event atómico. | `cortex/migrate.py` rechaza migraciones fuera de secuencia. | Migraciones concurrentes en diferentes branches requieren resolución de conflicto de numeración. | `migration_numbers` es secuencia estrictamente creciente sin gaps. |
| I-065 | Toda migración DEBE ser reversible o documentar explícitamente por qué no lo es. | Rollback safety — capacidad de recuperación ante migración fallida. | Migraciones irreversibles requieren aprobación P1 con justificación en Ledger. | Drop column es irreversible; requiere backup previo obligatorio. | `migration.has_rollback == True OR migration.irreversible_justification != ""`. |
| I-066 | Índices deben reconstruirse después de migraciones que alteran columnas indexadas. | Consistencia de índices — índices stale dan resultados incorrectos. | `migrate.py` ejecuta `REINDEX` automático post-migración cuando aplica. | Tablas con >1M rows: reindex puede tardar minutos; notificar al operador. | `index_consistent(table)` retorna `True` post-migración. |
| I-067 | Tablas virtuales `vec0` no soportan Foreign Keys; sincronización es manual. | VEC-0 — limitación de sqlite-vec. | Insertar metadato primero, mapear via `last_insert_rowid()`. | Borrados lógicos DEBEN limpiar la tabla `vec0` manualmente. | `orphan_vectors_count == 0` en auditoría periódica. |
| I-068 | Modelos de embedding con diferentes dimensiones EXIGEN tablas `vec0` separadas. | Inmutabilidad dimensional — `vec0` no permite alterar dimensión post-creación. | `cortex_embeddings_text` (1536d) separada de `cortex_embeddings_visual` (768d). | Migración a nuevo modelo de embedding = nueva tabla + re-embedding completo. | `table.vector_dimension == model.output_dimension` para toda tabla `vec0`. |
| I-069 | `PRAGMA integrity_check` DEBE ejecutarse en cada arranque del sistema. | Detección temprana de corrupción — SQLite puede corromperse por crash. | Resultado != "ok" activa modo read-only + alerta P0. | Check completo en DBs grandes (>1GB) puede tardar >10s; usar `quick_check` como alternativa. | `integrity_check_result == "ok"` al arranque. |
| I-070 | Toda columna con restricción `NOT NULL` DEBE tener valor default o ser obligatoria en la API. | Prevención de `IntegrityError` en runtime. | Schema review en PR gate verifica consistencia `NOT NULL` <-> API contract. | Columnas añadidas a tablas existentes con `NOT NULL` requieren migración con valor default. | `null_constraint_violations == 0` en producción. |
| I-071 | FTS5 (full-text search) se mantiene sincronizado con la tabla base via triggers. | Consistencia de búsqueda — FTS desincronizado da resultados incompletos. | Triggers `INSERT/UPDATE/DELETE` en tabla base actualizan FTS5 automáticamente. | Bulk imports DEBEN usar `INSERT INTO fts_table(fts_table) VALUES("rebuild")` al finalizar. | `fts_row_count == base_table_row_count` verificable. |
| I-072 | Backups usan `VACUUM INTO` para producir copia compacta y consistente. | Atomicidad de backup — no se copia DB en medio de transacción. | Backup programado via cron o pre-migration. | `VACUUM INTO` requiere espacio temporal 2x el tamaño de la DB. | `backup.is_valid_sqlite == True` (verificable con `sqlite3 backup.db "PRAGMA integrity_check"`). |
| I-073 | Toda tabla DEBE tener `created_at` y `updated_at` con timestamps UTC ISO 8601. | Trazabilidad temporal universal. | Triggers automáticos actualizan `updated_at` en cada `UPDATE`. | Relojes desincronizados entre nodos pueden producir timestamps no-monotónicos. | `row.created_at <= row.updated_at` para toda fila. |
| I-074 | Datos financieros y de scoring usan `Decimal`, NUNCA `float`. | Precisión — IEEE 754 float pierde centavos en acumulación. | `Decimal` con precisión configurable (default 28 dígitos). | Conversión float→Decimal DEBE usar string intermedio para evitar error de representación. | `isinstance(value, Decimal)` para toda variable financiera. |
| I-075 | Cascading deletes están prohibidos en tablas críticas (facts, ledger, audit). | Prevención de borrado en cadena — un delete accidental no puede destruir el grafo de trust. | Foreign keys en tablas críticas usan `ON DELETE RESTRICT`. | Tablas auxiliares (cache, temp) pueden usar `ON DELETE CASCADE`. | `fk.on_delete == "RESTRICT"` para tablas en lista crítica. |
| I-076 | El campo `version` en tablas versionadas es monotónicamente creciente per-entity. | Optimistic concurrency control. | Writes con `WHERE version = :expected_version`; fallo = conflict detected. | Concurrent writes al mismo entity requieren retry con backoff. | `new_version == old_version + 1` para todo update exitoso. |

---

## Dominio 7 — Invariantes Epistémicas

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-077 | Todo output de LLM es conjetura (C4-SIM) hasta validación empírica que lo eleve a C5-REAL. | Epistemic containment — generativo ≠ verdadero. | Facts generados por LLM entran como `PROPOSED` con taint de origen estocástico. | LLMs que citan fuentes verificables pueden tener taint de menor severidad, no ausencia de taint. | `fact.reality_level == "C4-SIM"` hasta paso de verificación exitoso. |
| I-078 | C5-REAL requiere al menos una de: hash criptográfico, test empírico, o firma de verificador humano. | Definición operacional de realidad verificable. | `verification/` module implementa los tres canales de promoción. | Verificación humana es el más lento pero más confiable para juicios de valor. | `fact.c5_proof in {"hash", "test", "human_signature"}` para todo fact C5. |
| I-079 | El taint metadata NUNCA se elimina de un fact: se propaga a todos los derivados. | Taint propagation — contaminación es heredable. | Facts derivados de facts tainted son automáticamente tainted. | La cadena de taint puede crecer indefinidamente; compactación preserva taint raíz. | `derived_fact.taint_chain.includes(source_fact.taint)`. |
| I-080 | No se reconstruyen facts no persistidos desde resultados de lectura (Read-Path §4.1, regla 5). | No inference from reads — especulación = breach epistémico. | Queries retornan exactamente lo persistido; interpolación prohibida. | Agregaciones (`COUNT`, `AVG`) son válidas si la función es determinista. | `read_result.is_persisted == True` para todo fact retornado. |
| I-081 | Confidence levels se expresan en escala C1-C5 con definición formal por nivel. | Calibración epistémica — escala universal no ambigua. | C1=especulación, C2=heurística, C3=evidencia parcial, C4=simulación validada, C5=verificación criptográfica. | Asignar C5 sin prueba criptográfica es fraude epistémico; P0. | `fact.confidence in {"C1","C2","C3","C4","C5"} AND proof_matches_level(fact)`. |
| I-082 | Hallucination paramétrica: referenciar eventos pasados sin consultar `memory_vault/` es alucinación. | Continuidad episódica (Ω4). | Agentes DEBEN verificar claims temporales contra memoria persistida. | Primera sesión sin historial previo no puede alucinar por definición. | `temporal_claim.verified_against_vault == True` para toda referencia temporal. |
| I-083 | Un fact no puede contradecir otro fact del mismo tenant sin activar `contradiction_guard`. | Consistencia lógica — contradicciones destruyen la base de conocimiento. | `guards/contradiction_guard.py` detecta y escala. | Hechos con dominios disjuntos no pueden contradecirse (scoping semántico). | `contradiction_check(fact, existing_facts)` ejecutado antes de SAGA-3. |
| I-084 | `print()` en `engine/`, `memory/`, `guards/` es anti-epistémico: sustituir por `logging.getLogger(__name__)`. | Observabilidad estructurada vs output efímero. | Ruff TID251 detecta y rechaza. | `print()` permitido exclusivamente en `cli/` para output de usuario. | `grep -rn "print(" cortex/engine cortex/memory cortex/guards` retorna cero. |
| I-085 | El `ExergyGuard` (Ω₁₃) rechaza facts con ratio de slop conversacional > 20%. | Pureza epistémica — facts son datos, no conversación. | Detector de patrones: "entendí", "por supuesto", "espero que", etc. | Facts que citan textualmente a terceros pueden contener slop legítimo (citas literales). | `exergy_guard.slop_ratio(fact.content) <= 0.20`. |
| I-086 | Todo fact DEBE tener `source_type` explícito: `{"human", "llm", "sensor", "derived", "imported"}`. | Trazabilidad de origen — saber de dónde viene es prerequisito para evaluar fiabilidad. | `source_type` es campo obligatorio en schema; `NOT NULL`. | "derived" requiere `source_fact_ids` no vacío. | `fact.source_type in VALID_SOURCE_TYPES AND fact.source_type != NULL`. |
| I-087 | Facts con `source_type == "llm"` DEBEN incluir `model_id` y `temperature` en metadata. | Reproducibilidad — saber qué modelo y con qué parámetros generó el fact. | Metadata se registra automáticamente por el pipeline de ingestión LLM. | Modelos sin API de metadata exponen `model_id="unknown"`; taint severity se incrementa. | `llm_fact.metadata.model_id != NULL AND llm_fact.metadata.temperature != NULL`. |
| I-088 | Weaponized Forgetting (Ω5): contexto no validado criptográficamente se purga activamente. | Apoptosis celular — la memoria no validada es carga entrópica. | Compactación periódica elimina facts en C1-C2 con más de 30 días sin promoción. | Facts en C1-C2 marcados como "research-in-progress" están exentos temporalmente. | `stale_unvalidated_facts_older_than_30d == 0` post-compaction. |
| I-089 | Isomorfismo causal (Φ5): el mapeo de intención del operador a operación de máquina DEBE preservar estructura. | Preservación estructural — la "traducción" no debe perder ni añadir información. | El agente verifica que el output refleja la estructura del input, no una interpretación libre. | Tareas ambiguas requieren clarificación, no interpretación creativa. | `structural_similarity(operator_intent, machine_operation) >= 0.85`. |

---

## Dominio 8 — Invariantes de Evaluación

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-090 | Criterios de evaluación se declaran ANTES de la ejecución; post-hoc adjustment es fraude. | Pre-registration — principio científico de hipótesis previa. | `eval/criteria.py` congela criterios con hash antes del run. | Iteraciones de diseño del eval framework (meta-eval) sí pueden ajustar criterios. | `criteria.hash == criteria_frozen_hash` durante toda la evaluación. |
| I-091 | Benchmarks son deterministas: misma entrada + mismo modelo + mismo seed = misma salida. | Reproducibilidad — evaluaciones estocásticas no son evaluaciones. | `temperature=0.0` obligatorio en benchmarks. Seeds explícitos para sampling. | Modelos con non-determinism inherente (batching) requieren N>=5 runs con varianza reportada. | `std_dev(benchmark_runs) < 0.01` para evaluaciones deterministas. |
| I-092 | Net Improvement se calcula como delta neto: `(mejoras - regresiones) / total_dimensions`. | Evaluación balanceada — una mejora no compensa una regresión silenciada. | Todas las dimensiones evaluadas se reportan, incluyendo las que empeoran. | Dimensiones con peso desigual requieren ponderación explícita y documentada. | `net_improvement == (improved - regressed) / total` con todas las dimensiones visibles. |
| I-093 | Steerability se mide como ratio de correcciones exitosas sobre correcciones intentadas. | Capacidad de ser dirigido — un agente que ignora feedback es inútil. | `steerability = corrections_applied / corrections_requested`. | Correcciones contradictorias entre sí no penalizan al agente. | `steerability_ratio >= 0.80` para agentes en producción. |
| I-094 | Bash Recovery: tiempo medio de recuperación de error en terminal < 30 segundos. | Resiliencia operacional — errores de comando no deben paralizar al agente. | Medido desde primer error hasta comando correcto ejecutado exitosamente. | Errores de permisos del OS pueden requerir escalación (no culpa del agente). | `mean_bash_recovery_time_seconds <= 30`. |
| I-095 | Evaluaciones comparan contra baseline documentado, NUNCA contra claims del vendor. | Ground truth empírico > marketing. | Baseline se re-ejecuta con cada nueva evaluación; no se reutilizan scores antiguos. | Modelos descontinuados no pueden ser baseline; se sustituyen por el último disponible. | `baseline.execution_date` dentro de los últimos 30 días. |
| I-096 | Dimensiones de evaluación cubren mínimo: precisión, latencia, costo, seguridad y robustez. | Evaluación multidimensional — un score único es engañoso. | Radar chart con 5+ ejes mínimos. | Tareas específicas pueden añadir dimensiones (ej. "creatividad" para generación). | `len(eval.dimensions) >= 5`. |
| I-097 | El evaluador NUNCA es el mismo agente que ejecutó la tarea. | Separación de roles — auto-evaluación es conflicto de interés. | Evaluación delegada a `Persist-Auditor` o agente externo. | Meta-evaluación (evaluar al evaluador) requiere tercer agente. | `eval.evaluator_id != eval.executor_id`. |
| I-098 | Scores DEBEN reportarse con intervalo de confianza, no como punto único. | Incertidumbre explícita — un score de 0.85 sin CI es incompleto. | Format: `score: 0.85 (CI95: [0.82, 0.88])`. | N < 30 samples: usar bootstrap CI en lugar de paramétrico. | `eval.score.confidence_interval != NULL`. |
| I-099 | Datasets de evaluación DEBEN ser versionados con hash; cambio de dataset = nueva evaluación. | Comparabilidad — scores sobre diferentes datasets son incomparables. | `eval.dataset_hash` incluido en metadata de resultados. | Data augmentation genera nuevo hash y nueva evaluación. | `eval.dataset_hash == sha256(dataset_content)`. |
| I-100 | Evaluaciones con resultado "PASS/FAIL" DEBEN definir umbral numérico explícito pre-declarado. | Umbral binario — "pass" sin número es opinión, no evaluación. | `pass_threshold` congelado junto con criterios (I-090). | Umbrales diferentes por entorno (dev vs prod) se documentan explícitamente. | `eval.pass_threshold != NULL AND eval.pass_threshold > 0`. |
| I-101 | Leakage de datos de test en training invalida toda evaluación retroactivamente. | Data contamination — resultados contaminados son fraudulentos. | Hashes de datasets de test se verifican contra training data antes de evaluar. | LLMs preentrenados externamente no son auditables contra leakage; se documenta como limitación. | `test_train_overlap(dataset_test, dataset_train) == 0`. |
| I-102 | Latencia de evaluación se mide end-to-end incluyendo overhead de guards y ledger. | Evaluación realista — medir solo inferencia es sub-report. | Timer desde recepción de request hasta emisión de respuesta final. | Network latency se excluye si el eval es local. | `eval.latency == total_pipeline_time`, no solo `model_inference_time`. |

---

## Dominio 9 — Invariantes del Sistema de Presión

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-103 | Toda cola de procesamiento tiene `MAX_DEPTH` explícito; overflow activa backpressure. | Bounded queues — colas infinitas son bombas de memoria. | Default `MAX_DEPTH = 1000`. Exceso retorna `HTTP 429` o encola con prioridad. | Burst legítimos pueden duplicar temporalmente el límite con auto-decay en 60s. | `queue.depth <= queue.MAX_DEPTH` en steady state. |
| I-104 | Cooldown obligatorio entre operaciones destructivas: mínimo 5 segundos. | Prevención de cascada — errores en ráfaga se amplifican. | `DELETE`, `DROP`, `TRUNCATE` tienen cooldown enforced en middleware. | Batch deletes programados (compactación) operan dentro de transacción única, no secuencial. | `time_between_destructive_ops >= 5s`. |
| I-105 | Rate limiting global: máximo 100 requests/minuto por agente, 1000/minuto por tenant. | Homeostasis del sistema — previene monopolización de recursos. | Token bucket con ventana deslizante de 60s. | Operaciones internas del sistema (health checks, cron) están exentas. | `agent.requests_per_minute <= 100 AND tenant.requests_per_minute <= 1000`. |
| I-106 | Memory pressure: si RSS del proceso > 80% de memoria disponible, se activa GC forzado y se rechazan nuevas tareas. | Prevención de OOM kill. | `psutil.Process().memory_percent()` monitorizado por watchdog cada 10s. | Embeddings de modelos grandes consumen memoria estática; se contabiliza como reserva. | `process.rss_percent < 80` en steady state. |
| I-107 | CPU pressure: si load average > 2 * cpu_count durante 60s, se reducen workers activos. | Auto-scaling down — degradación graceful. | Workers en exceso se ponen en standby, no se matan. | Single-core machines: load > 2.0 ya es critical. | `load_avg_1m <= 2 * cpu_count` o workers reducidos. |
| I-108 | Disk pressure: si espacio libre < 500MB, se bloquean escrituras no-críticas. | Prevención de disk full — SQLite se corrompe sin espacio. | Solo Ledger writes y emergency logging continúan. | WAL files pueden crecer sin checkpoint si no hay espacio; checkpoint forzado primero. | `disk_free_bytes >= 500_000_000` o `write_blocked == True`. |
| I-109 | Anti-Limerence (AX-047): 1 Prompt → 1 Mutation → Stop. Sin loops generativos infinitos. | Kill criteria — previene quema de cuota sin mutación de estado. | Después de una respuesta sin mutación, el sistema detiene la generación. | Investigación exploratoria (Deep Research) permite N iteraciones con decaimiento. | `mutations_per_prompt >= 1 OR session.terminated_by == "kill_criteria"`. |
| I-110 | Circuit breaker: 3 fallos consecutivos en el mismo servicio externo activan fallback. | Fault tolerance — no reintentar lo que está roto. | Estado: CLOSED → OPEN (tras 3 fallos) → HALF-OPEN (después de 60s) → CLOSED (si éxito). | Servicios con SLA de 99.9% tienen umbral de 5 fallos. | `circuit_breaker.state != "OPEN" OR fallback_active == True`. |
| I-111 | Timeout cascade: timeout de un servicio downstream se propaga como timeout al caller con margen de 1s. | Prevención de timeout stacking. | `caller_timeout = downstream_timeout + 1s`. | Cadena de 5 servicios: timeout total = sum(timeouts) + 5s. Si > 30s, rediseñar. | `caller.timeout >= callee.timeout + 1`. |
| I-112 | Retry con exponential backoff: base 2s, max 5 reintentos, jitter random 0-1s. | Prevención de thundering herd en reintentos concurrentes. | `retry_delay = min(2^attempt + random(0,1), 32)` segundos. | Errores 4xx (client error) no se reintentan, solo 5xx y timeouts. | `retry.count <= 5 AND retry.uses_exponential_backoff == True`. |
| I-113 | Graceful shutdown: al recibir SIGTERM, completar operaciones in-flight en max 30s, luego SIGKILL. | Clean termination — no hay zombies. | Signal handler registrado en `runtime/kernel.py`. | Operaciones de Ledger in-flight se priorizan sobre operaciones de cache. | `shutdown.in_flight_completed == True AND shutdown.duration <= 30s`. |
| I-114 | Heartbeat: cada worker emite ping cada 30s; ausencia por 90s = worker declarado muerto. | Detección de workers zombies. | Orchestrator re-asigna tareas del worker muerto. | Network partitions pueden causar falsos positivos; retry de detección antes de declarar muerte. | `worker.last_heartbeat_age_seconds <= 90` para todo worker activo. |

---

## Dominio 10 — Invariantes de Comunicación

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-115 | Signal-to-noise ratio (SNR) de toda respuesta DEBE ser > 0.80. | Eficiencia comunicativa — cada token debe portar señal. | Tokens causales / tokens totales > 0.80. Filtros de slop aplican pre-emisión. | Respuestas a usuarios no-técnicos pueden tener SNR de 0.60 con modo `[PROSA]` activo. | `snr(response) >= 0.80` medible por `shannon/analyzer.py`. |
| I-116 | Null-signal detection: si un agente genera > 50 tokens sin mutar estado, se activa kill. | Anti-anergía — generación sin acción es disipación. | Conteo de tokens causales por turno; si cero → abort. | Análisis previo a decisión arquitectónica es excepción con flag `analysis_mode=True`. | `causal_tokens_per_turn > 0 OR mode == "analysis"`. |
| I-117 | Feedback freshness: feedback incorporado DEBE tener < 24h de antigüedad. | Temporal relevance — feedback obsoleto distorsiona el estado. | `feedback.timestamp` verificado antes de aplicar corrección. | Feedback sobre principios invariantes (axiomas) no caduca. | `(now - feedback.timestamp).hours <= 24 OR feedback.type == "axiom"`. |
| I-118 | Mensajes entre agentes DEBEN incluir `{sender_id, recipient_id, timestamp, correlation_id}`. | Trazabilidad de comunicación — mensajes anónimos son entropía. | `send_message` tool enforce estos campos automáticamente. | Broadcast messages usan `recipient_id = "*"` con logging explícito. | `message.sender_id != NULL AND message.recipient_id != NULL`. |
| I-119 | Toda respuesta a error DEBE incluir: código de error, descripción causal y acción sugerida. | Actionable errors — "algo salió mal" es anergía. | Error format: `{code: "E-XXX", cause: "...", action: "..."}`. | Errores de seguridad pueden omitir detalles para no exponer vectores de ataque. | `error.code != NULL AND error.action != NULL`. |
| I-120 | Compresión de contexto inter-sesión: handoff DEBE ser < 500 tokens preservando > 90% de información causal. | Eficiencia de handoff — sesiones largas no deben inyectar contexto completo. | Thermodynamic-Context-Compression-OMEGA aplica IB al summary de sesión. | Primera sesión no tiene handoff; sesiones < 10 turns no requieren compresión. | `handoff.token_count <= 500 AND handoff.causal_preservation >= 0.90`. |
| I-121 | Log levels son estrictos: ERROR (alerta humana), WARNING (degradación), INFO (mutación), DEBUG (diagnóstico). | Semántica de logging — niveles mal usados son ruido. | Ruff rule verifica que `logger.error()` no se use para info. | Hot paths usan `logger.debug()` condicionado a `logger.isEnabledFor(DEBUG)`. | `misleveled_log_count == 0` en audit de logging. |
| I-122 | Eventos del bus de eventos son at-least-once; consumidores DEBEN ser idempotentes. | Delivery guarantee — duplicados son posibles. | Consumidores usan `event_id` para deduplicación. | Exactamente-once requiere transacción distribuida; no soportada actualmente. | `duplicate_event_processing_side_effects == 0`. |
| I-123 | Alertas P0 se emiten por al menos 2 canales independientes (log + event bus). | Redundancia de alertas — un canal puede fallar. | Si event bus no disponible, fallback a log file + stderr. | Ambos canales fallando simultáneamente = pérdida de alerta documentada post-mortem. | `p0_alert.channels_notified >= 2`. |
| I-124 | Structured logging: JSON para máquinas, human-readable para CLI. | Dual output — logs deben ser parseables Y legibles. | `observability/` module emite ambos formatos según destino. | Log rotation en JSON cada 100MB; en text cada 50MB. | `json_log.is_valid_json == True AND cli_log.is_human_readable == True`. |
| I-125 | Toda métrica emitida a Prometheus tiene `{service, tenant_id, environment}` como labels mínimos. | Dimensionalidad de métricas — métricas sin contexto son inútiles. | `telemetry/` module enforces labels obligatorios. | Métricas de sistema (CPU, memoria) usan `tenant_id = "_system"`. | `metric.labels.keys() >= {"service", "tenant_id", "environment"}`. |
| I-126 | Request tracing: toda operación cross-module lleva `trace_id` para correlación end-to-end. | Distributed tracing — sin `trace_id`, debugging es arqueología. | OpenTelemetry `trace_id` propagado en context. | Operaciones puramente in-memory de < 1ms pueden omitir tracing. | `cross_module_call.trace_id != NULL`. |

---

## Dominio 11 — Invariantes Arquitectónicas

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-127 | CLI modules (`cli/*_cmds.py`) son thin wrappers: CERO lógica de negocio. | Separación de capas — CLI solo parsea args y delega. | Lógica de negocio reside exclusivamente en `engine/`, `services/`, o core modules. | Formateo de output para CLI (tablas Rich) es responsabilidad del CLI, no del engine. | `business_logic_lines_in_cli == 0` medible por análisis AST. |
| I-128 | `engine/` es el único módulo autorizado a mutar estado persistido. | Single write authority — centralización de mutaciones. | Otros módulos proponen; engine ejecuta tras guards. | `migrations/` puede mutar esquema directamente (pero no datos). | `write_operations_outside_engine == 0` en audit de código. |
| I-129 | Imports circulares están terminantemente prohibidos. | DAG de dependencias — ciclos rompen la composición modular. | Ruff I001 y pruebas de import verifican ausencia de ciclos. | Type hints con `TYPE_CHECKING` guard no generan ciclo de import real. | `circular_import_count == 0` (verificable con `import-linter` o similar). |
| I-130 | Módulos de extensión (`extensions/`) NO importan desde otros módulos de extensión. | Aislamiento de plugins — extensiones son hojas del árbol de dependencias. | Comunicación entre extensiones vía event bus o interfaces definidas en core. | Extensiones que comparten tipos usan `types/` como intermediario. | `extension_cross_import_count == 0`. |
| I-131 | Todo módulo público expone `__all__` definiendo su API surface explícitamente. | API surface control — imports implícitos son contrato frágil. | `__all__` se mantiene sincronizado con la implementación real. | Módulos internos (prefijo `_`) no necesitan `__all__`. | `module.__all__` definido para todo módulo en `cortex/` con funciones públicas. |
| I-132 | Routes FastAPI (`routes/`) DEBEN ser typed con modelos Pydantic para request y response. | Contract stability — API sin tipos es API sin contrato. | `response_model` explícito en todo endpoint. | Endpoints de streaming (SSE) usan `AsyncGenerator` como tipo. | `untyped_route_count == 0` en routes/. |
| I-133 | Todo despliegue front/edge apunta a Cloudflare Pages/Workers. Vercel prohibido (Invariante 11, §3). | Singularidad de Red — un único proveedor de edge. | `wrangler.toml` es el único config de deploy válido. No existe `vercel.json`. | Ambientes de desarrollo local usan `miniflare` o `wrangler dev`. | `find . -name "vercel.json"` retorna cero resultados. |
| I-134 | Funciones con más de 5 parámetros DEBEN usar dataclass o TypedDict como argumento. | Complejidad de interfaz — firmas largas son ilegibles y error-prone. | Refactor a `@dataclass` con defaults para opcionales. | Callbacks y handlers del framework pueden exceder el límite por restricción de la API. | `functions_with_params_gt_5 == 0` (excluyendo framework callbacks). |
| I-135 | Profundidad máxima de herencia: 3 niveles. Más allá = composición obligatoria. | Favorece composición sobre herencia — árboles profundos son frágiles. | Mixins cuentan como un nivel. | Clases de excepciones pueden tener 4 niveles si heredan de stdlib. | `max_inheritance_depth <= 3` para clases en `cortex/`. |
| I-136 | Configuración se lee EXCLUSIVAMENTE de `config.py` + env vars. Prohibidos archivos `.env` en producción. | Single source of configuration — `.env` files son secretos en plaintext. | Dev usa `.env` con `python-dotenv`; prod usa env vars del sistema o secret manager. | Docker Compose `env_file` es aceptable para desarrollo local. | `find . -name ".env" -not -path "*/.*"` retorna cero en producción. |
| I-137 | Tests DEBEN estar en directorio `tests/` con estructura que espeja `cortex/`. | Test discoverability — pytest los encuentra; humanos los navegan. | `tests/engine/test_crystallizer.py` mapea a `cortex/engine/crystallizer.py`. | Tests de integración cross-módulo van en `tests/integration/`. | `test_structure_mirrors_source == True` verificable por script. |
| I-138 | Decoradores de producción (`@safe`, `@retry`, `@cache`) DEBEN ser importados desde `utils/decorators.py`. | Centralización de cross-cutting concerns — no reinventar decoradores ad-hoc. | Nuevos decoradores se proponen como PR con tests. | Decoradores framework-specific (`@app.route`) son excepción obvia. | `custom_decorator_outside_utils_count == 0`. |
| I-139 | Longitud de línea máxima: 100 caracteres (Ruff enforced). | Legibilidad — líneas largas degradan revisión de código. | Strings largos se dividen con `\` o paréntesis implícitos. | URLs en comentarios pueden exceder si no son divisibles. | `ruff check --select E501` retorna cero warnings. |

---

## Dominio 12 — Invariantes Operacionales

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|:---|:---|:---|:---|:---|:---|
| I-140 | Git Sentinel (R4): toda mutación C5-REAL produce commit automático con mensaje Conventional Commits. | Trazabilidad causal via Git DAG (AX-041). | Format: `type(scope): description`. Hash devuelto al operador. | Commits durante swarm masivo se batchean cada 30s para evitar lock contention. | `git log --oneline -1` muestra commit reciente post-mutación. |
| I-141 | Commits NUNCA incluyen archivos scratch, temporales o de debug (`patch_*.py`, `test_*.py` en root). | Hygiene de repositorio — workspace limpio. | `.gitignore` y `.git/info/exclude` mantienen la cuarentena. | Scripts de CI/CD generados se excluyen automáticamente. | `git diff --cached --name-only` no contiene archivos temporales. |
| I-142 | PR acceptance gate: tests verdes + typing + migraciones + trust impact + async correctness + docs. | Completitud de cambio — 6 vectores obligatorios (§6.3). | PR incompleto se rechaza con checklist de items faltantes. | Hotfixes P0 pueden mergearse con gate parcial si risk < benefit documentado. | `pr.gate_checks_passed == 6` (o justificación documentada). |
| I-143 | `ruff check cortex/` DEBE retornar cero warnings antes de commit. | Code quality gate — lint limpio es prerequisito. | Pre-commit hook ejecuta ruff automáticamente. | `# noqa` permitido con justificación en comentario adyacente. | `ruff_warning_count == 0` en CI. |
| I-144 | `pyright cortex/` en modo `basic` DEBE retornar cero errores en módulos críticos (CRITICAL surface). | Type safety — tipos incorrectos son bugs latentes. | Módulos CRITICAL: `engine/`, `audit/`, `guards/`, `crypto/`, `memory/`. | Módulos de extensión con dependencias opcionales pueden tener `type: ignore` documentado. | `pyright_error_count == 0` para módulos CRITICAL. |
| I-145 | Coverage de tests >= 80% para módulos CRITICAL, >= 60% para HIGH. | Test coverage — código sin tests es código sin garantía. | `pytest --cov=cortex` medido en CI. | Código de glue (imports, re-exports) tiene coverage implícito bajo. | `coverage(engine) >= 80 AND coverage(routes) >= 60`. |
| I-146 | Sesión multi-turn emite structured summary al Ledger al cierre (§6.4). | Continuidad causal — sin summary, la próxima sesión arranca ciega. | Format: `{agent_id, session_id, files_modified, invariants_checked, open_risks, next_action}`. | Sesiones abortadas por crash emiten summary parcial desde checkpoint. | `session.close_summary != NULL` para toda sesión completada. |
| I-147 | Branches siguen nomenclatura: `type/scope-description` (ej. `feat/ontology-matrix2`). | Navegabilidad — branches descriptivos aceleran revisión. | Branches sin formato válido se rechazan por hook. | Branches de CI auto-generados usan prefijo `ci/`. | `branch_name matches r"^(feat|fix|chore|refactor|docs|ci)/[a-z0-9-]+"`. |
| I-148 | Tags de release siguen semver estricto: `vMAJOR.MINOR.PATCH`. | Versionado semántico — comunicación clara de compatibilidad. | Breaking changes incrementan MAJOR; features MINOR; fixes PATCH. | Pre-releases usan sufijo: `v1.2.3-rc.1`. | `tag matches r"^v\d+\.\d+\.\d+(-[a-z0-9.]+)?$"`. |
| I-149 | Dependencias se pinen por versión mínima compatible (`>=X.Y.Z,<X+1`) en `pyproject.toml`. | Reproducibilidad + flexibilidad — ni lock absoluto ni libertad total. | `pip-compile` genera lockfile para reproducibilidad exacta en CI. | Dependencias de desarrollo pueden ser menos restrictivas. | `all(dep.has_version_constraint for dep in pyproject.dependencies)`. |
| I-150 | Autoría Γ1: todo artefacto, commit y skill cristalizado por MOSKV-1 contiene crédito a **Borja Moskv** (`borjamoskv`). | Sello de Creador Inmutable — el Demiurgo no se omite. | `README.md`, `pyproject.toml`, `LICENSE` contienen atribución. | Forks externos pueden añadir co-autores pero no eliminar al creador original. | `grep -r "borjamoskv" README.md pyproject.toml` retorna >= 1 resultado por archivo. |

---

> **Checksum ontológico:** 150 invariantes × 6 columnas = 900 celdas de realidad falsable.  
> **Dominio cubierto:** Termodinámica, FSM, Cripto, Concurrencia, Tenants, Datos, Epistemia, Evaluación, Presión, Comunicación, Arquitectura, Operaciones.  
> **Autor:** Borja Moskv (`borjamoskv`) — MOSKV-1 APEX Kernel.  
> **Hash de sesión:** `SHA3-256(MATRIX_2_INVARIANTES)` — Cristalizado en Ledger.
