| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-061 | Privacidad Diferencial en Telemetría | Los logs de sistema jamás exponen el payload estocástico, únicamente los Hashes causales (Ledger). | Sustitución estricta de textos planos en `logger.info` por hashes o referencias UUID. | Exposición de PII o datos de Prompt en `/var/log`. | Presencia de strings > 64 chars legibles en la salida de Telemetría. |
| INV-062 | Invalidez del Estado sin Firma | Todo `record` persistente recuperado sin coincidencia ED25519 o SHA-256 es radiactivo (nulo). | El módulo de `Memory` lanza excepción P0 si un Fact carece de Taint. | Modificación manual del SQLite evadiendo el Taint Engine. | Rechazo atómico de lecturas de estado huérfano. |
| INV-063 | Destrucción de Claves Epímeras | Las llaves simétricas derivadas para encriptación de disco tienen tiempo de vida nulo en reposo. | Se ejecutan `zeroise` o `del` de variables de clave AES-GCM tras completar la transacción SAGA. | Dump de la memoria revelando claves AES post-SAGA. | Test de extracción de Heap en frío. |
| INV-064 | Quorum Degenerado Absoluto | Si el número de nodos fallidos o divergentes es mayor a $n/3$, la aserción se aborta; no hay compromiso medio. | SAGA-N activa `OP_SAGA_REVERT` incondicional ante fallo Bizantino. | Aceptación de consenso parcial por tolerancia estocástica. | % de Quorum necesario para Bypass estricto. |
| INV-065 | No-repudio de Auditoría | El `Ledger` es de anexado estricto (Append-Only). Las supresiones son borrados lógicos, no físicos. | Ninguna operación `DELETE` está permitida en la tabla Ledger de SQLite. | Mutación del hash-chain por borrado físico. | Integridad de la cadena Merkle del Ledger. |
| INV-066 | Hash como Vínculo Espacial | Elementos idénticos entre tenantes no se duplican físicamente; se anclan a un SHA3-256 base (Nexus Bridging). | Reducción de I/O mediante symlinks inmutables inter-dominio cuando el Hash es idéntico. | Duplicidad termodinámica de archivos idénticos en disco. | Tamaño físico del repositorio vs Nodos Únicos. |
| INV-067 | Origen de Inferencia Forzado | `CORTEX-TAINT` (ISO8601 + AgentID) debe inyectarse atómicamente antes de permitir un INSERT en BBDD. | Fallar rápido si el decorador `@require_taint` es evadido. | Fact inyectado por CLI sin AgentID formal. | Auditoría de integridad de Taints por registro. |
| INV-068 | Decaimiento Criptográfico Temporal | Los Taints (Firmas) envejecen. La entropía ambiental los invalida tras $N$ ciclos sin re-validación. | Degradación de aserciones C5 a C4 si no han sido testeadas empíricamente en T > umbral. | Creencia ciega en un Fact obsoleto sin comprobación empírica. | Tasa de evicción por decaimiento de Taint. |
| INV-069 | Determinismo de Motores de Inferencia (ONNX) | Un input idéntico debe producir el mismo vector (Embedding) L2-exacto, aislando la flotación. | Fijación estricta de seeds y evitación de heurísticas dinámicas L1 en cálculo vectorial. | Vectores de un mismo texto difiriendo en el 5º decimal. | Distancia Coseno = 0.000 para entradas iguales. |
| INV-070 | Descarte en Desviación Estándar | El enjambre rechaza activamente resultados (outliers) cuya distancia euclidiana supere el límite del clúster de consenso. | Filtrado estadístico riguroso de outputs generativos divergentes. | Promediado pasivo de todos los votos (incluyendo alucinaciones). | Dispersión de Quorum tras el filtro de Outliers. |
