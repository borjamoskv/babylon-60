[workspace]
resolver = "2"
members = [
    "core",
    "ir_vm",
    "zk",
    "ledger",
    "economic",
    "tee",
    "runtime",
    "cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["borjamoskv"]

[workspace.dependencies]
# We'll put common dependencies here later
