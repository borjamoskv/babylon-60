services:
  - type: web
    name: cortex-persist-api
    env: docker
    dockerContext: .
    dockerfilePath: Dockerfile
    disk:
      name: cortex-data
      mountPath: /data
      sizeGB: 10
    envVars:
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: STRIPE_PRICE_TABLE
        value: '{"pro": "price_1...", "team": "price_2..."}'
