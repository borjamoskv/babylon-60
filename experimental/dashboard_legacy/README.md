# Cortex-Persist 
**Tamper-evident memory & decision lineage for AI agents. Cryptographic proof of what your agent knew.**

Cortex-Persist is an autonomous verification layer designed to ensure AI Agents (Swarm Operations, Financial Decisions) cannot be manipulated without leaving an immutable cryptographic trace (Proof of Knowledge via Vector Symbolic Architectures).

## The Core Value Demo 🚀
Are you dealing with pilots, pitch decks, or validation checks? See exactly how Cortex detects Memory Tampering in literally 2 minutes.

1. Navigate to the `demo` directory.
2. Run the canonical walkthrough:

```bash
cd demo
python3 demo_canonical.py
```

This single local script handles Genesis Memory writing, synthetics a database hijacking event, and runs Falsation Checks to produce an exportable Audit Seal.

👉 **[Read the Full Sales Pilot Kit](./docs/pilot_kit.md)**

---

## Workspace Structure
- `/demo` - The canonical 2-minute product narrative (CLI native).
- `/docs` - Go-to-market materials and Pilot Deployment rules.
- `/engine` - The native Rust Cortex engines (`cortex-strike`, `cortex-db`).
- `/scripts` - Swarm and Agent logic (`hound_omega`, etc.).
- `/ui` - The Industrial Noir Vite-based Dashboard.
