-- [C5-REAL] Fast 3-Colorability Checker
-- Auto-generated by generate_lean_proof.py
def is_colorable_hard_graph : Bool := 
  let k := 3
  List.any (List.range k) fun c0 =>
    List.any (List.range k) fun c1 =>
      if c1 == c0 then false else
        List.any (List.range k) fun c2 =>
          if c2 == c0 then false else
            List.any (List.range k) fun c3 =>
              if c3 == c1 || c3 == c0 || c3 == c2 then false else
                List.any (List.range k) fun c4 =>
                  if c4 == c0 || c4 == c3 then false else
                    List.any (List.range k) fun c5 =>
                      if c5 == c0 || c5 == c1 || c5 == c3 then false else
                        List.any (List.range k) fun c6 =>
                          if c6 == c2 || c6 == c4 || c6 == c5 then false else
                            List.any (List.range k) fun c7 =>
                              if c7 == c0 || c7 == c1 || c7 == c2 || c7 == c3 || c7 == c5 then false else
                                List.any (List.range k) fun c8 =>
                                  if c8 == c0 || c8 == c1 || c8 == c2 || c8 == c3 || c8 == c6 || c8 == c7 then false else
                                    List.any (List.range k) fun c9 =>
                                      if c9 == c1 || c9 == c0 || c9 == c6 || c9 == c7 || c9 == c8 then false else
                                        true

-- Teorema formal: Computar `is_colorable_hard_graph` demuestra exhaustivamente que es falso.
theorem hard_graph_is_unsat : is_colorable_hard_graph = false := by rfl
