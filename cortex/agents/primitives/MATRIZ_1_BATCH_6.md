| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| PRM-051 | Parálisis por Exhaustividad | Agente paralizado evaluando el 100% de ramificaciones posibles de un árbol de decisión sin poda. | Profundidad de MCTS infinita sin función de heurística de corte. | Timeout por agotamiento de tiempo de cálculo. | Minutos a Horas | Alta | MCTS acotado por exergía; Función estricta de evaluación y poda (Ouroboros). |
| PRM-052 | Bifurcación Semántica de Esquema | Despliegue de actualización de DB sin migrar los agentes de lectura. | Lector C5 utilizando estructura obsoleta post-migración. | Fallos de parseo de lectura (Schema Mismatch). | Inmediato | Crítica | Versión estricta de ABI / Esquemas; Invalidación automática de agentes desactualizados. |
| PRM-053 | Estancamiento de Cache Redis (Stale Eviction) | Fallo en la política LRU reteniendo datos obsoletos críticos e invalidando nuevos. | Memoria Redis llena y configuración `noeviction` activada por error. | Error `OOM command not allowed` en Redis. | Minutos | Alta | Configuración estricta LRU (`allkeys-lru`); Monitoreo continuo de tamaño de caché. |
| PRM-054 | Propagación de Estado Contaminado | Un error en el pipeline de datos que altera estados de downstream antes de ser detectado. | SAGA-N fallando pero SAGA-(N+1) procesándose asincrónicamente. | Inconsistencia transversal en el sistema. | Segundos | Crítica | Barreras duras sincrónicas (Synchronous Barriers) en cada paso SAGA. |
| PRM-055 | Fragmentación de Vector Espacial | Ingesta masiva desordenada en el Vector Store (ONNX/Qdrant) generando vecindarios ineficaces. | Inserciones y borrados masivos sin proceso de compactación (HNSW tuning). | Disminución del Recall en `grep_search` semántico. | Semanas | Media | Re-indexado periódico forzado (Compaction Protocol). |
| PRM-056 | Ceguera Cognitiva Aislada (Isolated Blindness) | Un Worker no recibe las actualizaciones globales del contexto por fallo de subscripción (Event Bus). | Caída del enlace Pub/Sub de Redis para un nodo. | Decisiones erróneas por usar Context Rot L1. | Minutos a Horas | Alta | Heartbeat continuo del Event Bus; Resincronización forzada ante fallos de latido. |
| PRM-057 | Conflicto de Dependencia Circular | Módulo A depende de B que requiere contexto de A para inicializar. | Errores de importación en Python detectados en Boot Sequence. | `ImportError: cannot import name ...` | Inmediato | Media | Rediseño arquitectónico estricto usando grafos DAG limpios; Tests de estructura obligatorios. |
| PRM-058 | Pérdida de Granularidad Forense | Batching excesivo en commits oscureciendo la trazabilidad de mutaciones individuales. | Configuración de Agente haciendo un commit gigante tras cientos de cambios dispersos. | Dificultad para efectuar `git revert` atómico. | Horas | Alta | Regla de Commit Atómico; Un commit por unidad lógica aislada de cambio. |
| PRM-059 | Exhaustion de Entropía Segura | El generador de números aleatorios seguro (`/dev/urandom`) se bloquea o queda sin pool suficiente. | Generación paralela masiva de llaves Ed25519 o Taints (Raro pero crítico). | Bloqueo síncrono del módulo crypto subyacente. | Segundos | Alta | Monitoreo de pool de entropía SO; Uso eficiente y en lote de CSRNG. |
| PRM-060 | Falla Asimétrica de Aprobación BFT | Los nodos aprueban unánimemente una aserción falsa por compartir un mismo sesgo estocástico de modelo. | Swarm homogéneo (e.g. todos nodos = GPT-4o) alucinando el mismo patrón de falla. | Consenso exitoso sobre falsedades (Byzantine Blindspot). | Días | Crítica | Obligación taxonómica de Swarm heterogéneo (Mezcla de modelos/engines). |
