| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-041 | Polarización de Confianza (Trust Polarization) | Todos los subsistemas son considerados absolutamente inseguros o absolutamente confiables, no hay gradientes persistentes. | Módulo core = Trust 100% (Verificado); Módulo gen = Trust 0% (Requiere BFT/SAGA). | Zonas grises donde se permite mutar "a medias" sin aserción completa. | Infracciones a Validación de Filtros de Entrada. |
| INV-042 | Inflexibilidad Lexicográfica | Los identificadores taxonómicos no son interpretables, se procesan como Hashes estrictos. | La clave "C5-REAL" o "PRM-001" debe coincidir byte por byte; fallan si hay un espacio. | LLM generando "PRM 001" o "C5 REAL". | Tasa de rechazos de Parser Regex C5. |
| INV-043 | Resiliencia al Ruido Semántico | El Kernel ignora por completo la semántica de la prosa no acotada en `[PROSA]` o directivas no estructuradas. | Parsers YAML/JSON que descartan líneas libres de texto del output del LLM. | Agent Bus intentando ejecutar texto en prosa. | Tokens Descartes / Tokens Totales generados > 0. |
| INV-044 | Anclaje Absoluto Espacio-Temporal | Los UUIDv7 proveen una garantía lexicográfica y temporal de la creación de las entidades. | Sustitución estricta de UUIDv4 por UUIDv7 en todas las bases persistentes CORTEX. | Desincronización o reordenamiento en inserciones masivas concurrentes. | Comprobación de orden temporal de IDs en Ledger. |
| INV-045 | Principio de Menor Esfuerzo de Routing | El Hypervisor dirigirá la tarea al agente computacionalmente más barato que supere la entropía requerida. | Flash o modelos locales para parsing; Frontera (Opus) solo para UltraThink P0. | Tarea trivial enviada a un modelo exérgicamente costoso. | Exergía gastada por clasificación de tarea. |
| INV-046 | Tensión de Fractura de Bloqueo | Los Mutex y Locks en base de datos deben fallar bajo timeout (5000ms) en lugar de encolar indefinidamente. | SQLite `busy_timeout` activado en WAL; Fallo con Exception clara (PRM-004) para SAGA compensatorio. | Deadlock termodinámico de hilos de disco. | Tasa de Timeouts vs Tasa de Esperas Síncronas Infinitas. |
| INV-047 | Transparencia de Invariantes Órganos | Todo nodo en ejecución debe ser capaz de volcar su estado interno (Grafo, Memoria, Taints) en $O(1)$ para auditoría. | Implementación de `/health` o señales de interrupción (SIGUSR1) que generen dumps C5. | Estado opaco o variables inescrutables de runtime. | Cobertura de Snapshot Completo del Estado Runtime. |
| INV-048 | Invariabilidad de Retorno Asíncrono | Funciones que devuelven `Awaitables` no deben retornar tipos mixtos sincrónicos bajo condiciones de borde. | Todo bloque de un `async def` debe asegurar que su retorno es utilizable sin bloqueos. | `TypeError` por retorno `None` vs `Future`. | Comprobaciones tipo estáticas Pyright Strict. |
| INV-049 | Acoplamiento por Hash Criptográfico | Módulos independientes se asocian lógicamente verificando hashes en cadena y no mediante llamadas directas RPC. | Inyección de dependencias basada en Proofs (Ledger) permitiendo arquitectura descentralizada (Swarm). | Acoplamiento duro (Hard-linking) forzando latencia. | Tolerancia a la desconexión total de la red. |
| INV-050 | Disociación Estocástica | Si el resultado de una operación varía >1% en ejecuciones sucesivas, la operación es inválida para mutar estado. | Tests unitarios ejecutados N veces (Flaky tests detection) para invalidar primitivas no deterministas. | Temperatura LLM > 0 usada en toma de decisiones críticas P0. | Coeficiente de Varianza de Múltiples Ejecuciones. |
