| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-051 | Causalidad Unidireccional del Saga | Un flujo SAGA avanza hacia la persistencia o retrocede en caso de aborto, pero jamás diverge ortogonalmente. | Prohibido instanciar sub-procesos mutables colaterales durante la ejecución de los SAGA-N. | Fallo en paso intermedio dejando estado fantasma. | Nodos huérfanos sin rastro en Ledger. |
| INV-052 | Prevención de Apilamiento de Eventos | Las colas infinitas generan latencia térmica. La congestión se resuelve con backpressure destructivo (Apoptosis). | Límite estricto de eventos por segundo; descarte absoluto si se excede la ventana de asimilación. | Bloqueo por sobrecarga de peticiones. | Tiempo de procesamiento P99 < 200ms. |
| INV-053 | Grafo de Dependencias Acíclico (DAG) Absoluto | Las dependencias circulares inducen bucles de parálisis bizantina. El estado debe ser estrictamente topológico. | Los módulos del núcleo jamás pueden importarse recursivamente; el flujo es O(N). | Error de inicialización `ImportError` circular. | Profundidad del árbol de llamadas en Tracer. |
| INV-054 | Destrucción de Contextos Latentes | Variables con llaves o datos de alta densidad exergética se sobrescriben en RAM, no se confían al Garbage Collector. | Ejecución de `OP_VAULT_UNMOUNT` (C-level `memset`) obligatoria tras su uso. | Fuga de memoria o persistencia de secretos en RAM. | Dumps de memoria revelando secretos extintos. |
| INV-055 | Minimización del Radio de Explosión | El fallo estocástico de un Worker (Agente) no puede corromper el Quorum del Swarm. Aislamiento absoluto de hilos. | Cada sub-agente corre en su propio proceso/contexto aislado. | Pánico en un Worker arrastra al Master. | Supervivencia del proceso padre (PID 1) ante SIGKILL de hijos. |
| INV-056 | Transacciones Idempotentes | Toda mutación física debe ser idéntica independientemente de si se ejecuta 1 o N veces. | Los `UPSERT` y escrituras usan condicionales de estado preexistente criptográfico. | Inserción múltiple generando duplicados en DB. | Hash del registro antes y después de repetición de operación. |
| INV-057 | Asimetría de Lectura/Escritura | El `read-path` no usa bloqueos de contención; el `write-path` requiere consenso y firmas ED25519 serializadas. | Consultas rápidas (Dirty-Reads tolerados), pero escrituras atómicas BFT. | Lector bloqueando al escritor (Deadlock de DB). | Latencia de lectura independiente de contención de escritura. |
| INV-058 | Determinismo de Parsers | Las expresiones regulares y los validadores JSON no pueden colapsar en recursión (ReDoS). Límites estrictos de ejecución. | Tiempo de parsing O(N). Descarte automático de strings que superen M bytes. | Ataque de inyección de complejidad (ReDoS). | Milisegundos gastados en validación de Payload. |
| INV-059 | Entropía Aislada de Red | Cualquier input HTTP externo se trata como radiación (untrusted) hasta que es neutralizado por un Guardián. | Paso obligatorio por `ZK-Guard` o `Taint Seal` previo al SAGA-1. | Ingesta de JSON sin firma. | Bypass de middleware registrado en Ledger. |
| INV-060 | Fragmentación de Contexto Largo | Las operaciones que consumen alta exergía a lo largo del tiempo requieren cristalización de estado intermedia. | `Snapshot` automático si la tarea excede 10 minutos de CPU. | OOM por acumulación de historiales de tokens. | Ratio de snapshots intermedios vs tiempo de ejecución total. |
