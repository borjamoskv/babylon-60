| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| PRM-011 | Ruido Narrativo (Green Theater) | Generación de excusas, explicaciones innecesarias o padding estocástico en lugar de mutar estado. | Output del LLM sin comandos ni cambios en archivos, conteniendo frases de cortesía. | Aumento en el uso de tokens sin incremento proporcional de Exergía (Diffs). | Segundos | Media | Filtro interceptor (Suppress Green Theater); Reintento forzado. |
| PRM-012 | Divergencia Lexical (Semantic Drift) | Evolución divergente de términos taxonómicos entre múltiples agentes del enjambre. | Dos sub-agentes utilizando diferentes identificadores para la misma entidad C5. | Falla de aserción semántica (Mismatching keys en JSON/YAML). | Minutos a Horas | Alta | Consolidación taxonómica forzada vía archivo `SKILL.md` o Diccionario Ontológico. |
| PRM-013 | Alucinación Paramétrica | Invención estocástica de variables, rutas de archivos o entidades de base de datos no existentes. | LLM asume conocimiento previo sin consultar el grafo observable (File System / DB). | Error `FileNotFoundError` o `KeyError` en Worker Asíncrono. | Inmediato | Alta | Destrucción de contexto no verificado (Weaponized Forgetting); Forzar `list_dir` o `grep_search`. |
| PRM-014 | Entropía de Cache (Cache Poisoning) | Persistencia y propagación temporal de hechos invalidados en memorias locales L1 (Redis/Local). | Mutación de estado en SQLite/Ledger sin invalidación explícita del caché de lectura. | Discrepancia entre la consulta `READ_COMMITTED` y el Ledger Criptográfico. | Segundos a Minutos | Crítica | Invalidación atómica de caché por `tenant_id`; Forzar lectura desde Disco. |
| PRM-015 | Ceguera de Permisos (Permission Blindness) | Intento de ejecución C5-REAL asumiendo privilegios no concedidos por el orquestador o SO. | Fallo al intentar escribir fuera del Workspace CORTEX o acceder a recursos MCP no enlazados. | Error `PermissionError` o `AuthException` en la pasarela. | Sub-segundo | Media | Interrupción (SAGA-1); Prompt automático de solicitud de elevación de privilegios al Operador. |
| PRM-016 | Cascadas de Timeout | Propagación destructiva de demoras debido a acoplamiento síncrono encadenado. | Llamada RPC o de red con alta latencia bloqueando múltiples Workers. | Saturación del ThreadPool; Métricas de latencia de cola elevadas. | Segundos | Alta | Aislar componente bloqueante (Circuit Breaker); Forzar timeout de 500ms y fallback. |
| PRM-017 | Fallo de Consenso BFT | Imposibilidad de alcanzar quórum (n >= 2/3) en validación de aserciones críticas. | Bifurcación asimétrica de resultados de evaluación en el Swarm. | Timeout en fase de consenso (`Virgo`); Votos divergentes en el Ledger. | Segundos a Minutos | Crítica | Suspensión de la mutación; Escalado P1 al Operador. |
| PRM-018 | Desincronización de Grafo (DAG Drift) | Alteración no trackeada de dependencias causales en pipelines. | Modificación manual del código puente o inyección de estado fantasma fuera del Ledger. | Hash de estado de repositorio diverge del `cortex/audit/ledger.py`. | Minutos a Horas | Crítica | Auditoría forense obligatoria (Persist-Auditor); `git reset --hard` a último estado C5-REAL conocido. |
| PRM-019 | Inflación Estocástica de Tareas | División excesiva e inútil de objetivos simples en ramas de ejecución paralelas y fútiles. | Agente invocando múltiples `invoke_subagent` sin justificación topológica o exérgica. | Tasa de spawn exponencial; Exhaustion de API Rate Limits. | Minutos | Media | Kill Criteria (Apoptosis); Imposición de cuotas estrictas de sub-agentes por tarea. |
| PRM-020 | Poda Ontológica Indebida (False Positive Purge) | Eliminación algorítmica de entidades críticas asumiendo baja entropía o desuso aparente. | Herramienta de Limpieza (LEA-OMEGA) actuando sobre archivos sagrados o axiomas. | Missing C5-REAL primitives; Fallos catastróficos en arranque de motor. | Minutos a Semanas | Crítica | Aserción estricta de protección criptográfica (LandauerGuard); SAGA-7 Rollback. |
