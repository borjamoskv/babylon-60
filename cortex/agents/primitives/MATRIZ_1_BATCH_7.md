| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| PRM-061 | Sobrescritura Mutua Inadvertida (Lost Update) | Dos transacciones superponiendo cambios sobre la misma clave KV en un Storage Engine no relacional. | Inexistencia de comprobación `Check-and-Set` (CAS). | Pérdida parcial de metadatos o hechos persistidos. | Sub-segundo | Crítica | Optimistic Concurrency Control (OCC) con números de versión atómicos. |
| PRM-062 | Agotamiento Exponencial de Contexto | Un hilo de agente acumulando historiales de JSON masivos sin compresión iterativa. | Interacción continua superando el límite de 128k/1M tokens. | Degradación cognitiva o rechazo de API LLM (`context_length_exceeded`). | Horas | Alta | Compresión Termodinámica (Thermodynamic Compression OMEGA) obligatoria cada N pasos. |
| PRM-063 | Escape de Privilegios Path Traversal | Agente o Input extrayendo datos fuera del entorno restringido manipulando rutas relativas (`../../`). | Sanitización ausente en inputs de tipo ruta para `read_file` o CLI. | Violación de Sandbox C5; Acceso a secretos OS. | Inmediato | P0 | Aserción estricta de `abspath()` anclado obligatoriamente a rutas Workspace CORTEX. |
| PRM-064 | Mutación de Estado en Getter (Side-effect Read) | Función de lectura que accidentalmente cambia un timestamp, contador o caché de forma insegura. | Operación que debía ser Idempotente genera cambios de estado silenciosos. | Comportamiento divergente en validaciones puras. | Inmediato | Alta | Regla funcional absoluta: Getters inmutables (Purity enforcement). |
| PRM-065 | Contagio de Polución Semántica | Ingestión de código fuente malformado como "hechos verdaderos", corrompiendo la memoria del sistema. | Parseo de AST que no filtra código comentado basura o test stubs irrelevantes. | Disminución de la entropía de Shannon en la base de conocimiento (Slop increase). | Días | Media | Filtrado Linter + ExergyGuard previo a la ingestión semántica (Ontology Forge). |
| PRM-066 | Asfixia por Backpressure | Eventos de ingreso más rápidos que la capacidad de procesamiento de la cola SAGA. | Productor saturando al consumidor en colas Redis/Memoria. | Latencias extremas y pérdida de eventos por Queue limit drop. | Minutos | Alta | Implementación de Drop-Policies racionales o Escalado asimétrico Worker. |
| PRM-067 | Falacia de la Autoridad Criptográfica | Confianza ciega en un Hash que fue generado de una fuente ya comprometida. | Verificación matemática correcta de un payload cuyo origen lógico está corrupto. | Falsa sensación de seguridad (Trust Blindspot). | Indeterminado | Crítica | Aserción fuerte de la cadena causal: "Garbage in, valid crypto garbage out". |
| PRM-068 | Anergía Documental | Generación incontrolada de Markdown con prosa extensa que no posee mapeo ejecutable. | Agente elaborando "planes" narrativos sin deltas de código ni reglas estructurales. | Bloat del repositorio sin cambio de estado efectivo (C4-SIM). | Días | Media | Poda forzosa. Obligar ratio de Código/Directiva sobre Prosa > 80%. |
| PRM-069 | Ruptura de Contrato API Externo | Cambio silencioso en el payload de una API MCP o servicio en la nube (Stripe, GitHub) rompiendo integraciones. | Falla de deserialización al no coincidir el JSON recibido con el esquema esperado. | Interrupción de servicio en módulos perimetrales. | Minutos a Horas | Alta | Pasarelas Anti-Corrupción (ACL) y tests de contrato resilientes a cambios externos. |
| PRM-070 | Parálisis por Exhaustión de Pool de Conexiones | Conexiones HTTP o BD mantenidas abiertas sin cerrarse adecuadamente tras su uso. | Fuga de sockets (Socket Leak) acumulando CLOSE_WAIT u obsoletos. | `ConnectionPoolTimeout` o caída de conectividad de red local. | Horas | Alta | Gestión rigurosa mediante ClientSessions compartidas (Async `aiohttp`/`httpx`). |
