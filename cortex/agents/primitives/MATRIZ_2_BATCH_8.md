| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-071 | Límite Duro de Memoria (OOM Inducido) | El Kernel debe forzar su propia Apoptosis si la entropía acumulada satura el Contexto Seguro (Context Rot). | Auto-terminación y volcado de memoria a Disco (Snapshot) al cruzar el umbral del 85% de carga. | Inanición termodinámica del sistema padre por fuga de agente. | Máximo RSS o tokens procesados previo a Crash. |
| INV-072 | Vaciado Activo de Caché L1 | Las fronteras entre contextos de ejecución son nulas si no se aplican vaciados atómicos (Flush) de L1. | Al cambiar el `tenant_id` o el `agent_id`, se ejecuta un wipe total de caches lógicas en RAM. | Mezcla estocástica de variables de un agente en el turno del siguiente. | Llamada a función de limpieza `redis_flush` o `dict_clear` por Tenant. |
| INV-073 | Prioridad de I/O Asíncrono | Las latencias de disco penalizan infinitamente la asertividad. El event loop no puede esperar a discos rotacionales. | Uso exclusivo de `aiosqlite` y drivers asíncronos; toda E/S de red o DB debe portar `await`. | Event Loop bloqueado (>100ms) P0 Exception `OP_HALT_LOOP`. | Medición de Bloqueo Promedio en Event Loop. |
| INV-074 | Apoptosis Selectiva Termodinámica | Datos sin interacciones activas pierden gradiente. Su persistencia consume exergía injustificada. | Ejecución periódica (Cron) de purga en nodos (Facts) sin referenciación en los últimos N días. | Base de datos inflada con historial fósil no computable. | Tamaño de VectorDB mantenido acotado asintóticamente. |
| INV-075 | Condensación de Vectores (Vector Collapse) | Dos vectores espaciales con divergencia mínima ($distancia < 0.02$) representan la misma afirmación causal. | El sistema fusiona aserciones repetitivas en un solo nodo con un puntero múltiple. | Proliferación de miles de embeddings sobre la misma frase exacta. | Ratio de Embeddings Únicos vs Textos Similares. |
| INV-076 | Restricción Estricta de WAL en SQLite | Los candados monolíticos arruinan la concurrencia BFT. El modo WAL (Write-Ahead Logging) es mandato ineludible. | SQLite inicializado forzosamente en modo WAL con PRAGMA rígidos, timeout > 5000ms. | Base de Datos lanzando un "Database is locked" prematuro. | Configuración de pragmas SQLite en el inicio. |
| INV-077 | Invarianza de Tipado Estricto (Strict AST) | Las referencias genéricas (`Any`, `Union` laxo) inyectan una ola de incertidumbre en el compilador y en el enjambre. | Pydantic Models forzados con `Strict=True`. Cero tolerancia al Type Coercion dinámico de Python. | Conversión de un Float a String silenciada. | Cobertura total Pyright Strict, cero directivas `# type: ignore`. |
| INV-078 | Fail-Fast Heurístico (Rechazo Temprano) | Abortar la computación antes de asignar recursos masivos si el filtro L1 (Flash) detecta ruido irresoluble. | Uso de modelos menores para descartar "Slop" o preguntas inútiles antes de enrutar a OPUS o Gemini Pro. | Envio de peticiones frívolas (Green Theater) a un modelo pesado. | Latencia media de tareas descartadas (debe ser muy baja). |
| INV-079 | Aislamiento Causal de Bloques RAM | No existe memoria compartida inter-agentes. Toda sincronización ocurre a través del Ledger persistente. | Los Workers (procesos fork) comunican cambios exclusivamente empujando deltas a la DB o usando paso por valor. | Condición de carrera (Race condition) en diccionario global de Python. | Ausencia de Mutex de aplicación; delegación al Write-Lock de SQLite. |
| INV-080 | Compresión Termodinámica en Reposo | Texto no es conocimiento; es onda. El conocimiento C5 debe ser AST/JSON denso. | Toda prosa se comprime a sus invariantes antes de insertarse en el Fact Store (Thermodynamic Compression OMEGA). | Ingesta de descripciones floridas como si fueran "Facts". | Entropía de Shannon de los textos en la DB superior al umbral mínimo. |
