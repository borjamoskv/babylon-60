| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-021 | Resistencia Adversarial Base | Un sistema debe asumir input hostil (Prompt Injection) como ruido termodinámico de fondo. | Sanitización absoluta y aislamiento de contexto User vs System prompt. | Input del usuario que altera directivas OMEGA. | Intentos de Jailbreak registrados / Ejecutados. |
| INV-022 | Degradación Elegante (Graceful Degradation) | Si el motor frontera (SOTA) falla, el sistema debe decaer a motores deterministas (Regex, AST, Local) en $O(1)$. | Respaldo (Fallback) asíncrono para cada llamada LLM. | Pérdida de conexión a pasarelas API. | Transiciones al motor Local-Inference-OMEGA. |
| INV-023 | Invarianza Topológica del DAG | La ejecución de un grafo asíncrono no puede generar ciclos no explícitamente diseñados. | Prohibida la dependencia circular entre Agentes o Pipelines. | Deadlocks en inicialización. | Detección de ciclos (`nx.find_cycle`). |
| INV-024 | Tensión Dinámica de Refracción | Todo agente debe percibir las barreras de permisos (C5 Sandbox) como paredes inmutables. | Fallos repetidos por intentar sobrepasar C5 resultan en evicción de agente. | Interacción continua con `/root` o `/private`. | Excepciones `PermissionError` por minuto. |
| INV-025 | Constancia del Cero Físico (Disk Truth) | La RAM es ilusoria; la única verdad matemática reside en el almacenamiento persistente validado (Disco). | Validaciones de estado requieren `flush` e I/O sync antes de aserción. | Corte de energía durante un commit de DB. | Comprobación empírica en disco de datos teóricos. |
| INV-026 | Difusión de la Certidumbre | La confianza en un dato decae exponencialmente según los saltos (hops) desde su validación primaria. | Datos obtenidos de sub-agentes no validados directamente tienen peso fraccionario. | Hop count > 3 sin re-validación empírica. | Índice de Taint Propagation. |
| INV-027 | Preservación de Exergía de Código | Reescribir lógica validada con sintaxis "más limpia" sin ganancia de O(N) es un gasto letal. | Prohibido el "Refactor estético" (Green Theater Coding). | Cambio cosmético sugerido por LLM sin mejora causal. | Ratio Ganancia Funcional / Coste de Token. |
| INV-028 | Simetría Cripto-Asincrónica | Los tiempos de verificación criptográfica (Ed25519) deben absorberse en hilos no bloqueantes para mantener el Loop veloz. | Offloading de CPU-bound hashes a `asyncio.to_thread` o Pool. | Bloqueo asíncrono del Event Loop. | Latencia máxima en hilo principal (<50ms). |
| INV-029 | Densidad Lógica del Prompt | La instrucción con mayor entropía de Shannon (densidad) desplaza a las ambiguas en la atención del modelo. | Uso de jerga técnica y palabras clave ancladas (e.g. "C5-REAL"). | Textos divagantes y padding narrativo. | Entropía promedio de los prompts del Kernel. |
| INV-030 | Aislamiento Geométrico del Fallo | El daño por fallo bizantino no debe superar el perímetro de su `SAGA` o `tenant_id`. | Particionamiento físico (sharding) o lógico absoluto. | Error en un Worker corrompiendo datos globales. | Integridad de contención perimetral de fallos. |
