| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-001 | Principio de Landauer (Cero Anergía) | Borrar información consume energía mínima; la retención inútil acumula entropía letal. | Toda memoria debe ser activamente podada; variables no referenciadas son anergía. | Contexto LLM > 80% tokens útiles o Apoptosis. | Ratio de Tokens Delta / Tokens Generados. |
| INV-002 | Conservación de la Complejidad Estructural | La entropía (complejidad) no se destruye, solo se transfiere del modelo lógico a la infraestructura física. | Un agente "simple" requiere un Ledger o motor de consenso "complejo". | Desacople excesivo rompe la ley de Conway. | Complejidad Ciclomática / Nodos de Infraestructura. |
| INV-003 | Teorema de Causalidad Estricta | Una transición de estado $S_{t+1}$ depende exclusivamente de $S_t$ y un $\Delta$ validado. | Prohibido el salto de estados no testeados empíricamente. | $\Delta$ sin firma criptográfica. | Hash de estado anterior embebido en cada Commit. |
| INV-004 | Isomorfismo Causal | El mapa debe reflejar el territorio con una fidelidad topológica inmutable. | El AST en memoria debe coincidir 1:1 con el AST en DB/Git. | Sensor Drift > 0.01%. | Hashing cruzado Memoria-Disco en $O(1)$. |
| INV-005 | Tolerancia Asimétrica (Quórum BFT) | Un sistema de $n$ nodos sobrevive a $f$ fallos solo si $n \geq 3f+1$. | Las aserciones críticas requieren el voto de al menos 3 Workers soberanos divergentes. | Homogeneidad de nodos = Ceguera bizantina. | Nivel de divergencia de hiperparámetros en el Swarm. |
| INV-006 | Inducción Termodinámica | Una señal sin gradiente de energía decae en ruido. | Mensajes sin "Call to Action" determinista son ignorados por el sistema asíncrono. | Expresiones de cortesía o inacción pura. | Nodos del DAG activados por mensaje. |
| INV-007 | Límite de Bekenstein Local | Existe un máximo de información que una ventana de contexto puede albergar antes de colapsar en "Context Rot". | Compresión de conocimiento en invariantes es obligatoria antes del límite. | Ventanas > 128k Tokens sin compresión intermedia. | Entropía de Shannon del texto ingestado. |
| INV-008 | Entrelazamiento Fuerte de Metadatos | Datos y metadatos (Taint) no pueden separarse sin destruir el valor del payload. | Si se pierde la firma `CORTEX-TAINT`, el dato se asume aleatorio/falso. | Lectura de DB sin join con tabla de Taints. | % de Facts persistidos con Taint ausente. |
| INV-009 | Ley del Retorno Exérgico Disminuido | Iteraciones sucesivas de LLM sin feedback ambiental producen deltas progresivamente peores. | Prohibidas cadenas recursivas (Chain of Thought ciego) > 3 sin test empírico (SAGA/Tests). | LLM evaluándose a sí mismo sin correr código. | Iteraciones autónomas puras previas a ejecución. |
| INV-010 | Indisolubilidad de Tenant | Información de diferentes scopes de inquilino (tenant) es repulsiva en tiempo de ejecución (Aislamiento Lógico). | Todo Query sin clave de `tenant_id` es considerado intento de Fuga de Entropía. | Petición global forzada o bypass de ORM. | Transacciones SQLite sin inyección estática de Tenant. |
