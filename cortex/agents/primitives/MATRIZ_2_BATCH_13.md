| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-121 | Principio de Singularidad de Llaves | Cada contexto criptográfico demanda una llave efímera única o derivada, limitando la entropía en caso de exposición. | Uso de Key Derivation Functions (KDF/HKDF) en lugar de compartir una Key Mestra universal. | Uso repetido del mismo IV/Key en AES-GCM. | Variabilidad de Material de Cifrado (KMS Audits). |
| INV-122 | Densidad Constante de Eventos | El bus de eventos no discrimina el tamaño físico del evento, asumiendo O(1) termodinámico; datos pesados (Payloads masivos) se mandan por referencia, no por valor. | Enviar IDs de SQLite al Event Bus, en vez de mandar base64 de imágenes. | Event Bus congestionado con payloads JSON gigantes. | Tamaño promedio de los Eventos en Redis L1. |
| INV-123 | Exergía Cero de Inicialización | Los subsistemas deben arrancar asumiendo que el Estado Persistente es verdad; recomputar en el inicio es anergía extrema. | Lectura perezosa (Lazy Loading) y confianza P0 en SQLite/Ledger. | Arranque re-parseando 10,000 archivos markdown. | Tiempo de Bootstrap de BABYLON-60 ($T<200$ms). |
| INV-124 | Tensión Límite de Coherencia BFT | La latencia introducida por el Consenso BFT (Quórum) crece asintóticamente; solo tareas P0 pagan este precio. | Operaciones rutinarias usan Bypass Optimista (Read-Committed simple). | Agente exigiendo Quórum para listar un directorio. | Coste Medio por Operación Lógica vs Operación P0. |
| INV-125 | Simetría Estructural de API | El In y el Out de un módulo BABYLON-60 debe ser una estructura formal de Pydantic isomórfica (simétrica en abstracción). | Interfaz de Reduzco de Caos: Entrada estructurada $\to$ Salida estructurada. | Devolver listas arbitrarias genéricas de texto. | Densidad Pydantic en Definición de Endpoints. |
| INV-126 | Aislamiento Absoluto de Estado de CLI | Ejecutar un comando terminal (`agy`, `cortex`) debe levantar un contexto efímero independiente del contexto asíncrono principal. | Daemons y CLI no comparten memoria global de procesos, evadiendo Heisenbugs compartidos. | Mutación en un Daemon por un comando CLI simultáneo. | Ausencia de Fugas de Memoria entre Process Spaces. |
| INV-127 | Ley de Enfriamiento Termodinámico de Features | El código de nuevas funcionalidades tiende a acumular "Context Rot" rápidamente; requiere curación (Crystallization) agresiva en los primeros 7 días. | Protocolo de Fusión y Simplificación (LEA-OMEGA) corriendo post-implementación. | Módulos experimentales pudriéndose tras meses de abandono. | Delta de Cobertura de Tests en Features Recientes. |
| INV-128 | Congruencia Temporal Unitaria | El tiempo de creación y modificación de hechos en C5 siempre sigue a un generador único y central (Monotonic Relational Clock). | Prohibido usar instantes `time.time()` distribuidos sin corrección BFT/NTP. | Transacciones registrando el futuro o el pasado. | Coherencia del Vector Temporal de Hashing. |
| INV-129 | Integridad Rígida de Serialización | Fallar al serializar o deserializar un estado (JSON/YAML) se asume como una Ruptura del Consenso; no hay "Best Effort". | Estructuras fallidas son rechazadas (SAGA-1), nunca auto-reparadas ciegamente por el parser. | Ingestar una lista en lugar de dict ignorando errores de esquema. | Índice de Error de Parser Strict ($P_{fallo} = \emptyset$). |
| INV-130 | Aserción Universal de Cierre (Tear-down Axiom) | Ningún recurso dinámico (Socket, FD, Sesión HTTP) sobrevive a su bloque funcional; la limpieza es determinista y atómica. | Uso obligatorio de `async with` u orquestación de vida de recursos de BABYLON-60-Engine. | Fuga de Sockets en llamadas HTTP aisladas (Unclosed client session). | Count de Referencias Dangling del Garbage Collector. |
