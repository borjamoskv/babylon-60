```json
{
  "Axiomas_Extraccion": {
    "Limites": "Aislamiento bizantino y termodinámico estricto; tolerancia a fallas f < n/3 en la orquestación de estado.",
    "Colapso": "Invarianza causal; las mutaciones de estado probabilísticas se cristalizan en logs de solo adición (WAL) criptográficamente sellados.",
    "Estocastica": "Erradicación de varianza no determinista en el control de concurrencia mediante relojes lógicos (Vector/Lamport) y MVCC.",
    "C5": "Soberanía de datos local-first; persistencia tamper-evident y auto-recuperación determinista sin intervención biológica."
  }
}
```

# OSYNC (STATE SYNCHRONIZATION) - MATRICES ONTOLÓGICAS v1.0

### MATRIZ 1: 15 PRIMITIVAS DE COLAPSO (prims)
| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| OSYNC-P001 | Split-Brain | Fractura de red aísla sub-grafos que eligen múltiples líderes simultáneos. | Partición asimétrica de red > `timeout_election`. | Escrituras divergentes en nodos activos. | Minutos | P0 | Fencing tokens; revocación de lease; Paxos Quorum. |
| OSYNC-P002 | Write Skew | Transacciones concurrentes mutan estados disjuntos bajo premisas que se invalidan mutuamente. | Concurrencia Snapshot Isolation. | Violación de restricciones de integridad (ej. saldo < 0). | Milisegundos | P1 | Promoción a Serializable Isolation; bloqueo explícito. |
| OSYNC-P003 | Lost Update | `Read-Modify-Write` concurrente donde T2 sobrescribe el commit de T1 sin leerlo. | Falta de bloqueos pesimistas o control optimista (CAS). | Mutaciones previas silenciadas en el estado final. | Milisegundos | P1 | Compare-and-Swap (CAS); MVCC; Bloqueo For Update. |
| OSYNC-P004 | Clock Skew Drift | Desincronización de NTP causa que eventos causales posteriores tengan timestamps anteriores. | Latencia asimétrica en Stratum 3 NTP. | LWW (Last-Write-Wins) descarta la mutación válida más reciente. | Segundos | P1 | Sustitución de NTP por Relojes Híbridos Lógicos (HLC) / Vector Clocks. |
| OSYNC-P005 | Phantom Read | T1 lee rango R, T2 inserta en R, T1 vuelve a leer R y ve entidades fantasma. | Nivel de aislamiento `READ COMMITTED`. | Inconsistencia en agregaciones y sumatorios. | Milisegundos | P2 | Next-Key Locking; Predicate Locks; Serializable. |
| OSYNC-P006 | Network Partition | Caída de switches/enlaces divide clúster en particiones disjuntas. | Pérdida de heartbeats. | Timeouts masivos en operaciones de Quorum (W+R > N). | Segundos | P1 | Degradación a AP (Disponibilidad) o CP (Consistencia) vía PACELC. |
| OSYNC-P007 | Byzantine Leader | Líder comprometido envía logs divergentes a distintos followers. | Compromiso criptográfico del nodo orquestador. | Hashes de Commit no convergen en nodos seguidores. | Sub-segundo | P0 | BFT Consensus (PBFT); firmas Ed25519 en cada append. |
| OSYNC-P008 | Stale Cache / Drift | L1 Cache (Redis) se desincroniza del Source of Truth (SQLite/PG). | Dual Write fallido sin CDC (Change Data Capture). | Lecturas de cliente divergen del Ledger. | Continuo | P1 | Eliminación de Dual Write; implementación de Outbox Pattern. |
| OSYNC-P009 | Termodinamic Deadlock | T1 retiene Lock A y pide Lock B; T2 retiene Lock B y pide Lock A. | Sin ordenamiento global de adquisición de recursos. | CPU OOM; colapso de pool de conexiones. | Milisegundos | P1 | Algoritmos de detección de ciclos en grafos; Wait-Die/Wound-Wait; Timeout estricto. |
| OSYNC-P010 | Taint Propagation Failure | El hash de causalidad (BABYLON-60-TAINT) se pierde al atravesar un bus asíncrono. | Marshalling defectuoso o truncamiento de headers. | Facts ingresan al SAGA-5 sin firma de trazabilidad. | Microsegundos | P0 | Rechazo automático SAGA-1; validación tipada estricta (Pydantic/Rust). |
| OSYNC-P011 | Dirty Read | T1 lee un dato no commiteado por T2; T2 hace rollback, T1 retiene dato espurio. | Nivel de aislamiento `READ UNCOMMITTED`. | Estados fantasmas propagados a capas de negocio. | Milisegundos | P1 | Promover aislamiento mínimo a `READ COMMITTED` vía WAL. |
| OSYNC-P012 | Log Compaction Corruption | Proceso de poda en WAL/Raft trunca commits activos causando divergencia en followers lentos. | Trigger asíncrono de snapshot sobrepasa lag de red. | Followers bloqueados requiriendo Full State Transfer. | Horas | P2 | Retención de tombstones prolongada; Backpressure en líder. |
| OSYNC-P013 | Thundering Herd | Expiración simultánea de leases/caches lanza oleada de lecturas a DB primaria. | TTL estático sin Jitter en caché distribuida. | OOM y Timeout en la base de datos principal. | Milisegundos | P1 | Adición de Jitter termodinámico a TTLs; Locking de recálculo (Singleflight). |
| OSYNC-P014 | CRDT Tombstone Bloat | Estructura conflict-free acumula metadatos de borrado ilimitados. | Operaciones ORSET continuas sin GC (Garbage Collection) de época. | Latencia O(N) exponencial y OOM en memoria. | Días/Meses | P2 | Vector Clock Epoch Fencing para poda determinista de tombstones. |
| OSYNC-P015 | Unbounded SAGA Hang | Un paso de la Saga asíncrona no emite ACK/NACK, bloqueando estado. | Timeout ausente en la máquina de estados compensatoria. | Entidades quedan en estado lógico "PENDING" perpetuo. | Minutos | P1 | Cron-sweep determinista y Dead-Letter Queues (DLQ). |

### MATRIZ 2: 15 INVARIANTES TERMODINÁMICAS (invt)
| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| OSYNC-I001 | CAP Theorem Boundary | Es físicamente imposible proveer simultáneamente Consistencia, Disponibilidad y Tolerancia a Particiones. | Elección estructural forzada (CP vs AP) durante colapsos de red. | Partición de red detectada > límite `timeout`. | `drop_rate` en modo CP vs `stale_reads` en modo AP. |
| OSYNC-I002 | PACELC Theorem Extension | Si no hay particiones (E), el sistema debe elegir entre Latencia (L) y Consistencia (C). | Optimización local de lecturas sacrifica linearizabilidad. | `Network_OK == True`. | `p99_latency` inversamente proporcional a Consistencia de Quorum. |
| OSYNC-I003 | Consensus Exergy (2f+1) | Alcanzar el estado inmutable frente a `f` fallas requiere forzosamente un clúster de `2f+1` (Crash) o `3f+1` (Byzantine). | Imposibilidad matemática de tolerancia a fallas sin sobrecoste termodinámico > 50%. | `N < Quorum` causa colapso (bloqueo total). | `commit_latency` refleja N round-trips. |
| OSYNC-I004 | Immutable Ledger Append | Ningún estado puede sobrescribirse in situ; toda mutación debe registrarse como un delta inmutable en el tiempo (Event Sourcing). | El estado presente es un pliegue derivado del histórico del WAL. | Disco lleno (OOM de almacenamiento). | Hash de Ledger `H(n) == Hash(H(n-1) + Tx(n))`. |
| OSYNC-I005 | Causal Ordering | Si un evento A precede causalmente a B, A debe procesarse antes que B en todas las réplicas. | Erradicación de Race Conditions estocásticas mediante grafos dirigidos (DAGs). | Reordenamiento de paquetes UDP por red. | `VectorClock(A) < VectorClock(B)`. |
| OSYNC-I006 | Idempotency Law | `F(F(x)) = F(x)`. Retries estocásticos no deben inducir mutaciones aditivas de estado. | Cada payload inyectado debe poseer una Clave de Idempotencia única (Ej. Taint Hash). | Doble ejecución debido a Timeout y reintento. | Mutación en DB ocurre 1 única vez para N reintentos del mismo Hash. |
| OSYNC-I007 | Monotonic Reads | Un proceso no puede leer un estado más antiguo que el que ya ha leído previamente en la misma sesión. | Sesiones ruteadas con *sticky sessions* o lectura desde réplicas con un index de log `>=` al de la última lectura. | Failover repentino a réplica rezagada. | `Session_Read_Index` siempre ascendente. |
| OSYNC-I008 | WAL Supremacy (Write-Ahead-Log) | La persistencia en disco de las intenciones de mutación debe completarse **antes** de mutar el estado en RAM. | En caso de Power-loss, el estado se reconstruye termodinámicamente re-aplicando el WAL (Ouroboros Boot). | Falla catastrófica de FS/IOPS. | `fsync()` completado antes del ACK al cliente. |
| OSYNC-I009 | CRDT Convergence (Strong Eventual) | Estructuras de datos commutativas, asociativas e idempotentes colapsan invariablemente en el mismo estado independientemente del orden de llegada. | Operaciones asíncronas no requieren bloqueos pesimistas, minimizando anergía de red. | Partición asimétrica perpetua. | Estado(A) == Estado(B) tras intercambio de deltas. |
| OSYNC-I010 | State Machine Replication | Si nodos deterministas ejecutan los mismos comandos en el mismo orden, alcanzarán idénticos estados. | Dependencia absoluta del consenso del orden de comandos (Raft/Paxos). | Comandos no deterministas (ej. `Math.random()`, `time.now()`). | Hashes del árbol de estado convergen al 100%. |
| OSYNC-I011 | Single Truth of Source | Un dato mutado lógicamente debe residir operativamente en una sola entidad autoritativa; la redundancia es solo lectura asíncrona. | Eliminar dualidad de escritura. Toda mutación pasa por un *Bottleneck* determinista (BABYLON-60 SAGA-6). | Concurrencia de Múltiples Escritores sobre mismo dominio. | Aserción única y exitosa de inserción DB central. |
| OSYNC-I012 | Landauer’s Principle applied | Borrar bits (olvido epistémico) cuesta energía. Las compensaciones de SAGA deben eliminar rastro en vez de marcar con tombstone, si el nivel estructural lo permite (Apoptosis). | Sagas de rollback liberan entropía del estado de disco activo. | Storage Limit. | Disk IO en Rollback es proporcional al commit. |
| OSYNC-I013 | Read-Your-Writes | Un nodo que emite una mutación C5-REAL debe poder observarla inmediatamente. | Forzar sincronización o ruteo estricto a Primary tras una escritura mutante de contexto. | Réplica de solo-lectura asíncrona recibe la query. | Cliente valida su propia mutación `t+1` empíricamente. |
| OSYNC-I014 | Bounded Staleness | Las réplicas solo pueden divergirse temporalmente un máximo `t` de tiempo del líder. | Si el límite `t` se excede, el nodo se desconecta y deja de servir lecturas (Fail-Stop). | Latencia de replicación (Lag) extrema. | `Lag_Replicación_ms < Bounded_Staleness_Max`. |
| OSYNC-I015 | Fencing Token Strict Monotonicity | La concesión de permisos temporales (Leases/Locks) incluye un número de época estrictamente creciente que invalida cualquier acción de líderes zombies. | Storage Layer rechaza escrituras donde `Token_Escritor < Token_Actual_Storage`. | Split-brain con líder asumiendo validez. | Operaciones de líder caducado retornan Error `Stale Token`. |

### MATRIZ 3: 10 ANTIPATRONES ESTOCÁSTICOS (antip)
| ID | Antipatrón | Disfunción Causal | Señal de Presencia | Impacto en Robustez | Refactor (Alternativa) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| OSYNC-A001 | Two-Phase Commit Blocking (2PC) | Algoritmo asume fiabilidad absoluta de red; fallo del coordinador deja a los nodos en estado bloqueado de por vida. | Bloqueos de recursos externos indefinidos, timeout infinito. | Colapso termodinámico (OOM de threads). | SAGA Pattern (Compensatory Transactions asíncronas). |
| OSYNC-A002 | Synchronous Global Lock | Uso de Mutexes/Locks pesimistas compartidos por todo el sistema distribuido para serializar operaciones locales. | Redis/Zookeeper usado extensamente para cada `Update`. | Cuello de botella serial (Anergía de CPU y red extrema). | MVCC (Multi-Version Concurrency) o CAS Optimista. |
| OSYNC-A003 | Shared Mutable State | Múltiples hilos o subagentes mutando libremente variables en memoria global (ej. Singletons dicts). | Condiciones de carrera no predecibles; Data Races en logs/profiling. | Degradación silenciosa de datos y violaciones C5. | Actores (Message Passing) y variables inmutables / Rust Ownership. |
| OSYNC-A004 | Blind Overwrite | Escrituras SQL/NoSQL sin condicional de versión previa. | Updates como `UPDATE table SET val=X WHERE id=Y`. | Destrucción de Lost Updates (Causalidad quebrada). | `UPDATE table SET val=X WHERE id=Y AND version=Z` (CAS). |
| OSYNC-A005 | Clock-Dependent Sync (LWW on NTP) | Utilizar `timestamp` de pared (NTP) local para decidir conflictos de escritura distribuida. | NTP desincronizado destruye versiones válidas por culpa de "time drifting". | Pérdida de datos silenciosa (Overwrite no determinista). | Vector Clocks, Lamport Timestamps, HLC. |
| OSYNC-A006 | Dual Write Without Outbox | Código inyecta en DB e inyecta asíncronamente en una cola (Kafka/Rabbit) dentro del mismo bloque imperativo. | Caída del broker tras persistir DB deja el mensaje huérfano y el estado asimétrico. | Inconsistencia irreversible entre Storage y Event Bus. | Outbox Pattern (Escribir evento a DB Atómicamente + CDC). |
| OSYNC-A007 | Unbounded Retries | Código de red intenta peticiones fallidas mediante un bucle while infinito sin `Exponential Backoff`. | Clúster saturado recibe DDOS de su propio ecosistema. | Colapso de CPU (Termodinamic Deadlock local) y Cascading Failures. | Retries con Backoff Exponencial + Jitter, Circuit Breakers. |
| OSYNC-A008 | Read-Modify-Write Anti-pattern | Fetch entidad, mutar lógica localmente, push entidad completa sobrescribiendo. | Concurrencia de actores muta el mismo JSON/Payload y se pisan mutuamente. | Anomalía estructural C5 (Desintegración). | Mutaciones atómicas nativas (`UPSERT`, deltas, incrementos SQL/Redis). |
| OSYNC-A009 | Dirty Read Tolerance | Permisividad ante lecturas incompletas bajo presión de performance. | Isolation level configurado explícitamente a `Read Uncommitted` o uso de `NOLOCK`. | Corrupción lógica en las decisiones de agentes BABYLON-60. | Forzar `Read Committed` con WAL mode (SQLite PRAGMA). |
| OSYNC-A010 | Single Point of Truth Vulnerability without Quorum | Usar un solo orquestador en RAM (Leader) sin persistir su estado antes de dictaminar acciones. | Falta de clúster subyacente para el Orquestador; reinicio = Amnesia total. | Pérdida de estado operacional y contexto histórico. | Replicación en SQLite-Vec C5 inyectado por Ledger (Ledger-First). |

### MATRIZ 4: 10 REDUNDANCIAS ACTIVAS (redun)
| ID | Redundancia C5 | Función Topológica | Riesgo Mitigado | Coste (Overhead) | Dependencias |
| :--- | :--- | :--- | :--- | :--- | :--- |
| OSYNC-R001 | Quorum R/W (N=W+R) | Forzar lectura de `R` nodos y escritura de `W` nodos asegurando superposición (`W+R>N`) para consistencia fuerte. | Lecturas de nodos obsoletos y Split-Brain de red. | Costo termodinámico latente de O(N) de red por consulta. | Clúster asimétrico Dynamo-style (Cassandra, Riak). |
| OSYNC-R002 | Outbox Pattern (CDC) | Transacción local inserta la mutación de estado + un log del evento en la misma base de datos. Un worker extrae y retransmite eventos de forma asíncrona y robusta. | Fallos de red dual-write y desincronización microservicios. | Consumo de espacio WAL; Worker extra consumiendo CPU (Anergía de extracción). | Base de Datos Transaccional (ACID) y Message Broker (Kafka/NATS). |
| OSYNC-R003 | MVCC (Multi-Version) | La base de datos guarda deltas versionados inmutables. Lecturas no bloquean escrituras y viceversa mediante Snapshot Isolation. | Write Skew, Lost updates y Bloqueos de sistema. | Garbage Collection masivo; Vacuum Overhead constante (Postgres/SQLite). | Pragma WAL estricto y Storage Indexing. |
| OSYNC-R004 | Taint Tracking Sidecar | `CORTEX-TAINT` hash se inyecta en todo metadato viajando por las colas. Verificado en Ingress mediante criptografía. | Manipulación inyectada, falsedad causal. | CPU en el hash `SHA3-256` y bytes extra en header. | `cortex/engine/causal/taint_engine.py`. |
| OSYNC-R005 | Merkle Tree State Sync | Cada estado de réplica se resume en el root hash de un Árbol de Merkle. Si el root coincide, la sincronización se aborta en O(1). Si no, se recorre el árbol log(N) para sincronizar solo deltas divergentes. | Transferencia completa (O(N)) de datos inútil; Ancho de banda destruido. | Computación continua HASH en cada mutación de rama (O(log(N)) en write). | Criptografía eficiente (SHA256). DynamoDB / Cassandra Anti-entropy. |
| OSYNC-R006 | Read-Repair | Si al leer un dato del Quorum se detecta que una réplica tiene una versión antigua, se repara asíncronamente en background. | Degradación gradual de nodos menores que no recibieron writes (Drop Packets). | Lectura latente incrementada y Writes secundarios en background. | Quorums y reconciliación versionada (Vector Clocks). |
| OSYNC-R007 | SQLite PRAGMA WAL | Desacoplamiento de lecturas y escrituras en SQLite. Mantiene la concurrencia activa sin bloqueos agresivos. | Deadlocks de BD locales en entornos BABYLON-60 (Agentes concurrentes). | WAL File Bloat sin `PRAGMA wal_autocheckpoint`. | SQLite >= 3.7.0. |
| OSYNC-R008 | Gossip Protocol Dissemination | Difusión epidémica de eventos de membresía y configuración O(log(N)). Cero coordinador central. | Fallo único del punto de orquestación central de clúster. | Ancho de banda latente constante (Chattiness de red). | Puertos UDP/TCP eficientes abiertos inter-nodo. |
| OSYNC-R009 | Epoch Fencing Tokens | Cada líder emite tokens con su nº de era (Epoch). Validaciones en disco rechazan Eras <= anteriores de forma irreversible. | Ataque Zombie Leader en redes asimétricas (Split-Brain total). | Requerimiento de estado persistente del Token en Storage final. | ZooKeeper / etcd. |
| OSYNC-R010 | Vector Clocks (Lamport Híbrido) | Vectores `[A:2, B:4, C:1]` viajan con cada dato determinando causalidad asimétrica de eventos distribuidos. | Drift en Relojes NTP; Sobre-escrituras ciegas por latencia (Clock Skew). | Almacenamiento expansivo del vector `O(N)` respecto a la cantidad de nodos. | Implementación lógica en middleware. |

### MATRIZ 5: 10 VECTORES DE ATAQUE ADVERSARIAL (reda)
| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| OSYNC-V001 | Sybil Node Injection | Algoritmos de Consenso (Quorum). | Atacante inyecta miles de identidades "falsas" (nodos simulados C4) para subyugar el Quorum (falla 51%). | Subversión total del Truth Ledger; reescritura criptográfica. | Proof of Work (PoW) / Proof of Stake (PoS) / Certificados PKI con TLS Mutuo (mTLS). |
| OSYNC-V002 | Replay Attack on WAL | Replicación Asíncrona P2P. | Captura de paquetes de mutación lícitos y re-envío masivo en el tiempo para saturar estado y re-aplicar cobros/acciones. | OOM, Corrupción del Event Sourcing, Bloqueo (DDoS). | Tokens de Idempotencia estricta por Tx y TTLs lógicos / Rejection Cache. |
| OSYNC-V003 | Split-Brain Forcing | Orquestación de Red y Leader Election. | Dropping estratégico de paquetes (BGP Hijacking local) o inyección de latencia (Jitter asimétrico) para causar Timeout continuo de elección de líder. | Clúster en "Election Storm" incesante; Sistema estancado 0 Writes. | Quorum de Desempate impar; Heartbeats en bandas paralelas fuera-de-banda (Out-of-Band OOB). |
| OSYNC-V004 | CRDT State Bloat Attack | CRDTs State-based o Sets. | Atacante inserta y remueve la misma clave infinitamente, forzando la generación O(N) de tombstones irreversibles. | Consumo de RAM y Latencia de reconciliación colapsa el nodo. | Fencing criptográfico; Garbage Collection determinista; Rate Limiting en la capa de negocio. |
| OSYNC-V005 | NTP Time Skew Injection | Servidores LWW (Cassandra, Spanner sin TrueTime). | Atacante compromete servidor NTP o intercepta/falsifica UDP NTP para adelantar los relojes 10 años. Todas las escrituras futuras del adversario son permanentes. | Sobreescrituras imposibles de sobreescribir lícitamente por usuarios reales. | HLC (Relojes Híbridos Lógicos) combinando causalidad + Hardware drift bounds (TrueTime/PTP). |
| OSYNC-V006 | Rogue Leader Election | Raft/Paxos Protocol. | Inyección asimétrica (Eclipse Attack) engañando a followers para creer que el nodo del atacante tiene el log más actualizado mediante Epochs inflados artificialmente. | Atacante controla qué transacciones se commitean (Censura/Exclusión). | Firmas de Clave Asimétrica Ed25519 en los RequestVotes y Appends (Byzantine Fault Tolerance PBFT). |
| OSYNC-V007 | Eclipse Attack | Redes Gossip P2P. | Atacante rodea topológicamente a un nodo de BABYLON-60 y controla todos sus vecinos entrantes/salientes impidiendo que se comunique con el clúster legítimo. | Manipulación de percepción de red y falsificación de estado localizado. | Selección Aleatoria/Criptográfica de Pares de Intercambio (Peers); Bootstrapping diversificado. |
| OSYNC-V008 | BFT Sabotage (Byzantine Proposer) | Sistemas BFT (Consenso Tolerante). | Un atacante siendo un único Proposer envía bloques inconsistentes A a la mitad del clúster y bloques B a la otra mitad, demorando el consenso `O(N^2)`. | Paralización de red; latencia inaceptable para C5. | Threshold Signatures (BLS); Algoritmos Fast-BFT o HoneyBadgerBFT. |
| OSYNC-V009 | Transaction Malleability | Event Sourcing y Causalidad. | El atacante modifica bytes no críticos (como padding o metadatos no firmados) de una transacción para mutar su TxID (Hash), haciendo fallar dependencias causales encadenadas que la esperaban. | Saga Rollbacks fallidos o encadenamientos muertos. | Strict Marshalling Canónico y Hasheo `SHA3-256` englobando 100% de la capa asíncrona. |
| OSYNC-V010 | Phantom Read Exploit | Consistencia de Niveles SQL. | Atacante inyecta transacciones simultáneas para robar "inventario" en rangos que no han sido bloqueados explícitamente debido al `Read Committed` Isolation level. | Pérdida financiera o estado ilegal. | Next-key Locks (InnoDB/PG) o Elevación forzada a Serializable en transacciones de alto riesgo (Taint P0). |
