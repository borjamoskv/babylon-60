# MATRIZ 1: PRIMITIVAS DE COLAPSO (041 - 060)

| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
|----|-----------|------------------|----------------------|------------------|-----------------|----------|--------------|
| PRM-041 | Tormenta de Hooks (Hook Storm) | Commits iterativos disparan hooks pesados (ej. pylint + tests E2E) infinitamente. | Modificaciones masivas de código | Bloqueo en fase `pre-commit` | min | Grave | Ejecución de `git commit --no-verify` (Singularidad P0) |
| PRM-042 | Saturación de Pipes (Broken Pipe) | Un proceso orquestador lee de un subagente y el buffer STDOUT colapsa. | Logs crudos no truncados del subagente | `Broken pipe` (SIGPIPE) | ms | Crítica | Flushing asíncrono y Piping estricto a `/dev/null` o LogFile |
| PRM-043 | Fuga de Entropía Temporal | Uso de `datetime.now()` sin timezone causando discordancia cruzada (Time Drift). | Consultas temporales locales | Registros de Ledger solapados/fuera de orden | seg | Grave | Uso exclusivo de Timezones Strict (UTC) o Reloj Lógico |
| PRM-044 | Fatiga de Sesión C5 | Agente pierde el mandato soberano tras ser invocado repetidas veces sin reinicio (Context Decay). | Número de turnos interactivos > 50 | Pérdida de formato C5-REAL (Empieza a usar prosa) | horas | Crítica | Aserción Epistémica / Purga del contexto viejo |
| PRM-045 | Excedente de Workers | Pool de subagentes no limitados (Mitosis Infinita) crea un worker por cada item de un iterador infinito. | Iteradores no acotados en código `cortex` | CPU al 100%, OOM local | ms | Fatal | Control estricto de Paginación y Pool Size |
| PRM-046 | Phantom Dependencies | Módulo python es requerido, pero instalado en el venv equivocado. | Inconsistencia de Paths | `ModuleNotFoundError` tras `pip install` exitoso | seg | Fatal | Obligar al uso de `sys.executable -m pip` |
| PRM-047 | Envenenamiento de Environment | Variables globales sobrescritas por librerías (ej. `os.environ` modificado temporalmente y no restaurado). | Ejecución secuencial de scripts divergentes | Fallos de conexión a DB / Auth | min | Grave | Modificación aislada vía Mock o subproceso |
| PRM-048 | Inconsistencia de Encoding (Mojibake) | Leer archivos usando ASCII o CP1252 por defecto en lugar de UTF-8. | OS no-Mac / Configuración de terminal rota | `UnicodeDecodeError` / Caracteres basura | ms | Grave | Forzar `encoding="utf-8"` en todos los file I/O |
| PRM-049 | Bloqueo por Symlink Circular | El motor de indexado (Watcher) sigue un enlace simbólico que apunta a su propio padre. | Búsquedas de directorios (`list_dir`) | `RecursionError` o consumo infinito de FS | ms | Crítica | Resolución canónica (`os.path.realpath`) y Detección de ciclos |
| PRM-050 | Desfase de Configuración (Ghost Config) | CORTEX lee de `~/.cortex/config` pero la UI modifica `~/.gemini/config`. | Desdoblamiento del Workspace de la IA | El Agente y el Demonio actúan con reglas opuestas | min | Fatal | Singularity Nexus: Symlink absoluto unificador |
| PRM-051 | Fallo de Parsing JSON (Truncado) | La LLM emite un bloque JSON válido pero la cuota de output lo corta a la mitad. | Respuestas masivas > límite max tokens | `json.decoder.JSONDecodeError` | seg | Crítica | Lógica de Continuación forzada (`continue`) o Truncado Parcial de Streaming |
| PRM-052 | Intercepción de Señal SigINT | El usuario presiona Ctrl+C, pero el subagente atrapa la señal y no muere. | Handlers globales atrapando `Exception` | Proceso zombie bloqueando la terminal | ms | Moderada | Manejo específico de `KeyboardInterrupt` |
| PRM-053 | Oscilación de Throttling | Auto-Backoff excesivo que disminuye el rate de request hasta converger a 0 RPS (Parálisis). | Función de backoff errónea | Latencia infinita sin llegar nunca a error | horas | Fatal | Backoff acotado por un umbral máximo |
| PRM-054 | Saturación de Colas (Queue Overflow) | Los eventos entran a `asyncio.Queue` más rápido de lo que los consumers pueden procesar (Productor hiperactivo). | Tráfico pico P2P | Memoria hinchada / Latencia O(N) | min | Crítica | Tamaño de cola fijo `Queue(maxsize=1000)` + Backpressure |
| PRM-055 | Corrupción de Estado de Terminal | CORTEX limpia la terminal (Clear) en medio de la impresión de un log de sistema crucial. | TUI Refresh sobrepuesto con Stdout | Log invisible, usuario a ciegas | ms | Grave | Separación estricta TUI (stderr/pantalla alternativa) vs Logger (File) |
| PRM-056 | Inyección Ciega de Código (Blind Write) | Sobreescribir todo un archivo para cambiar una línea sin validar el resto. | El modelo olvida código intermedio en respuestas truncadas | Archivo de código diezmado (Eliminación masiva) | seg | Fatal | Exigir `multi_replace_file_content` o diff unificado AST |
| PRM-057 | Reversión de Merge Inconsciente | Ejecutar `git checkout` o `git stash` durante un conflicto deshace horas de mutaciones. | Herramientas Git automáticas ante conflictos | Código perdido (Ghosting) | min | Fatal | Guardar backup in RAM pre-git-operation |
| PRM-058 | Pérdida de Enfoque DOM (Click Miss) | El Bot UI de automatización intenta clicar un botón que fue desplazado por CSS/JS asíncrono. | Animaciones UI | Timeout o acción sobre elemento erróneo | ms | Moderada | Clicks anclados al Grafo CDP, nunca XY fijos |
| PRM-059 | Carga Infinita de Componente | El backend CORTEX nunca despacha la señal de render listo al motor de React/Vite. | Promesa colgada | Pantalla de carga permanente | seg | Crítica | Timeout a nivel Front + Skeleton Loaders |
| PRM-060 | Incongruencia de ID (UUID Collision) | Colisión extremadamente rara pero posible en UUIDs truncados o IDs secuenciales P2P. | Generación rápida de micro-identificadores | Sobreescritura en SQLite / Registro fusionado | min | Grave | UUIDv4 forzoso y compuesto |
