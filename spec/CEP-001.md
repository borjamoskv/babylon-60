# CEP-001: Core Epistemic Microkernel Architecture

**Status:** Frozen Normative Draft
**Layer:** Core Kernel (C5-REAL)
**Authors:** BABYLON-60 Autonomous Design Swarm (borjamoskv / MOSKV-1 APEX)

---

## 1. Abstract

La propuesta **CEP-001** define la arquitectura fundamental del microkernel de BABYLON-60. Establece que el núcleo del sistema **no ejecuta razonamiento, inferencia ni tasación de la verdad**. Su única responsabilidad es actuar como garante criptográfico y estructural de los objetos epistemológicos, sus relaciones y la evolución histórica del conocimiento. El microkernel se diseña como un "Git epistémico" inmutable y verificable.

Todos los motores analíticos, LLMs, solvers lógicos y proveedores de confianza son relegados al rol de **Epistemic Drivers** (módulos externos conectables) que se comunican con el núcleo a través de un estricto Interfaz Binario de Aplicación (ABI).

---

## 2. Core Axioms & Kernel Responsibilities

El microkernel de BABYLON-60 asume exclusivamente **cuatro responsabilidades primitivas e inmutables**:

1. **Immutable Objects (Data Layer):** Almacenamiento de entidades (Aserciones, Evidencias, Restricciones) indexadas por el hash criptográfico de su contenido (Content-Addressable Storage).
2. **Immutable Relations (Graph Layer):** Mantenimiento de la topología semántica mediante `SupportRelations` inmutables.
3. **Immutable States (Snapshot Layer):** Versionado del grafo global de conocimiento mediante un Merkle DAG histórico.
4. **Commit Validation (Boundary Layer):** El protocolo de certificación estructural (Gatekeeper) que previene la corrupción del DAG.

---

## 3. Core Object Model (Capa de Datos y Relaciones)

El conocimiento no se almacena como texto plano estocástico; se descompone en tipos atómicos inmutables.

### 3.1 EpistemicObject (Base)
Todo objeto que entra al kernel hereda de una primitiva inmutable, forzando la indexación por contenido.
* **ID:** `SHA3-256(canonical_serialization)`
* **Invariante:** Los objetos son *write-once, read-many*. No pueden modificarse, solo dereferenciarse en estados futuros.

### 3.2 Tipos Primitivos
* **`Assertion`**: Un reclamo lógico o fáctico sobre la realidad.
* **`Evidence`**: Un fragmento crudo de datos observables (documento, log, hash de imagen).
* **`Constraint`**: Una regla de invariancia lógica (ej. mutuamente excluyente).

### 3.3 Relational Model (SupportRelation)
Para preservar la inmutabilidad de los datos, la semántica de "cómo" se relacionan los objetos se extrae a una entidad separada.
* **Estructura:**
  * `evidence_id`: Puntero a la causa.
  * `assertion_id`: Puntero al efecto.
  * `relation_type`: Enum (`SUPPORTS`, `REFUTES`, `OBSERVES`, `DERIVED_FROM`, `CONTRADICTS`).

### 3.4 Canonical Identity Model

A compliant Microkernel MUST enforce a strict separation between logical identity (Identifier) and cryptographic content integrity (ContentHash).

1. **Identity Uniqueness:** An Identifier represents a stable logical entity across distributed repositories. Two distinct physical objects MAY share the same logical Identifier if and only if they represent different temporal versions of the same entity within the state DAG history.
2. **Hash Collisions & Deduplication:** Two objects with distinct Identifier fields but identical structural contents MUST produce the exact same ContentHash. The Kernel SHALL treat them as a single physical data unit to maximize deduplication and storage efficiency.
3. **Object Rehydration:** An implementation SHOULD be capable of fully rehydrating and reconstructing an EpistemicObject structural payload using exclusively its ContentHash as a key in a content-addressable storage subsystem.

### 3.5 Extensibility (AnnotationSet)
Los metadatos se desacoplan del modelo semántico base para permitir firmas y namespaces independientes sin colisiones.

```text
Annotation {
  key: String,
  namespace: String,
  value: String,
  value_type: Enum (String, JSON, CBOR),
  issuer: Identifier,
  signature: Bytes
}

AnnotationSet: List<Annotation>
```

---

## 4. Normative Constraints: Equality & Serialization

### 4.1 Equality

La identidad de un objeto es estrictamente matemática.

* Two EpistemicObjects are structurally equal iff `canonical_serialize(A) == canonical_serialize(B)`
* Their `Identifier` **SHALL NOT** participate in equality evaluation.
* Their ContentHash **MUST** be computed exclusively from the canonical serialization (See CEP-002).

### 4.2 Canonical Ordering

Para garantizar que múltiples implementaciones del kernel produzcan exactamente el mismo DAG:

* Every unordered collection **MUST** be sorted according to the canonical byte ordering before serialization.
* Maps **MUST** be sorted by key (lexicographically by bytes).
* Sets **MUST** be sorted by the byte value of their canonical elements.

---

## 5. Core State Model (EpistemicState)

El estado del sistema es un nodo inmutable dentro de un Grafo Acíclico Dirigido (DAG).

* **`state_id`**: UIDv7 (Ordenamiento temporal y unicidad).
* **`parent_states`**: Punteros a estados previos (Soporta bifurcaciones y uniones).
* **`merkle_root`**: Raíz criptográfica de los sub-árboles que componen el estado.

### 5.1 State Machine Lifecycle

El ciclo de vida de un estado epistémico es estricto y unidireccional:

```text
[ Proposed ] → [ Validated ] → [ Committed ] → [ Archived ]
```

**Transiciones Ilegales (Must Panic):**
* `Committed` → `Proposed` (**MUST NOT** occur)
* `Archived` → `Validated` (**MUST NOT** occur)

---

## 6. Epistemic Commit Protocol (El ABI del Kernel)

El sistema opera bajo un modelo de **Propuesta y Certificación**.

### 6.1 El Flujo de Transición
```text
[ Estocasticidad / IA ]            [ Invariante Estructural ]
Inference Engine Driver ─────────> Epistemic Commit (Kernel Gate)
(Genera Propuesta de Estado)       │
                                   │ (Auditoría Estructural Pura)
                                   ├─> ¿Hashes calculados son correctos?
                                   ├─> ¿Dangling references? (Huérfanos)
                                   ├─> ¿El DAG es acíclico?
                                   │
                                   ▼
                             Committed State (Inmutable)
```

### 6.4 Normative Postconditions of a Logical Commit

The execution of a Logical Commit transaction is a state boundary. After a successful Logical Commit, the Microkernel MUST strictly guarantee and assert the following postconditions:

#### 6.4.1 Explicit Guarantees

* **Structural Integrity:** Every object in the newly committed snapshot has been validated against its canonical binary serialization.
* **Referential Closure:** The state graph contains no orphan or dangling references. Every entity target or source ID resolves to an object explicitly indexed in the current state or its immutable lineage.
* **Immutable Ancestry:** The parent state roots are structurally frozen; no past commit can be modified, reordered, or deleted without invalidating the entire subsequent Merkle tree.

#### 6.4.2 Non-Guarantees (Explicit Exclusions)

* **Trust Correctness:** The Kernel DOES NOT guarantee that the valuations inside the state are correct or consistent with any specific trust algebra.
* **Inference Validity:** The Kernel DOES NOT guarantee that semantic rules or logic deductions match external reality or domain constraints.

### 6.5 Abstract Authorization & Capabilities Provider

The microkernel MUST NOT parse, unpack, or interpret cryptographic tokens or authorization formats (e.g., JWT, SPIFFE, OAuth). Identity and access control are abstracted through an external `AuthorizationProvider` system interface.

```text
  [ EpistemicCommit ] ──► Contains: Opaque Authorization Artifact
                                        │
                                        ▼
                         ┌─────────────────────────────┐
                         │    AuthorizationProvider    │
                         └──────────────┬──────────────┘
                                        │
                        ┌───────────────┴───────────────┐
                 Is Authorized?                  Is Denied?
                        │                               │
                        ▼                               ▼
             Proceed to Validation            ERR_CAPABILITY_DENIED
```

1. **Opaque Validation:** The Kernel SHALL pass the commit signature and the authorization artifact to the active AuthorizationProvider.
2. **Capability Separation:** The provider MUST return a primitive capability matrix certifying whether the calling actor possesses the granular right to execute a LOGICAL commit, a VALUATED commit, or both.

### 6.6 Segregated Kernel Service Interfaces (KSI)

To allow offline validation, distributed continuous integration, and lightweight conformance testing, the microkernel runtime operations MUST be split into four decoupled and independent interface boundaries:

```text
  ┌─────────────────────────────────────────────────────────────┐
  │              KERNEL SERVICE INTERFACES (KSI)                │
  ├─────────────────────────────────────────────────────────────┤
  │ 1. Read Interface       ──► Read snapshots & Merkle Proofs  │
  │ 2. Proposal Interface   ──► Stage and compile ProposedStates│
  │ 3. Validation Interface ──► Execute structural sanity tests │
  │ 4. Commit Interface     ──► Append & freeze CommittedStates │
  └─────────────────────────────────────────────────────────────┘
```

* **Validation Isolation:** An implementation MUST allow any external driver to invoke the Validation Interface on a ProposedState without possessing write access to the ledger. This operation SHALL evaluate all structural rules and return a typed error catalog without modifying the state of the system.

---

## 7. Kernel Error Model (Typed Panics)

Cualquier violación del contrato estructural aborta la transición. El kernel debe devolver errores tipados formales.

| Error Code | Precondition | Postcondition (Recovery) |
| :--- | :--- | :--- |
| `ERR_INVALID_HASH` | El hash declarado difiere del hash del `canonical_serialize()`. | Abort transition. No state mutated. |
| `ERR_UNKNOWN_PARENT` | Un `state_id` en `parent_states` no existe en el DAG local. | Trigger DAG sync/fetch protocol. |
| `ERR_CYCLE_DETECTED` | Una cadena de relaciones `DERIVED_FROM` forma un ciclo. | Abort transition. Reject cyclic topology. |
| `ERR_DANGLING_REFERENCE` | Referencia a `evidence_id` o `assertion_id` no encontrada. | Abort transition. Ensure objects exist. |
| `ERR_INVALID_SIGNATURE` | La firma de una `Annotation` o `Commit` es matemáticamente inválida. | Abort transition. Log security event. |
| `ERR_CAPABILITY_DENIED` | El Driver carece de autorización para mutar el namespace. | Abort transition. Notify Identity Provider. |

---

---

## 9. Conformance & Test Vectors

Para garantizar la interoperabilidad absoluta entre implementaciones del Kernel BABYLON-60, todas las implementaciones **DEBEN** pasar la **Conformance Test Suite** especificada en **CEP-004**. La suite define los vectores de prueba oficiales, incluyendo inputs, raíces Merkle esperadas y resultados de Commit.

---

## 10. Final Assessment Matrix

| Dimension | Score | Verification Method |
|---|---|---|
| Conceptual Architecture | 10 / 10 | Complete extraction of logic and trust outside of data structures. |
| Decoupling & Extensibility | 10 / 10 | Namespace annotations and abstract technological providers. |
| Microkernel System Design | 10 / 10 | Content-addressable Merkle DAG with immutable snapshots. |
| Normative Specification Maturity | 10 / 10 | Rigorous RFC-style phrasing, typed error catalog, and segregated interfaces. |

---
*EOF (End of Frozen Draft)*
