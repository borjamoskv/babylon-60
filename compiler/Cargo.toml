[package]
name = "compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel" }
proof_ir = { path = "../proof_ir" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
