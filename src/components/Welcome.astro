<div class="welcome-container">
	<section class="proof-strip">
		<div class="proof-block">
			<label>STORE</label>
			<h3>Structured facts, not chat residue</h3>
			<p>Capture decisions, incidents, preferences, and memory as typed state.</p>
		</div>
		<div class="proof-block">
			<label>VERIFY</label>
			<h3>Cryptographic lineage</h3>
			<p>Attach a tamper-evident trail to every write instead of trusting a log line.</p>
		</div>
		<div class="proof-block">
			<label>RETRIEVE</label>
			<h3>Hybrid context recall</h3>
			<p>Recover useful history without forcing operators through semantic landfill.</p>
		</div>
		<div class="proof-block">
			<label>EXPORT</label>
			<h3>Evidence you can ship</h3>
			<p>Generate audit artifacts for incident review, governance, and compliance workflows.</p>
		</div>
	</section>

	<section class="story-grid">
		<div class="story-panel">
			<label class="tag-alert">WHERE AGENTS BREAK</label>
			<h2>Context gets stored everywhere except where trust can survive.</h2>
			<p>
				Teams mix prompt state, vector chunks, logs, and admin folklore. When something
				goes wrong, nobody can prove which memory was used, whether it changed, or who
				mutated it.
			</p>
		</div>

		<div class="story-panel accent-electric">
			<label class="tag-success">WHAT CORTEX DOES</label>
			<h2>Insert a deterministic trust layer between agent runtime and memory.</h2>
			<p>
				CORTEX treats generated output as a proposal, then routes writes through guards,
				ledger continuity, tenant boundaries, and verification. You keep the memory
				stack, but gain evidence and governance.
			</p>
		</div>
	</section>

	<section class="capability-section">
		<div class="section-intro">
			<label class="section-label">TRUST-CORE</label>
			<h2>Built for the moments when "we think the agent knew this" is not enough.</h2>
			<p>
				The strongest story in the repo is not abstract AGI language. It is operational:
				store, verify, retrieve, export, and defend the history of a decision under real
				pressure.
			</p>
		</div>

		<div class="capability-grid">
			<div class="feature-item">
				<h4>Ledger + Merkle checkpoints</h4>
				<p>Hash-linked writes and batch integrity proofs for portable verification.</p>
			</div>
			<div class="feature-item">
				<h4>Tenant-aware memory</h4>
				<p>Isolation is a first-class invariant, not a best-effort filter applied later.</p>
			</div>
			<div class="feature-item">
				<h4>CLI, API, and MCP</h4>
				<p>One trust-core exposed to operators, products, and agent tooling.</p>
			</div>
			<div class="feature-item">
				<h4>Audit-ready exports</h4>
				<p>Decision lineage becomes reviewable evidence instead of reconstruction work.</p>
			</div>
		</div>
	</section>

	<section class="deployment-grid">
		<div class="deployment-card">
			<label>NOW</label>
			<h3>Local-first core</h3>
			<p>Best-supported path for teams that want deterministic trust without cloud coupling.</p>
		</div>
		<div class="deployment-card">
			<label>BETA</label>
			<h3>Self-hosted surfaces</h3>
			<p>REST, auth, gateway, and extended runtime tooling are usable but still tightening.</p>
		</div>
		<div class="deployment-card">
			<label>FIT</label>
			<h3>Compliance-heavy flows</h3>
			<p>Strongest product story: postmortems, governance, regulated review, and shared agent memory.</p>
		</div>
	</section>

	<section class="cta-band">
		<div>
			<label class="section-label">NEXT MOVE</label>
			<h2>Start with the narrow claim that CORTEX can defend today.</h2>
			<p>
				Ship it as verifiable memory and decision lineage for agents. Expand the rest only
				after the trust-core, auth, and compliance surfaces are boringly reliable.
			</p>
		</div>
		<div class="cta-actions">
			<a href="https://github.com/borjamoskv/Cortex-Persist" target="_blank" rel="noreferrer">OPEN REPOSITORY</a>
			<a href="/vsa">VIEW VSA EXPLORER</a>
		</div>
	</section>
</div>

<style>
	.welcome-container {
		max-width: 1240px;
		margin: 0 auto;
		padding: 2rem 2rem 5rem;
		display: flex;
		flex-direction: column;
		gap: 4rem;
	}

	.proof-strip {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: 1rem;
		padding-top: 1rem;
	}

	.proof-block,
	.story-panel,
	.feature-item,
	.deployment-card,
	.cta-band {
		background: rgba(18, 22, 28, 0.82);
		border: 1px solid rgba(255, 255, 255, 0.05);
		box-shadow: var(--ctx-shadow);
	}

	.proof-block {
		padding: 1.4rem;
		border-radius: 22px;
	}

	.proof-block label {
		font-family: 'Orbitron', sans-serif;
		font-size: 0.6rem;
		letter-spacing: 0.2rem;
		color: var(--ctx-electric);
		margin-bottom: 1rem;
		display: block;
	}

	.proof-block h3 {
		font-size: 1.18rem;
		line-height: 1.2;
		margin-bottom: 0.9rem;
		font-weight: 800;
		color: #fafafa;
	}

	.proof-block p {
		color: var(--ctx-muted);
		font-size: 0.9rem;
		line-height: 1.6;
		margin: 0;
	}

	.story-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 2rem;
	}

	.story-panel {
		padding: 2rem;
		border-radius: 28px;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.story-panel.accent-electric {
		border-color: rgba(116, 224, 212, 0.18);
		background:
			linear-gradient(180deg, rgba(116, 224, 212, 0.08), rgba(255, 255, 255, 0.02)),
			rgba(15, 19, 24, 0.92);
	}

	.tag-alert {
		color: var(--ctx-copper);
		font-family: 'Orbitron', sans-serif;
		font-size: 0.6rem;
		letter-spacing: 0.14rem;
		font-weight: 700;
	}

	.tag-success {
		color: var(--ctx-electric);
		font-family: 'Orbitron', sans-serif;
		font-size: 0.6rem;
		letter-spacing: 0.14rem;
		font-weight: 700;
	}

	.story-panel h2,
	.section-intro h2,
	.cta-band h2 {
		font-size: clamp(1.8rem, 3vw, 2.55rem);
		line-height: 1.2;
		color: #fafafa;
		margin: 0;
	}

	.story-panel p,
	.section-intro p,
	.cta-band p {
		color: var(--ctx-muted);
		line-height: 1.7;
		margin: 0;
	}

	.capability-section {
		display: grid;
		grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
		gap: 2rem;
		align-items: start;
	}

	.section-label {
		display: inline-block;
		margin-bottom: 0.9rem;
		font-family: 'Orbitron', sans-serif;
		font-size: 0.64rem;
		letter-spacing: 0.18rem;
		color: var(--ctx-electric);
	}

	.capability-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
	}

	.feature-item {
		padding: 1.4rem;
		border-radius: 22px;
		text-align: left;
	}

	.feature-item h4 {
		font-family: 'Outfit', sans-serif;
		font-size: 1.12rem;
		margin: 0 0 0.8rem;
		color: #fafafa;
	}

	.feature-item p {
		font-size: 0.92rem;
		color: var(--ctx-muted);
		line-height: 1.65;
		margin: 0;
	}

	.deployment-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1rem;
	}

	.deployment-card {
		padding: 1.4rem;
		border-radius: 22px;
	}

	.deployment-card label {
		display: block;
		margin-bottom: 0.9rem;
		font-family: 'Orbitron', sans-serif;
		font-size: 0.62rem;
		letter-spacing: 0.16rem;
		color: var(--ctx-copper);
	}

	.deployment-card h3 {
		margin: 0 0 0.8rem;
		font-size: 1.2rem;
		color: #fafafa;
	}

	.deployment-card p {
		margin: 0;
		color: var(--ctx-muted);
		line-height: 1.65;
	}

	.cta-band {
		padding: 2rem;
		border-radius: 30px;
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 2rem;
		border-color: rgba(214, 167, 123, 0.18);
	}

	.cta-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.8rem;
	}

	.cta-actions a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.95rem 1.15rem;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(255, 255, 255, 0.04);
		color: #fafafa;
		font-family: 'Orbitron', sans-serif;
		font-size: 0.7rem;
		letter-spacing: 0.16rem;
		text-decoration: none;
		transition: transform 0.24s ease, border-color 0.24s ease;
	}

	.cta-actions a:hover {
		transform: translateY(-1px);
		border-color: var(--ctx-line-strong);
	}

	@media (max-width: 1080px) {
		.proof-strip,
		.deployment-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.story-grid,
		.capability-section,
		.cta-band {
			grid-template-columns: 1fr;
			display: grid;
		}
	}

	@media (max-width: 720px) {
		.welcome-container {
			padding: 1rem 1rem 4rem;
			gap: 2.5rem;
		}

		.proof-strip,
		.capability-grid,
		.deployment-grid {
			grid-template-columns: 1fr;
		}

		.story-panel,
		.cta-band {
			padding: 1.4rem;
			border-radius: 22px;
		}

		.cta-actions a {
			width: 100%;
		}

		.cta-band {
			gap: 1.25rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.cta-actions a {
			transition: none;
		}
	}
</style>
