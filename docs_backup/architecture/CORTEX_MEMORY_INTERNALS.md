# CORTEX Memory OS — Deep Code Internals
> *Autogenerated AST extraction of all memory subsystems.*

---
## Module: `auramem_sdk.py`

**Description:**
CORTEX v7 — AuraMem Python SDK
Sovereign 130/100 Protocol

Zero-drift Semantic RAM Client. Connects to the AuraMem edge API
to provide O(1) deduplication before facts ever hit the local vector store.

### Class: `AuraMemClient`
> Sovereign SDK for AuraMem.
**Methods:** __init__

---

## Module: `bloom.py`

**Description:**
CORTEX v6+ — Bloom Filter for O(1) Fast-Negative Dedup Check.

Strategy #8: Before computing expensive embeddings and running
the ART Gate, a Bloom filter answers in O(1):
"Does something similar to this fact PROBABLY exist?"

- If NO  → store directly, skip ART Gate (saves ~10ms per store)
- If YES → run full ART Gate pipeline

Uses content hashing with multiple hash functions for low
false positive rates (~1% at 10 bits per element).

### Class: `BloomFilter`
> Space-efficient probabilistic set membership test.
**Methods:** __init__, _hashes, add, might_contain, size_bytes, hash_count, bit_count

---

## Module: `causal.py`

**Description:**
CORTEX v6+ — Causal Memory Graph (do-calculus chains).

Strategy #6: Don't just store WHAT happened — store WHY.
Each engram carries a causal chain enabling:
- Contrafactual reasoning: "What if we reverted this?"
- Zombie decision detection: if the cause no longer exists,
  the decision may be obsolete
- Impact analysis: "What depends on this fact?"

### Class: `CausalLink`
> A directed causal relationship between two engrams.
**Methods:** is_strong

### Class: `CausalGraph`
> Directed graph of causal relationships between engrams.
**Methods:** __init__, add_link, effects_of, causes_of, impact_chain, _process_impact_queue, root_causes, _process_root_queue, find_zombies, node_count, edge_count

---

## Module: `circadian_models.py`

**Description:**
CORTEX v7 — Anamnesis-Ω: Circadian Cycle Models.

This module defines the foundational data structures for the Vigilia/Sueño
(Wake/Sleep) memory cycle, introducing Shannon Entropy TTLs and
Reputation-Weighted Asynchronous Byzantine Fault Tolerance (RWA-BFT).

### Class: `TemporalCache`
> Short-term memory (Vigilia) - High volatility, dynamic TTL.
**Methods:** *None*

### Class: `ConsolidatedMemoryGraph`
> Long-term memory (Sueño) - Validated distributed storage.
**Methods:** *None*

### Class: `AgentNode`
> Network participant identity for RWA-BFT consensus.
**Methods:** *None*

---

## Module: `compression.py`

**Description:**
CORTEX v6+ — Semantic Compression (MDL-based Fact Fusion).

Strategy #5: Compress N similar engrams into a single dense engram.

50 engrams × 200 tokens = 10,000 tokens
→ 1 compressed × 500 tokens = 500 tokens
→ 95% reduction, ~98% semantic retention

Uses Minimum Description Length (MDL) principle: the best
compression is the shortest description that preserves all
actionable information.

### Class: `CompressionResult`
> Result of a semantic compression operation.
**Methods:** savings_percent

### Class: `SemanticCompressor`
> Compresses clusters of similar engrams into dense representations.
**Methods:** __init__, compress, _default_compress, _estimate_tokens

---

## Module: `consolidation.py`

**Description:**
CORTEX v6+ — Silent Engrams & Systems Consolidation.

Strategy 4: Implements Tonegawa's discovery of silent engrams.

When a fact is stored, TWO engrams are created simultaneously:
  - Active engram in L2-hot (hippocampus): immediately searchable.
  - Silent engram in L3 (cortex): invisible to search until matured.

The silent engram matures autonomously over N days without contradiction.
If the active engram decays but the silent one has matured → memory persists.
If both decay → natural death (thermodynamic pruning).

Compute-in-Memory principle: Each engram carries its OWN maturation logic
as embedded methods, not as external daemon processing. The memory IS
the processor — no separation between storage and computation.

### Class: `EngramState`
> Lifecycle state of an engram in the consolidation pipeline.
**Methods:** *None*

### Class: `SilentEngram`
> An engram that exists but is invisible to regular search.
**Methods:** age_days, is_mature, tick, contradict

### Class: `SystemsConsolidator`
> Orchestrates the dual-trace memory consolidation pipeline.
**Methods:** __init__

---

## Module: `crdt.py`

**Description:**
CORTEX v6+ — CRDT Memory Merge (Multi-Agent Eventual Consistency).

Strategy #7: When multiple agents work in parallel (LEGIØN-1),
each accumulates independent memories. CRDTs enable conflict-free
merge without coordination:

- G-Counter for access counts (only grows)
- LWW-Register for content (last-write-wins with timestamp)
- OR-Set for tags (union of all tags)

No framework has this for agent memory. This is the killer feature.

### Class: `GCounter`
> Grow-only counter CRDT. Each agent has its own slot.
**Methods:** increment, value, merge

### Class: `LWWRegister`
> Last-Writer-Wins Register CRDT with timestamp.
**Methods:** update, merge

### Class: `ORSet`
> Observed-Remove Set CRDT. Union semantics — both sides win.
**Methods:** add, remove, elements, merge

### Class: `CRDTEngram`
> An engram with CRDT-based fields for conflict-free multi-agent merge.
**Methods:** merge

---

## Module: `distributed_cache.py`

**Description:**
CORTEX v7.5 — "Hydra-Log" Distributed Cache (Redis Streams + Atomic Lua).

Axiom: Ω₁ (Multi-Scale Causality) × Ω₃ (Byzantine Default)
  - All state transitions must be causally reachable (at-least-once).
  - Volatile notifications are replaced by Persistent Streams for audit reliability.

Hydra-Log Architecture:
  1. The "Shadow Key" (L1 Cache): Holds the context.
  2. The "Trigger Key" (L1 Alarm): Triggers expiration events.
  3. The "Audit Stream" (L1 Persistent Log):
     - Every 'put' logs to the stream.
     - Every 'eviction' logs to the stream via a reliable handoff.
     - Background workers consume via XREADGROUP (Consumer Groups) to ensure
       no audit entry is lost even if nodes crash (Ω₃).
  4. At-Least-Once Delivery: Atomic Lua advances the chain-tip AND logs to
     the stream in a single transaction (where possible) or causal sequence.

### Class: `DistributedSovereignCache`
> Cluster-safe Redis L1 cache with Persistent Audit Streams (v7.5).
**Methods:** __init__

### Public Functions
- `make_fastapi_lifespan()`
---

## Module: `dream.py`

**Description:**
CORTEX v8 — Associative Dream Engine (REM Phase).

The creative counterpart to HippocampalReplay (NREM).
While NREM consolidates and compresses, REM CREATES:

  1. Cluster Detection: Identifies emergent semantic clusters
  2. Synthetic Bridging: Generates "what-if" connections between distant concepts
  3. Emotional Re-weighting: Adjusts valence based on post-interaction feedback

Biological basis:
  - REM sleep produces bizarre, creative associations (dream logic)
  - Memory consolidation during REM focuses on emotional integration
  - Creative insights often emerge from REM-phase neural activity

Together with HippocampalReplay:
  NREM (SCE) → Compress, merge, reinforce → Stability
  REM  (ADE) → Bridge, restructure, reweight → Creativity

Derivation: Ω₅ (Antifragile by Default) + Ω₄ (Aesthetic Integrity)
  → Stress the knowledge graph to discover hidden connections.
    Beautiful bridges emerge from structured randomness.

### Class: `DreamResult`
> Result of a single REM dream cycle.
**Methods:** total_operations

### Class: `SemanticCluster`
> A group of semantically related engrams discovered during dreaming.
**Methods:** *None*

### Class: `SyntheticBridge`
> A creative connection between two distant engram clusters.
**Methods:** *None*

### Class: `AssociativeDreamEngine`
> REM-phase dream engine for creative knowledge restructuring.
**Methods:** __init__, _detect_clusters, _expand_cluster, _build_cluster, _compute_dominant_project, _compute_cluster_avg_similarity, _identify_redundant_engrams, _mark_redundancies, _generate_bridges, _create_bridge_if_eligible, _emotional_reweight, __repr__

### Public Functions
---

## Module: `drift.py`

**Description:**
CORTEX v8 — Drift Monitor (Topological Stability Engine).

Detects catastrophic drift in the L2 vector space before recall degrades
silently. Uses 3 computationally honest proxies instead of theoretical TDA:

1. Spectral Gap   — λ₁/λ₂ of covariance.  Detects cluster collapse.  O(n·d²).
2. Intrinsic Dim  — MLE (Levina-Bickel 2004) with JL projection.     O(n·k·d).
3. Page-Hinkley   — Streaming change-point detection.                 O(1)/update.

Zero new required dependencies — numpy only (already in project).
scipy.spatial.cKDTree is opt-in for intrinsic dimensionality (brute-force fallback).

Stratified anchoring: different stability thresholds per fact_type.

### Class: `DriftSignature`
> Immutable topological snapshot of the vector space at time t.
**Methods:** to_dict, from_dict, save, load

### Class: `PageHinkley`
> O(1) streaming change-point detection (Page-Hinkley test).
**Methods:** update, reset

### Class: `DriftMonitor`
> Monitors topological health of the L2 vector space.
**Methods:** __init__, model_hash, checkpoint, _calculate_health_score, health, load_baseline

### Public Functions
- `stratum_for_type()`
- `spectral_gap()`
- `intrinsic_dimensionality()`
- `centroid_drift()`
- `model_hash_from_name()`
---

## Module: `encoder.py`

**Description:**
CORTEX v5.2 — Async Embedding Encoder.

Wraps the existing LocalEmbedder in asyncio.to_thread() to guarantee
event-loop immunity for CPU-bound ML inference.

The model is loaded lazily on first use and cached for the process lifetime.

### Class: `AsyncEncoder`
> Event-loop-safe async wrapper around LocalEmbedder.
**Methods:** __init__, dimension, model_identity_hash

---

## Module: `engrams.py`

**Description:**
CORTEX v6+ — Thermodynamic Memory Engrams.

Translates biological connectomics and somatic homeostasis into actionable code.
Engrams are functionally stable embeddings wrapped in metabolic energy levels.

### Class: `CortexSemanticEngram`
> Computable Engram representing active, metabolically decaying memory.
**Methods:** access, compute_decay

---

## Module: `episodic.py`

**Description:**
CORTEX v6+ — Temporal Abstraction (Episodic Day Summaries).

Strategy #9: Instead of storing every individual action,
compress an entire day/period into a structured episodic
snapshot that captures the essence.

Biological basis: Hippocampal time cells create temporal
sequences that are compressed during consolidation into
schema-compatible summaries.

### Class: `EpisodicFrame`
> A single bounded moment in time before it becomes a semantic fact.
**Methods:** *None*

### Class: `EpisodicBuffer`
> Holds recent EpisodicFrames before semantic consolidation.
**Methods:** __init__, add_frame, get_context

### Class: `EpisodeSummary`
> A compressed temporal episode (e.g., one day's work).
**Methods:** density

### Class: `TemporalAbstractor`
> Compresses time-bounded engram sets into episodic summaries.
**Methods:** __init__, abstract, _extract_by_type, _heuristic_summary

### Class: `CausalTracer`
> Reconstructs causal DAGs from parent_decision_id chains.
**Methods:** __init__, _build_summary

---

## Module: `evaluator.py`

**Description:**
V8 Governance: Memory Evaluator.

### Public Functions
- `calculate_recall_precision()`
---

## Module: `frequency.py`

**Description:**
CORTEX v6+ — Continuous Memory System (CMS) & BIFT Frequency Bands.

Strategy 1 (CMS): Stratifies memory into frequency layers with different
update cadences, inspired by HOPE/Titans Nested Learning.

Strategy 5 (BIFT): Maps retrieval to oscillatory frequency bands
inspired by neural oscillations (gamma, beta, theta, delta).

Together they create a hierarchical temporal memory where engrams
ascend in frequency if they persist and decay if ephemeral.

### Class: `MemoryFrequency`
> Memory tier frequencies inspired by HOPE/Titans.
**Methods:** *None*

### Class: `FrequencyMetadata`
> Metadata tracking an engram's frequency tier and access count.
**Methods:** *None*

### Class: `ContinuousMemorySystem`
> Manages frequency-based memory stratification.
**Methods:** __init__, classify_tier, evaluate_and_migrate

### Class: `RetrievalBand`
> Neural oscillation-inspired retrieval bands.
**Methods:** *None*

### Class: `BandConfig`
> Configuration for a retrieval frequency band.
**Methods:** *None*

### Class: `BIFTRouter`
> Routes retrieval queries to the appropriate frequency band.
**Methods:** classify_query, get_config

---

## Module: `guardrails.py`

**Description:**
CORTEX v6+ — Session-Level Token Budget Guardrails.

Wraps WorkingMemoryL1 with a hard cap on total tokens consumed
across an entire session (not just the sliding window).

Inspired by InitRunner's guardrails but adapted to CORTEX's
thermodynamic memory architecture.

### Class: `SessionGuardrail`
> Hard cap on total tokens consumed per agent session.
**Methods:** consume, tick_turn, consumed, remaining, utilization, turns, session_duration_seconds, status

---

## Module: `hindsight_bridge.py`

**Description:**
hindsight_bridge — L4 Episodic Memory Bridge to Hindsight.

DECISION: Ω₃ (Byzantine Default) + Ω₅ (Antifragile by Default)
Hindsight validated independently (Virginia Tech + WaPo, arXiv:2512.12818).
MIT license. Auditable. Forkable.

Architecture:
    CORTEX L1-L3 (existing) ←→ HindsightBridge ←→ Hindsight Server/Embedded

    - Retain: Each cortex.store() also pushes to Hindsight for entity extraction
    - Recall: Hindsight recall fuses with CORTEX RRF for superior retrieval
    - Reflect: Hindsight reflect generates mental models unavailable in CORTEX

Dependencies:
    pip install hindsight-client  (client-only, for remote server)
    pip install hindsight-all     (embedded, includes PG + server)

### Class: `HindsightConfig`
> Configuration for the Hindsight bridge.
**Methods:** __init__

### Class: `HindsightBridge`
> Bridge between CORTEX memory and Hindsight agent memory.
**Methods:** __init__, connect, _connect_remote, _connect_embedded, is_available, retain, recall, reflect, close

---

## Module: `hologram.py`

**Description:**
CORTEX V6 - Holographic Memory (Vector 2 of the Singularity).

Maintains a RAM-resident mapping of the Sovereign Vector Store.
Bypasses SQLite and I/O latency for background swarm (Heartbeat) context retrieval
using heavily optimized NumPy matrix operations (O(1) inherent retrieval).

### Class: `HolographicMemory`
> RAM-resident semantic memory map for zero-latency CORTEX retrieval.
**Methods:** __init__, is_ready, _cortex_decay

---

## Module: `homeostasis.py`

**Description:**
CORTEX v6+ — Thermodynamic Homeostasis Engine.

Implements active biological memory management (Forget Gates & Synaptic Pruning).
Integrates with drift monitoring for post-prune topological health assessment.

### Class: `EntropyPruner`
> Scans and regulates the structural entropy of the memory database.
**Methods:** __init__

### Class: `DynamicSynapseUpdate`
> Handles biological learning loops: strengthening existing links instead of copying.
**Methods:** __init__

### Class: `HomeostaticScaler`
> Turrigiano-style multiplicative synaptic scaling.
**Methods:** __init__

---

## Module: `l2_hybrid_search.py`

**Description:**
CORTEX v8 — L2 Hybrid Search Engine (Mem0-Killer).

Implements Reciprocal Rank Fusion (RRF) over the L2 vector database
(SovereignVectorStoreL2 — facts_meta + vec_facts + facts_meta_fts).

Unlike the main hybrid search (cortex.search.hybrid), which operates on the
primary `facts` table via aiosqlite, this engine operates directly on the
L2 sync SQLite connection (sqlite3) that lives inside SovereignVectorStoreL2.

Architecture:
    ┌──────────────────────────────────────────────────┐
    │  L2 Hybrid Search                                │
    │                                                  │
    │  Query ──┬─→ Vector KNN (vec0/sqlite-vec)    ─┐  │
    │          └─→ FTS5 BM25 (facts_meta_fts)     ─┤  │
    │                                              ↓  │
    │                          RRF Fusion (k=60)      │
    │                                              ↓  │
    │                         Ranked L2 Results        │
    └──────────────────────────────────────────────────┘

Key design decisions:
  - RRF_K=60 — same constant as main hybrid for consistency
  - FTS5 mirror table (`facts_meta_fts`) is created lazily on first
    call to `ensure_fts_table()` — zero migration friction
  - UUID Trick: results carry a sequential `rank_index` (0,1,2...) for
    clean LLM context injection without exposing internal UUIDs
  - Thread safety: sync lock inherited from parent store's asyncio.Lock

Derivation: Ω₁ (Multi-Scale Causality) + Ω₂ (Entropic Asymmetry)
DECISION: Operate on L2's sync conn to avoid aiosqlite overhead for
         hot-path recall queries. asyncio.to_thread wraps at call site.

### Class: `L2SearchResult`
> A single L2 hybrid search result.
**Methods:** to_context_dict

### Class: `L2HybridSearch`
> Hybrid Search engine operating on SovereignVectorStoreL2's sync connection.
**Methods:** __init__, ensure_fts_table, _vector_search, _fts_search, _rrf_fuse, _hydrate, search_sync

### Public Functions
---

## Module: `ledger.py`

**Description:**
CORTEX v5.3 — L3 Event Ledger (Immutable Event Sourcing).

Every interaction, thought, and state change is recorded as an immutable
event in SQLite WAL via `cortex.db`. If L1 or L2 collapse, the full
cognitive state can be reconstructed by replaying L3 events.

Uses the hardened `cortex.db.connect_async()` factory for zero-lock I/O.

### Class: `EventLedgerL3`
> Immutable event sourcing ledger for cognitive memory.
**Methods:** __init__

### Public Functions
---

## Module: `manager.py`

**Description:**
CORTEX v5.3 — Cognitive Memory Orchestrator.

### Class: `CortexMemoryManager`
> Orchestrator for the Tripartite Cognitive Memory Architecture.
**Methods:** __init__, _start_bg_workers, _check_deduplication, _determine_layer, get_context_vector, l1, l3, _cancel_background_tasks, __repr__

---

## Module: `memory_archaeology.py`

**Description:**
CORTEX v7 — Anamnesis-Ω Semantic Deduplicator (Memory Archaeology).

Finds isolated, isomorphic facts across the timeline and condenses them
into unified "crystallized" patterns. Transfers causal links (parent_decision_id).

### Class: `MemoryArchaeologist`
> Consolidates disjoint semantic facts into a single dense engram.
**Methods:** __init__, _fetch_active_facts, _extract_vectors, _build_clusters

---

## Module: `memory_compression.py`

**Description:**
memory_compression — Background L1 Overflow Compression Pipeline.

Extracted from CortexMemoryManager to satisfy the Landauer LOC barrier (≤500).
Handles the 'Sleep-time Compute' pattern: overflowed L1 events are compressed
(LLM or raw) and persisted to L2 in a bounded background asyncio task.

### Public Functions
- `raw_concat()`
---

## Module: `memory_retrieval.py`

**Description:**
memory_retrieval — L2 Episodic Retrieval with Reciprocal Rank Fusion.

Extracted from CortexMemoryManager to satisfy the Landauer LOC barrier (≤500).
Pure retrieval logic: HDC + Dense recall + RRF fusion.
No state mutations. Always returns serializable dicts.

### Class: `KnowledgeGapException`
> Raised when Metamemory FOK evaluates retrieval potential as too low to proceed.
**Methods:** *None*

### Public Functions
- `fact_to_dict()`
- `apply_rrf()`
- `apply_retrieval_schemas()`
---

## Module: `metamemory.py`

**Description:**
CORTEX v7+ — Metamemory: The Agent That Knows What It Knows.

Nelson & Narens (1990) framework: FOK, JOL, calibration, TOT detection.
Schema layer (Verdict, MemoryCard, etc.) lives in metamemory_schema.py.

### Class: `MetaJudgment`
> Frozen metacognitive assessment of a knowledge state.
**Methods:** *None*

### Class: `RetrievalOutcome`
> Ground-truth record of a retrieval attempt for calibration tracking.
**Methods:** *None*

### Class: `MetamemoryMonitor`
> Continuous metacognitive introspection engine (pure in-memory, no I/O).
**Methods:** __init__, judge_fok, fok_recommendation, judge_procedural_fok, judge_jol, record_outcome, calibration_score, introspect, knowledge_gaps, calibration_report, __repr__

### Public Functions
---

## Module: `metamemory_schema.py`

**Description:**
CORTEX — Metamemory Schema Layer: Models, Index, and Judge.

Extracted from metamemory.py for module splitting (Ω₂: LOC ≤ 500).

Components:
  - Verdict / FOKDirective: Enum-based routing decisions
  - MemoryCard: Frozen metacognitive snapshot of a single memory
  - MetamemoryStats: Aggregate health metrics
  - MetamemoryIndex: O(1) in-memory registry of MemoryCards
  - MetacognitiveJudge: Maps retrieval results → epistemic verdicts
  - detect_repair_needed / build_memory_card: Factory utilities

### Class: `Verdict`
> Metacognitive decision emitted by the judge.
**Methods:** *None*

### Class: `FOKDirective`
> Pre-retrieval routing directive based on Feeling of Knowing.
**Methods:** *None*

### Class: `MemoryCard`
> Metacognitive snapshot of a single memory's epistemic state.
**Methods:** *None*

### Class: `MetamemoryStats`
> Aggregate metacognitive health summary.
**Methods:** *None*

### Class: `MetamemoryIndex`
> O(1) in-memory registry of MemoryCards.
**Methods:** __init__, register, register_batch, remove, introspect, introspect_batch, query_weak_memories, needs_repair, summary_stats, size, __len__, __contains__, __repr__

### Class: `MetacognitiveJudge`
> Decision engine: maps retrieval results to epistemic verdicts.
**Methods:** __init__, judge, judge_with_rationale

### Public Functions
- `detect_repair_needed()`
- `build_memory_card()`
---

## Module: `models.py`

**Description:**
CORTEX v5.3 — Cognitive Memory Domain Models.

Tripartite Memory Architecture (KETER-∞ Frontera 2):
- MemoryEntry: Legacy L2 vector store payload (Qdrant-compatible).
- MemoryEvent: Pydantic v2 model for L1/L3 interaction events.
- EpisodicSnapshot: Pydantic v2 model for L2 compressed episodes.
- CortexFactModel: Zero-Trust L2 Vector Fact for CORTEX v6.

### Class: `CausalEpisode`
> A DAG of causally-linked facts forming a coherent temporal episode.
**Methods:** entropy_density

### Class: `MemoryEntry`
> Atomic unit of vector-stored memory.
**Methods:** to_payload

### Class: `SourceMetadata`
> Tracks origin of the knowledge for Source Monitoring.
**Methods:** *None*

### Class: `MemoryAccessStats`
> Metamemory statistics tracking retrieval and encoding health.
**Methods:** *None*

### Class: `MemoryEvent`
> Atomic unit of cognitive memory — an immutable interaction record.
**Methods:** *None*

### Class: `EpisodicSnapshot`
> Compressed memory episode stored in L2 (Vector Store).
**Methods:** *None*

### Class: `CortexFactModel`
> Zero-Trust L2 Vector Fact for SQLite-vec in CORTEX v6.
**Methods:** age_days, update_on_read

### Public Functions
- `now_iso()`
---

## Module: `navigation.py`

### Class: `AnchorEmbedding`
**Methods:** activate, age_hours

### Class: `NavigationStep`
**Methods:** *None*

### Class: `NavigationResult`
**Methods:** confidence

### Class: `TopologicalIndex`
**Methods:** __init__, add_anchor, remove_anchor, get_anchor, get_neighbors, find_closest_anchor, strengthen_edge, anchor_count, edge_count

### Class: `SemanticNavigator`
**Methods:** __init__, navigate, _astar, _reconstruct_path

### Public Functions
---

## Module: `navigator.py`

**Description:**
CORTEX v7+ — Semantic Navigator (Grid Cell Architecture).

Strategy #13: Navigate knowledge topology instead of just searching.
Inspired by hippocampal place cells and grid cells (Nobel 2014, O'Keefe/Moser).

Operations:
  jump_to(query)          → Direct semantic teleportation
  drift(direction, steps) → Continuous movement through embedding space
  explore_cluster(seed)   → Map the semantic neighborhood
  find_path(src, tgt)     → Conceptual route between two ideas

KnowledgeMap:
  get_dense_regions()  → Where do I know a lot?
  get_sparse_regions() → Where are my blind spots?
  get_bridges()        → What connects separate clusters?
  get_islands()        → What is completely isolated?

Derivation: Ω₁ (Multi-Scale Causality) + Ω₄ (Aesthetic Integrity)

### Class: `NavigationState`
> Snapshot of the navigator's current position in semantic space.
**Methods:** is_dense, is_sparse

### Class: `ClusterInfo`
> Metadata about a semantic cluster.
**Methods:** label

### Class: `SemanticBridge`
> A connection between two otherwise separate clusters.
**Methods:** *None*

### Class: `SemanticPath`
> A route through semantic space from source to target.
**Methods:** hop_count

### Class: `SearchAdapter`
> Protocol for vector search backends.
**Methods:** *None*

### Class: `SemanticNavigator`
> Navigate CORTEX's knowledge topology.
**Methods:** __init__, position, trail

### Class: `KnowledgeMap`
> Topological self-awareness of the knowledge space.
**Methods:** __init__, __repr__

### Public Functions
---

## Module: `nrem_cycle.py`

**Description:**
CORTEX v8+ — NREM Consolidation Cycle.

Unified orchestrator for the "sleep" sweep that transforms volatile
short-term memory into stable long-term knowledge.

Phases (modeled after NREM slow-wave sleep):
  1. Maturation  — Silent engrams that survived → promote to stable
  2. Pruning     — Depleted engrams (ATP < threshold) → destroy
  3. Synaptic decay — STDP edges decay globally (use-it-or-lose-it)
  4. Homeostatic scaling — Turrigiano normalization prevents runaway LTP/LTD

Each phase is independently skippable. The cycle returns a frozen
NREMReport with stats from every phase.

Derivation: Ω₂ (Entropic Asymmetry) + Ω₅ (Antifragile by Default)

### Class: `NREMReport`
> Immutable report from a single NREM consolidation cycle.
**Methods:** total_mutations

### Class: `NREMConsolidationCycle`
> Orchestrates the full NREM consolidation sweep.
**Methods:** __init__

---

## Module: `pii_sanitizer.py`

**Description:**
CORTEX v8 — Sovereign PII Sanitizer.

Detects Personally Identifiable Information (PII) in raw content before
it is vectorized and stored in L2. Applies selective field-level encryption
so the vector captures semantic *shape* ("there is an address here") but
the actual value is only recoverable with the tenant key.

Derivation: Ω₃ (Byzantine Default) + Ω₂ (Entropic Asymmetry)
DECISION: Encrypt, not redact — semantic vectors must remain useful.

Pattern categories detected:
  - EMAIL          → user@domain.com
  - PHONE          → +34 600 123 456 / 555-1234
  - ADDRESS        → calle/street/av/rue + number pattern
  - CREDIT_CARD    → 16-digit card numbers
  - DNI/NIF        → Spanish national ID
  - IBAN           → bank account numbers
  - IP_ADDRESS     → IPv4 / IPv6

Usage:
    sanitizer = PIISanitizer()
    clean, report = sanitizer.sanitize(raw_content, tenant_id="borja")

### Class: `PIICategory`
**Methods:** *None*

### Class: `PIIMatch`
> A detected PII fragment.
**Methods:** *None*

### Class: `SanitizationReport`
> Result of a sanitization pass.
**Methods:** has_pii, pii_categories

### Class: `PIISanitizer`
> Sovereign PII detection and field-level encryption layer.
**Methods:** __init__, detect, sanitize, _try_encrypt

### Public Functions
- `get_pii_sanitizer()`
---

## Module: `pipeline.py`

### Class: `QueryResult`
**Methods:** safe_to_respond, should_search_more

### Class: `StoreResult`
**Methods:** *None*

### Class: `NeuromorphicPipeline`
**Methods:** __init__, assess_query, record_retrieval_outcome, process_store, metamemory, stdp, calibration_score, __repr__

---

## Module: `predictive.py`

**Description:**
CORTEX v6+ — Predictive Memory & Anticipatory Caching.

Strategies #2 + #10: The brain doesn't just store — it PREDICTS.

Predictive Memory (#2):
  Co-access graph: "if you accessed A, you'll likely need B next"
  Pre-loads into L1 before explicit request.

Anticipatory Caching (#10):
  Full compute-in-memory loop:
  Prediction → Prefetch → Self-Process → Auto-Prune → Feedback

This is the CPU-inherent memory concept: no separation between
storage and computation. Memory anticipates, pre-positions,
self-processes, and self-destructs.

### Class: `PrefetchResult`
> Result of a predictive prefetch operation.
**Methods:** *None*

### Class: `CoAccessGraph`
> Tracks which engrams are accessed together.
**Methods:** __init__, record_access, predict_next, decay_all, edge_count, node_count

### Class: `AnticipatoryCache`
> Full CPU-inherent memory loop.
**Methods:** __init__, on_access, get_cached, evict, hit_rate, cache_size, status

---

## Module: `procedural.py`

**Description:**
CORTEX v7+ — Procedural Memory & Skill Buffer.

Basal Ganglia analogue: caches recently executed skills, tracking
execution outcomes (striatal valuation) and latencies. Shifts
skill selection from pure semantic matching to reinforcement-based
historical reliability.

### Class: `ProceduralEngram`
> Frozen representation of a skill's reinforcement history.
**Methods:** __post_init__, striatal_value

### Class: `ProceduralMemory`
> Skill Buffer with reinforcement tracking.
**Methods:** __init__, _get_connection, _load_from_db, _save_to_db, get_engram, record_execution, top_skills

---

## Module: `reconsolidation.py`

**Description:**
CORTEX v8.0 — Memory Reconsolidation v2 (Nader 2000 + Sprint 2 upgrades).

Sprint 2 of the 130/100 plan.

v1 had the core labilization logic but was missing:
  1. Version tracking — no audit trail of reconsolidation events
  2. Confirmation bias detection — LLM could silently rewrite its own history
  3. Tests (zero coverage)
  4. Dream cycle integration hook

v2 adds:
  - ReconsolidationEvent: immutable audit record of every state transition
  - LabilizationRecord.version_id + parent_version: full provenance chain
  - ReconsolidationTracker.audit_trail: O(1) lookup of all events for an engram
  - ConfirmationBiasDetector: flags when confirm/contradict ratio is suspicious
  - dream_sweep() hook: for integration with the dream cycle

Axiom derivation:
  Ω₃ (Byzantine Default): Every state change must be traceable.
  Ω₅ (Antifragile by Default): Track errors to build antibodies, not to hide them.

### Class: `ReconsolidationOutcome`
> Outcome of a labilization window resolution.
**Methods:** *None*

### Class: `ReconsolidationEvent`
> Immutable audit record of a single reconsolidation state transition.
**Methods:** *None*

### Class: `LabilizationRecord`
> Tracks the labile state of an accessed engram (v2).
**Methods:** is_expired, is_labile, age_seconds

### Class: `ConfirmationBiasDetector`
> Detects pathological reconsolidation patterns.
**Methods:** __init__, record, bias_score, is_biased, biased_engrams, report

### Class: `ReconsolidationTracker`
> Tracks labile engrams and resolves their fate (v2).
**Methods:** __init__, on_access, confirm, contradict, sweep, dream_sweep, audit_trail, all_audit_events, confirmation_bias_report, biased_engrams, labile_count, labile_ids, total_events, _make_event, _record_audit, __repr__

---

## Module: `replay.py`

**Description:**
CORTEX v6+ — Hippocampal Replay (Nocturnal Consolidation).

Strategy #1: During "sleep" cycles (daemon idle periods), replay
the day's HOT engrams against the existing semantic base.

- Resonating engrams → merge/compress
- Novel engrams → reinforce (energy +0.5)
- Contradictory engrams → generate ConflictEngram for user review

Biological basis: Sharp Wave Ripples (SWR) during NREM sleep
compress temporal sequences into burst-mode replay at 5-20x speed.

### Class: `ReplayResult`
> Result of a single replay cycle.
**Methods:** total_processed

### Class: `HippocampalReplay`
> Nocturnal consolidation engine.
**Methods:** __init__, _content_contradicts

---

## Module: `resonance.py`

**Description:**
CORTEX v6+ — Adaptive Resonance Gate (ART-inspired).

Implements Grossberg's Adaptive Resonance Theory as a pre-store filter.
Instead of blindly appending facts, the gate checks for semantic resonance
with existing engrams. If resonance is found (similarity > ρ), LTP is applied.
If not, a new engram is created.

Strategy 2: Eliminates semantic duplication at the source.

### Class: `AdaptiveResonanceGate`
> ART-v2 inspired gate that controls memory write operations.
**Methods:** __init__, _calculate_topographic_boost, _calculate_endocrine_shift, _evaluate_resonance

### Public Functions
- `cosine_similarity()`
---

## Module: `schemas.py`

**Description:**
CORTEX v6 — Schema Theory (Top-Down Memory Processing).

Based on Bartlett (1932): Memory is guided by pre-existing schemas.
Schemas act as attentional filters (encoding) and retrieval guides,
preventing the indiscriminate logging of raw text and increasing
relevance during fetch.

### Class: `MemorySchema`
> A cognitive schema representing a typical situation or pattern.
**Methods:** *None*

### Class: `SchemaEngineConfig`
**Methods:** *None*

### Class: `SchemaEngine`
> Applies Schema Theory top-down processing to memory operations.
**Methods:** __init__, _register_default_schemas, match_schema, apply_encoding_schema, apply_retrieval_schema, add_schema

---

## Module: `secure_cache.py`

### Class: `CortexSecureMemoryCache`
> CORTEX v6.0 — Sovereign Memory Cache with Evidence Chain (Ω₂).
**Methods:** __init__, get, put, _secure_eviction, _persist_to_storage, prove_forgetting, verify_forgetting_proof

---

## Module: `semantic_ram.py`

**Description:**
CORTEX v7 — Semantic RAM & Topological Mutator (130/100 Sovereign Standard).

Zero-Copy Infinite Minds architecture pillar.
Implements Read-as-Rewrite: as vectors are co-activated and proved useful,
a background ThreadPool Mutator updates their topological position (effective vector)
using Numpy (GIL-released) towards the query vector.
This creates Semantic Gravity without I/O latency on the main thread.

### Class: `SemanticFactPayload`
> Payload representing a fact transiting through working towards semantic memory.
**Methods:** *None*

### Class: `SemanticMutator`
> Non-blocking mutator that applies Topological Shifts (Read-as-Rewrite).
**Methods:** __init__, start, emit_pulse, _mutate_batch

### Class: `AutonomicMemoryBuffer`
> High-frequency temporary storage for active facts (Short-Term Memory).
**Methods:** __init__, add, flush

### Class: `DynamicSemanticSpace`
> Wraps the SovereignVectorStoreL2 to provide Read-as-Rewrite capabilities.
**Methods:** __init__, start

---

## Module: `sleep.py`

**Description:**
CORTEX v7+ — Sleep Orchestrator (Unified Consolidation Pipeline).

Connects NREM (HippocampalReplay) + REM (AssociativeDreamEngine) into a
single sleep cycle with adaptive calibration feedback.

Architecture:
  ┌──────────────────────────────────────────────────────────┐
  │                    SLEEP ORCHESTRATOR                    │
  ├──────────────────────────────────────────────────────────┤
  │  1. Pre-sleep diagnostics  → read MetamemoryMonitor      │
  │  2. NREM Phase             → HippocampalReplay.replay_cycle│
  │  3. REM Phase              → AssociativeDreamEngine.dream │
  │  4. Calibration update     → adjust FOK thresholds (Brier)│
  │  5. Post-sleep report      → emit to Nexus / log         │
  └──────────────────────────────────────────────────────────┘

Biological basis:
  Sleep architecture: NREM (slow-wave, engram compression) → REM
  (theta oscillations, creative association) → brief wake → repeat.
  4–6 cycles per night. CORTEX runs 1 cycle per heartbeat idle window.

Derivation: Ω₅ (Antifragile by Default) + Ω₁ (Multi-Scale Causality)

### Class: `SleepCycleReport`
> Composite report of a full NREM + REM sleep cycle.
**Methods:** total_duration_ms, calibration_improved

### Class: `SleepOrchestrator`
> Unified NREM + REM sleep cycle orchestrator.
**Methods:** __init__, _adaptive_threshold_update, __repr__

---

## Module: `sparse.py`

**Description:**
CORTEX v6+ — Sparse Encoding (Mushroom Body-inspired).

Strategy 3: Implements dimensionality expansion and sparse activation
inspired by the Drosophila Mushroom Body circuit.

The biological circuit:
  50 Projection Neurons → 2000 Kenyon Cells (40x expansion)
  APL neuron provides global inhibition → only ~5% KC active per odor

Computational translation:
  768-dim embedding → 3072-dim sparse representation
  Global inhibition via top-k selection → only top 5% survive
  Result: near-zero interference between engram memories.

### Class: `MushroomBodyEncoder`
> Sparse encoder inspired by Drosophila Mushroom Body.
**Methods:** __init__, _init_projection, encode, compute_sparsity_ratio

---

## Module: `sqlite_vec_store.py`

**Description:**
CORTEX v6 — Sovereign Vector Store (SQLite-Vec).

Zero-Trust, Multi-Tenant Semantic Memory backed by sqlite-vec.
Enforces partition by tenant_id and incorporates OUROBOROS success_rate
and temporal decay directly in the embedding retrieval.

### Class: `SovereignVectorStoreL2`
> Async vector store for CORTEX v6 L2 semantic memory.
**Methods:** __init__, _get_conn, hybrid_search, _get_sanitizer, _get_domain_tables

### Public Functions
- `cortex_decay()`
---

## Module: `stdp.py`

**Description:**
CORTEX v8 — Spike-Timing Dependent Plasticity (STDP).

Hebbian learning rule adapted for semantic graphs:
  "Neurons that fire together, wire together — but TIMING matters."

STDP modulates edge weights based on temporal co-activation ordering:
  - If A precedes B (causal) → LTP (strengthen connection)
  - If B precedes A (anti-causal) → LTD (weaken connection)

The weight change follows exponential decay within a time window,
producing temporally-sensitive co-activation patterns instead of
flat frequency counts.

Biological basis:
  - Pre → Post spike within 0-100ms → LTP (Long-Term Potentiation)
  - Post → Pre spike within 0-50ms → LTD (Long-Term Depression)
  - Beyond these windows → no change

Derivation: Ω₅ (Antifragile by Default) + Ω₁ (Multi-Scale Causality)
  → Edges evolve through temporal stress. Wrong-order patterns self-prune.

### Class: `SynapticEdge`
> A weighted, temporally-aware edge between two nodes.
**Methods:** plasticity_ratio

### Class: `STDPEngine`
> Spike-Timing Dependent Plasticity engine for semantic graphs.
**Methods:** __init__, record_activation, _apply_ltp, _apply_ltd, get_edge_weight, get_edge, strongest_successors, decay_all, edge_count, node_count, status, __repr__

---

## Module: `temporal.py`

**Description:**
CORTEX v5.0 — Temporal Fact Management.

Handles versioned facts with valid_from/valid_until semantics.
Never deletes — only deprecates. Enables time-travel queries.

### Public Functions
- `now_iso()`
- `is_valid_at()`
- `build_temporal_filter_params()`
- `time_travel_filter()`
---

## Module: `temporal_health.py`

**Description:**
CORTEX v8 — Temporal Health Scheduler (Sovereign Temporal Hierarchy).

The Axiom: La jerarquía temporal ES la arquitectura.

Running all probes at the same cadence is an architectural error:
  - HIGH-FREQ probes run at the wrong rate → false positives + CPU waste
  - LOW-FREQ probes run too often → O(n·d²) per write → degradación silenciosa

This module implements a multi-frequency scheduler that wraps DriftMonitor
and gates each probe behind a write-count or time-based trigger:

    ┌──────────────┬─────────────┬────────────────┬───────────────────────────┐
    │ Tier         │ Probe       │ Trigger         │ Complexity / call         │
    ├──────────────┼─────────────┼─────────────────┼──────────────────────────│
    │ PULSE        │ centroid    │ Every write     │ O(d) — running mean       │
    │ HEARTBEAT    │ page_hinkley│ Every 10 writes │ O(1) — streaming          │
    │ DIAGNOSTIC   │ spectral_gap│ Every 100 writes│ O(n·d²) — batch           │
    │ DEEP_SCAN    │ intrinsic_dim│ Every 500 writes│ O(n·k·d) — expensive     │
    └──────────────┴─────────────┴─────────────────┴──────────────────────────┘

The running-mean centroid is the only probe that touches every write.
Everything else is amortized. Total overhead per write ≈ O(d).

### Class: `TemporalHealthScheduler`
> Multi-frequency topological health scheduler.
**Methods:** __init__, set_baseline, on_write, _run_pulse, _run_heartbeat, _run_diagnostic, _run_deep_scan, status, force_diagnostic, _update_running_mean, _get_buffer_as_array, _compute_composite

---

## Module: `temporal_health_models.py`

**Description:**
temporal_health_models — SchedulerConfig, HealthReport, HealthTier.

Extracted from temporal_health.py to satisfy the Landauer LOC barrier (≤500).
Pure data contracts — zero business logic, zero numpy dependency in this file.

### Class: `SchedulerConfig`
> Controls when each probe fires.
**Methods:** *None*

### Class: `HealthReport`
> Unified health snapshot for a given write count.
**Methods:** is_alert, summary

---

## Module: `thalamus.py`

### Class: `ThalamusGate`
> Sovereign pre-filtering gate for CORTEX.
**Methods:** __init__

---

## Module: `topological_health.py`

**Description:**
CORTEX v7 — Topological Health Monitor (130/100 Sovereign Standard).

Breaks the circular Bayesian confirmation loop: reference signatures (p_0)
are versioned against the exact embedding model that generated them.

If the model mutates (fine-tuning, version bump, swap), the anchor
becomes invalid and MUST be recalculated in cold mode before the
SemanticMutator is allowed to continue writing.

The trap: measuring drift with the same embeddings being drifted
creates a self-confirming loop. This module enforces epistemic hygiene.

### Class: `AnchorInvalidError`
> Raised when attempting to measure drift with an invalidated anchor.
**Methods:** *None*

### Class: `TopologicalAnchor`
> Frozen reference signature for topological health measurement.
**Methods:** *None*

### Class: `TopologicalHealthMonitor`
> Guard against circular Bayesian confirmation in topological drift.
**Methods:** __init__, compute_anchor, validate_anchor, needs_recalibration, _validate_and_prepare_array, _calculate_drift_metrics, measure_drift, active_proxy, _compute_metrics, _check_correlation_divergence

---

## Module: `valence.py`

**Description:**
CORTEX v6+ — Emotional Valence Tagging (Amygdala Artificial).

Strategy #3: Memories with emotional charge are stored more
strongly. The amygdala doesn't just tag — it AMPLIFIES consolidation
via noradrenaline-mediated LTP enhancement.

Valence spectrum:
  +1.0 = Critical lesson (error that cost hours)
   0.0 = Neutral decision
  -1.0 = Anti-pattern (confirmed failure to avoid)

Both extremes (+1 and -1) get HIGHER energy than neutral.
Anti-patterns are as valuable as patterns.

### Class: `EmotionalTag`
> Discrete emotional categories for memory tagging.
**Methods:** *None*

### Class: `ValenceRecord`
> Emotional valence metadata for an engram.
**Methods:** energy_multiplier

### Public Functions
- `classify_valence()`
---

## Module: `void_detector.py`

**Description:**
CORTEX v7+ — Epistemic Void Detector.

Strategy #12: Before the agent responds, analyze the *topology* of
the retrieval results to determine whether it truly knows the answer,
lives in a fog zone, or faces an absolute epistemic void.

Taxonomy of Not-Knowing:
  CONFIDENT        → Dense cluster, high similarity, fresh data
  FOG_ZONE         → Sparse region, low candidate density
  VOID_ABSOLUTE    → No candidates above minimum similarity
  STALE_KNOWLEDGE  → Candidates present but energy/age indicates decay
  CONTRADICTION    → Two+ high-similarity engrams with opposing content

Biological analogy: the prefrontal cortex inhibits response when the
hippocampus returns a low-confidence match. This module IS that inhibition.

Derivation: Ω₃ (Byzantine Default) + Ω₁ (Multi-Scale Causality)

### Class: `EpistemicState`
> The agent's epistemic relationship to a query.
**Methods:** *None*

### Class: `ConflictPair`
> Two engrams with high similarity but opposing content signals.
**Methods:** *None*

### Class: `EpistemicAnalysis`
> Result of the epistemic void analysis for a query.
**Methods:** is_safe_to_respond, badge

### Class: `EpistemicVoidDetector`
> Detects epistemic voids, fog zones, contradictions, and staleness.
**Methods:** __init__, analyze, _detect_contradictions, _detect_negation, _check_staleness, __repr__

### Public Functions
---

## Module: `working.py`

**Description:**
CORTEX v5.3 — L1 Working Memory (Sliding Window).

Volatile, token-budgeted buffer that retains the N most recent
interaction events. When the budget overflows, oldest events are
evicted and returned for compression into L2 (Episodic Vector Store).

No I/O. No async. Pure in-memory speed.

### Class: `WorkingMemoryL1`
> Token-budgeted FIFO sliding window for short-term context.
**Methods:** __init__, _calculate_priority, add_event, get_context, get_access_frequency, clear, snapshot, restore, current_tokens, max_tokens, utilization, event_count, __len__, __repr__

---
